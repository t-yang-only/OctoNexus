# 待指导任务技术指导（NM-CUR-181，只读验收 + 等待跳过 + 开工指导）

- claim：`docs/agents/claims/2026-09-14-cursor-local-T-guidance-181.md`
- 盘点：登记台 31 行——done 22 / review 2（T-sec-001 待用户 key 轮换；T-group-003 已由 187 独立复验转 review）/ doing 4（T-pool-001 / T-quota-001 / T-acct-001 / T-deploy-001，均他车道在途或已开工）/ todo 4（T-pool-002 / T-route-002 已由 186 实施转 done 待 ledger 落行 / T-proto-001 / T-deploy-002，全部前置未过门或待落账）。**无自由 todo，无可独立交付的未完成问题，按用户指令指导后停止。**
- 本轮零业务代码改动（仅指导报告 + 台账四件套同步），不碰 `docs/agents/ledger.md`（避让 184/187 并行会话）。

## 1. T-group-003 只读验收（187 转 review 有效，不重复转状态）

- 在途完整性（只读 Grep，未改文件）：`web/src/api/group.ts` 双引用（`child_group_id?` + `GroupItemInput` 双引用）；`utils.ts` 双引用键（`g:<grant>`/`c:<child>` + `matchesMemberKey` 旧裸数字兼容）；`Editor.tsx` `ChildPickerSection`（候选除自身，防自引/成环由后端校验）；`Card/ItemList` 子分组徽标；三语 `nestedHint/addChild/childBadge/childHint/childGroupFallback/noChildCandidate` 三文件在位。
- 本轮实测：`npx tsc --noEmit`（web 目录）EXIT 0；`npx eslint` 本轮 6 文件 0 错；`go build -tags=jsoniter .` EXIT 0；`go vet ./...` EXIT 0；`go test -count=1 ./internal/relay/ ./internal/op/ ./internal/model/` 三包 ok。
- 结论：171 实施完整在途未灭失，187 转 review 有效；本会话不重复改 ledger、不抢 F 车道。

## 2. T-pool-001 等待指导后跳过（标记未完成）

- 缺口实证：唯一口径文件 `docs/requirements/2026-09-13-号池管理选型.md` 不在树（`Test-Path=False`），174/176 两代写行在途但本窗口零交付。
- 终裁沿用：NM-CUR-182 残门作废——需求台无"仓库号池设计"需求（R-pool-002 为官方账号转发），报告需求改立 `R-pool-003` 待用户确认是否真要仓库选型报告；T-pool-001 行维持 doing 作收敛出口，与部署测试脱钩。
- 等待指导事项（需用户或调度拍板，需指导任务按我方想法指导如下）：
  1. R-pool-003 去留：要则按 175 四节口径写一份即关行（候选清单 + 合规红线 + 并入映射 + 优先级一句话），不要则直接关 T-pool-001；
  2. T-pool-001 关行方式：174/176/181 三代写行二选一收敛，避免双写同一报告；
  3. T-sec-001 key 轮换时机（阻塞 T-deploy-002 上线门，他人不可代办）。
- 本分支按指令登记等待指导后跳过，不代写（避让 174/176 代写报告）。

## 3. 开工指导：T-deploy-001（182 已开工，183 已完成 D1）

- 发布候选 = R1 冻结全 done 项（余额链/归零停用/读侧/探测/跳转页/分组嵌套 + group-002 选路）。
- D1–D9 按 `docs/worklog/2026-09-13-收口部署测试规划152.md` §4 执行；D2 隔离导入对齐 136 基线（channels 12 / keys 36 / models 259 / grants 264 / groups 340 / items 615 / llm_infos 132），失败即停；D8 用旧形状库跑迁移 001–013 全链 + 重跑幂等。
- 红线：备份只读不贴原文、隔离库 + 隔离端口、测后整删、密钥不提交。
- 其余车道：B-quota-001 / C-acct-001 按 163 清单属 R2 并行；A-route-002 已由 186 实施待 ledger 落 done；G-proto-001 前置 W1 全过后开工；T-deploy-002 前置 deploy-001 过门 + 用户拍板 + sec-001 轮换。

## 4. 停止点

- 下轮开工条件（按优先级）：① T-deploy-001 D2–D9 取证；② 用户拍板 R-pool-003 去留后关 T-pool-001；③ B/C 车道完成即验收 quota-002 链路 / acct-001。
- 本会话指导后停止，不轮询等待。
