# T-group-002 验证收尾165（NM-CUR-165）

- claim：`docs/agents/claims/2026-09-14-cursor-local-T-group-002-165.md`
- 登记台：T-group-002（A 车道 W1#2；162 已 done，本轮为验证收尾不改状态）
- 前置：T-group-001 done（158 指导收口）

## 1. 盘点结论（避免与 162 单主冲突）

- 实测树上：`op.FlattenGroupItems` + `flattenInto`（DFS 展平/路径回边剪除/超 MaxDepth 截断/按成员行 ID 去重/缺失引用跳过）+ `groupSnapshot` 子成员 Available 回填 + `model.Group.WithItems` + `relay.PickGroupItem(group, flat)` + handler 每轮展平后传入，均已在树（158/161/162 线）。
- `internal/op/group_flat_test.go` 在 `git ls-files --others` 中可见但 `Get-ChildItem` 不可见（本机大小写/编码 listing 抖动），`go test -list` 显示 op 侧仅写侧 6 例，Flatten 四单测未被 test binary 列出——判为**未落盘到可测路径**，不与 162 claim 争功，记为环境侧未完成问题。
- `internal/relay/group_pick_test.go` 在树且 5 例全绿（冷却跳位/亲和保持/手动直选/空表零值/去重命中）。
- `internal/relay/group_tree*.go` 已不存在，与 164 清理记录一致，无死引用。
- `route.go` 232/245 双 `itemOf` 为早期 grep 误报，实盘仅一份 `itemOf` + 一份 `PickGroupItem`，vet 通过。

## 2. 本轮增量（最小收口，不重写展开算法）

- `internal/relay/group_pick_test.go` 追加 1 例：`TestPickGroupItemManualChildRefZero`——manual 下 `ActiveItemID` 指向子分组引用行时回零等待（引用行不在展平平面表内；整块选中语义未定义前冻结为确定行为，由 handler 按无目标等待，不误选块内首叶）。
- 删除本会话误建的 `internal/relay/route_nesting_test.go`（与在树 `group_pick_test.go` 5 例重复），保留单主测试文件，避免双写。
- 未改：T-group-001 模型/迁移；T-group-003 前端（169 在途的 `web/src/api/group.ts` + 三语 `nestedHint` 保持不动）；B/C/E 车道 doing 线。

## 3. 验证（GOTOOLCHAIN=local，修复 go-build 缓存缺目录抖动）

- `go vet ./internal/op/ ./internal/relay/ ./internal/model/` EXIT 0
- `go test ./internal/op/ ./internal/relay/ -count=1` ok（relay 6/6 含新增 1 例）
- `go test ./internal/op/ ./internal/relay/ -count=3` ok
- `go vet ./...` EXIT 0；`go build -tags=jsoniter ./...` EXIT 0；`go test ./... -count=1` 全包 ok（7 有测包全绿）

## 4. 登记状态

- T-group-002 行维持 **done（162 单主）**，本轮仅追备注，不改状态、不抢认领。
- 本 claim 165 关行（验证收尾完成）。
- 未完成问题：无（Flatten 四单测在途归 162 单主，本轮不另立案）。
- 停止点：登记台无自由 todo（7 个 todo 均有前置/他车在途：group-003 有 169 在途；pool-002/route-002/proto-001 前置 W1#3/W2#2/W3 未过；deploy-001 唯一残门=pool-001 报告；deploy-002 前置 deploy-001+用户拍板+sec-001 轮换；sec-001 review 只能用户执行），doing 3 行归 B/C/E 车道，按指令先停止。

## 5. 需要指导任务的指导（按本会话想法，纯文本指导不落盘抢线）

- T-pool-001（deploy-001 唯一残门，165 代写在途）：照 170 四节大纲写一份报告即过门——候选清单（ChatGPT/Gemini 各 2-3 个，字段统一为协议/登录方式/额度可见性/封号风险/维护活跃度，验证码绕过一票否决）+ 合规红线（仅官方 OAuth/扫码，密文落库，日志绝不出现 Key）+ 并入映射（Channel 四件套 + 监控凭证映射 + QuotaZeroStop + 跳转页兜底）+ 优先级（一句话：先接哪一个+为什么+风险，OpenAI/Gemini 先行 Claude 后续）。文件唯一口径 `docs/requirements/2026-09-13-号池管理选型.md`，在树非空壳即解锁 deploy-001。
- T-group-003（169 在途）：本轮已避让 `web/` 四文件；169 收口后若需补键，用新键名（如 `childAvailableHint`）别复用 `nestedHint` 同区；树形组件/选择器属 169 范围。
- T-sec-001：只能用户轮换线上 key + 确认旧备份处置，deploy-002 上线前置不变，任何会话不代办。
