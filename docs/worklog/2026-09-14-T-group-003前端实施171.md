# 2026-09-14 T-group-003 分组嵌套前端实施（NM-CUR-171）

- 台账：`NM-CUR-171`｜路由：`auto-skills` 仅命中 `using-superpowers`｜认领 claim：`docs/agents/claims/2026-09-14-cursor-local-T-group-003.md`
- 前置门：T-group-001 done（158）+ T-group-002 done（162），门已开；前端此前零 `child_group_id` 字段（Grep 实证），无并行冲突
- 旧 120 claim（2026-09-13-cursor-local-T-group-003.md）为前置灭失期占位，产出已回退留痕；本轮为门开后实质实施

## 1. 后端形状核对（只读，未改 Go）

- 读取 `GroupItem`：`channel_grant_id?` + `child_group_id?` 双可选 + `child_group_name`；提交 `GroupItemInput`：`{channel_grant_id, child_group_id}` 双必填互斥（0 表不引用）
- 语义冻结口径（T-group-002）：子分组选路展平，冷却/亲和按顶层持有，available=子树任一可转发

## 2. 前端改动

| 文件 | 改动 |
|---|---|
| `web/src/api/group.ts` | `GroupItem` 加 `channel_grant_id?/child_group_id?/child_group_name`；`GroupItemInput` 双引用必填 |
| `web/src/components/modules/group/ItemList.tsx` | `SelectedMember` 加 `kind/child_group_id`；子分组成员挂「子分组」徽标+副标题回退 |
| `web/src/components/modules/group/utils.ts` | `memberKey` 双引用区分（`g:<grant>`/`c:<child>`）+ `matchesMemberKey` 兼容旧裸数字键 |
| `web/src/components/modules/group/Editor.tsx` | 新增 `ChildPickerSection`（候选=除自身的全部分组，搜索+排序；防自引/成环由后端校验）；`useGroupList` 取候选；`initial.groupId` 透传；members Tab 改纵向滚动并挂 `nestedHint` |
| `web/src/components/modules/group/Card.tsx` | `displayMembers` 双引用映射；拖拽/移除/编辑提交全走双引用载荷；编辑弹窗透传 `groupId` |
| `web/src/components/modules/group/Create.tsx` | 创建提交双引用载荷 |
| 三语 `locales/*.json` | 补 `addChild/childBadge/childHint/childGroupFallback/noChildCandidate`（nestedHint 骨架由 158-7c 落盘，本轮在其上补齐） |

## 3. 验证

- `tsc --noEmit`：0 错（web 目录执行）
- `pnpm lint`：仅 2 处既有错误（`log/FilterBar.tsx:38`、`ui/chart.tsx:367/369` react-refresh，均非本轮文件，`git diff --name-only` 无交集）
- 后端未动：`go build -tags=jsoniter .` 过、`go vet ./internal/... .` 过、`go test -count=1 ./internal/op/ ./internal/server/router/` 绿
- 编码事故记录：中途曾用 PowerShell `Get-Content -Raw` 回写 `ledger.md` 致全表 UTF-8→系统编码损坏（git diff 67 行异常），已 `git checkout` 还原并改用 `StrReplace` 单行改动（diff 1 行正常）。教训：中文台账文件一律经编辑工具改，不经 shell 回写

## 4. ledger 同步

- T-group-003：`todo` → `doing`（认领行 NM-CUR-171，claim 见上）；完成门=本报告验证节 + 用户/调度验收后转 done
- 未动项：T-pool-001（165 代写在途）、T-quota-001/T-acct-001（B/C 车道 doing 在途）、T-group-002（162 done）、164/167/168 在途行未触碰

## 5. 遗留

- 子分组候选的可用性徽标未展示（后端 `GroupList` 未带子组 available，本轮选择器仅列名称；展示待后端 `ChannelGrantCandidate` 式聚合或复用 `groupSnapshot` 口径）
- 成环/自引用的前端即时提示未做（以后端 500 为准，与 claim 口径一致）
- 手动模式 ActiveItemID 指向子分组引用行时，卡片点选沿用既有 `item_id` 路径；块内首叶子语义以 168 claim 冻结口径为准，未另行分支
