# T-route-002 L5 均衡热路径落地 213

- claim：`docs/agents/claims/2026-09-14-cursor-local-T-route-002-L5-213.md`
- 调度：NM-CUR-201 L5；按 212-G7 开工单执行。Ownership 仅 `internal/relay/handler.go`、`internal/relay/route.go`、`internal/relay/balance*.go`；为保持设置默认关闭，补充 `internal/model/setting.go`、`internal/op/setting.go` 的设置键/测试缓存入口。
- 基线 HEAD：`10cb2e4 docs: guidance 212 - seven waiting items adjudicated with work orders`

## 目标

将已有 `rankCandidates` 加权候选定序接入 relay 热路径，同时保留关闭 flag 时原有行为；冷却、探测、亲和和路由状态仍由顶层 `RouteState` 负责。

## 交付

- `SettingKeyRouteBalanceEnabled`：默认值 `false`，设置缺失或非法时 fail-closed。
- `RouteState.balanceRound`：按顶层分组保存加权轮询计数，`ResetRouteState` 时随状态清理。
- `pickGroupItemHot`：handler 热循环统一入口；flag 关闭走原 `pickGroupItem`，flag 开启走 `pickGroupItemBalanced`。
- `pickGroupItemBalanced`：仅用 `rankCandidates` 重新排列候选，再复用原选路逻辑，不重复实现冷却/亲和/探测。
- `route_balance_test.go`：覆盖 flag 关闭原路径、flag 开启轮转、冷却行为保持、设置缺失/非法 fail-closed。
- 修复并行 WIP 暴露的迁移编译错误：`internal/db/migrate/011.go`、`013.go` 将 `ChannelGrantID` 按当前模型指针字段传值；这两处属于已有模型迁移WIP的必要编译兼容修复。

## 验证

- `go vet ./internal/relay/ ./internal/model/ ./internal/db/...`：通过。
- `go test ./internal/relay/ -count=1`：通过。
- `go test ./... -count=1`：全包通过。
- `go test ./internal/relay/ ./internal/op/ ./internal/model/ ./internal/db/migrate/ -count=3`：首次暴露并行 `backup_raw_test.go` count 隔离问题（`TestRawCreateSkipsAssociations` rows=0）；该失败属于 L1 `backup.go`/测试WIP，不在 L5 ownership，本轮未改动。
- `go build -tags=jsoniter ./...`：通过。
- `ReadLints`：近期编辑文件无 linter errors。

## 遗留/下一步

- `go test -count=3` 的 `backup_raw_test.go` 失败应由 L1 处理，原因是其内存 SQLite DSN 未带唯一序号；本轮不碰 L1 文件。
- L5 已完成，等待按 201 §2 先于 L6 合并；不提交，待调度统一归档。
