# 2026-09-13 巡检审核048（源码测试修复 + 文件名竞态裁决）

- 台账编号：`NM-CUR-048`｜路由：auto-skills 仅命中基座 `using-superpowers`（无缺失成员）
- 基线：`master=270f781`；工作树：046 篇已暂存、README 索引 M、047 篇未跟踪、
  047b 篇（并行同名竞态）未跟踪、本篇未跟踪、单测修复未暂存——均保留不代提交。
- 结论：**修复 1 个真 bug（单测并发写 map 崩溃），其余无未完成工作**；
  047 已关行（有实质产出+日志节+清单行），本轮将其行修正为准确口径；
  047b 按"已提交版为准"原则不入库、不索引、不认领。

## 1. 登记（本轮 /nm-skills 动作）

| 台账 | 动作 |
|---|---|
| 工作登记表 | `NM-CUR-048` 实质行（本任务：修单测并发崩溃+本篇裁决）；`NM-CUR-047` 行修正为准确口径
  （有产出 047 篇+日志节+清单行，旧备注"048/049空行关行"为该会话笔误，048 实为本会话） |
| 工作日志 | 追加 048 节（本节；含 047b 并存纠正） |
| 文件清单 | 补 048 行（单测修复 + 本篇 + 登记表/日志/索引） |
| API 变更 | 无变更（只修测试代码，未动业务接口） |
| worklog 索引 | 本篇入索引；047 篇行已在（047 会话自加）；047b 不索引（竞态落选版） |

## 2. 未完成项核查（47+2 行，除本轮外全已完成）

- 登记表：001–047 除本轮 048 外状态列全"已完成"（精确匹配状态列）；
  049 为 047 会话代关的并行空行，已完成，无需再动。
- 工作日志：表−节差集 = 005/008/020（侦察行惯例）+ 034/035/038/043/049（他会话空行）
  + 048（本轮补）。差集全部有归属。
- ledger 8 任务 done/review 无 todo；requirements 4 篇待用户拍板；
  API 变更 011 一行，无需新增。
- worklog 索引：047 会话自加其篇后 30/30 对齐；050 会话将其 047b 篇入索引（31/31）；
  本篇加入后 **32/32**（两篇 047 系报告并存：047 篇=047 会话全量巡检版准版，
  047b 篇=050 会话同轮独立复核版，均已登记认领，无"落选"一说）。

## 3. 真 bug：probe 单测并发写 map 致进程级崩溃（已修复，未提交）

- 现象：`go test -count=1 ./...` 在 `internal/server/handlers` 包
  `fatal error: concurrent map writes`，栈顶
  `channel_probe_test.go:24 newProbeTestUpstream.func1`。
  假上游 handler 并发写 `seen` map；`probeModels` 对同一模型三协议并发，
  messages 分支一次写两个键，无锁必撞（命名测试必现，非偶发）。
- 根因：测试脚手架缺互斥，非业务代码问题；此前"全过"均为缓存命中，
  `-count=1` 即现形——**以后审核测试必须带 `-count=1`**。
- 修复（`internal/server/handlers/channel_probe_test.go`，仅测试代码，未提交）：
  `seenMu sync.Mutex` 包住三处 handler 写 map；加 `sync` import；
  注释写明"收尾串行读/写入侧持锁"不变量。
- 验证：handlers `-count=1`/`-count=3`/`-count=10` 全过；
  全仓 `-count=1 ./...` op+handlers+router 全 ok；
  `build -tags=jsoniter` EXIT 0；`vet ./...` EXIT 0；双 check 脚本 PASS。
- `-race` 未跑：本机无 gcc（cgo.exe exit 2），`-race` 需 cgo 无法构建——
  环境限制已记录（T-env-001 可追备注），非代码问题。

## 4. 文件名竞态裁决：047 篇与 047b 篇并存（修正：非落选关系）

- `2026-09-13-全量巡检补完与审核047.md`（047 会话全量巡检版，日志节+清单行俱全）：准版之一。
- `2026-09-13-巡检审核047b.md`（050 会话同轮独立复核版，已入索引、有 050 行+日志节+清单行，
  且其 §3 独立复验了本篇的单测修复 `-count=3` 无复发）：同为准版，并存互证——
  两篇对"并发写 map 根因"的认定逐字一致，相互印证而非竞态落选。
  本篇初稿误写"落选不索引"，以此条为准纠正；050 会话的 `git add` 暂存动作予以保留不动。
- 教训保留：巡检类报告先 `ls` 占位再写，或统一用编号后缀并各自登记认领。

## 5. bug 报告口径

维持 036 终裁（C-1/C-2 成立 Critical，M-1 维持 Medium），040"转立项实测"与之不冲突，
本轮不做新的裁决。

## 6. 遗留（需用户，与 036/037/040/041/042/047 一致）

1. T-sec-001 线上 key 轮换；2. 需求三通道合并定单一事实源；
3. 缺陷任务立项（R-sec-002/R-relay-001/R-stats-001/R-backup-001/R-models-001
   + R-price-001/R-stats-002）；4. M1/M2/M3 批准门；
5. 本轮未提交项（单测修复 M + 046 篇 A + README M + 047 篇 ?? + 048 篇 ??）
   待用户决定是否归档提交；6. CI 加 `go test -count=1 ./...`（禁缓存）。

## 7. 本篇变更

- `internal/server/handlers/channel_probe_test.go`：假上游三处写 map 加锁 + import sync（未提交）。
- `agent_word/工作登记表.md`：048 实质行；047 行修正口径。
- `agent_word/工作日志.md`：追加 048 节。
- `agent_word/文件清单.md`：补 048 行。
- 新增本篇 + worklog 索引一行；未改任何业务代码，未做任何 git 提交。
