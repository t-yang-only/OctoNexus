# T-proto-001 连接协议转化验证（NM-CUR-200，五类用例矩阵）

- claim：`docs/agents/claims/2026-09-14-cursor-local-T-proto-001-200.md`
- 对象：`internal/relay/protocol.go`（`inspectStreamEvent`/`validateResponse`）+
  axonhub transformer 出入站（openai/anthropic/responses）+ 新增 `internal/relay/proto_matrix_test.go` 6 例
- 结论：**五类矩阵全绿，只读验证零业务代码改动；另登记 1 个 Responses 流终态缺口观察（非阻塞）。**

## 矩阵证据

| # | 用例 | 证据 |
|---|---|---|
| 1 | 非流式/流式语义 | Chat 普通 chunk（含 finish_reason）仍 mid-stream（last=false），仅 `[DONE]` 终结——与提交侧"首事件通过即接管"一致 |
| 2 | Anthropic 流 | `message_stop`→last=true；`content_block_delta`→last=false |
| 3 | usage | chunk 体 usage 三字段自洽（prompt+completion=total） |
| 4 | 错误 | OpenAI/Anthropic error 事件均判失败不可提交；SSE 事件名与正文类型错乱判失败 |
| 5 | tool call | `input_json_delta` 的 type/partial_json 透传不丢 |
| + | Responses 终态 | error/length/cancelled 判 `ResponseError`；stop 放行；nil 响应拒绝 |

## 审计发现（观察级，非阻塞，不改码）

- `validateResponse` 只覆盖 Responses 协议的终态；Chat/Anthropic 的非流式失败终态依赖
  transformer `TransformResponse` 侧抛错（`upstream.go:92` 有 `err: body` 包装证据）。
  口径成立但不对称——如后续 transformer 升级吞掉终态错误，此处是第二道门的缺口位置，已登记。
- 验证：`go test -count=3 ./internal/relay/` 全绿；`go build -tags=jsoniter` 过；`go vet ./...` 过。

## 台账

- T-proto-001：todo→done 建议（五类矩阵全绿；待用户/调度拍板）。遗留：真实上游三协议实转仍沿 136 结论复用，
  本轮隔离红线内不打真实上游。
