# 待指导任务技术指导（NM-CUR-170，只读指导 + 冲突避让）

- claim：`docs/agents/claims/2026-09-14-cursor-local-T-guidance-170.md`
- 盘点：登记台 31 行——done 21 / review 1（T-sec-001，用户动作）/ doing 3
  （B-117/C-128/E-135 他车道）/ todo 7（全部前置未过门或他车在途）。
  **无自由 todo，无未完成且可独立交付的问题，按用户指令先停止前先输出指导。**

## 1. T-pool-001 号池选型报告（deploy-001 唯一残门）指导

- 现状：E 车道 135 自认领后零交付；163/166 已决议"代写或收回改派"二选一，
  报告唯一口径 `docs/requirements/2026-09-13-号池管理选型.md`（候选+合规红线+并入映射+优先级）。
- 指导（代写会话照此四节写，一份报告即过门，禁再开新选型行）：
  1. **候选清单**：ChatGPT/Gemini 官方账号池开源候选各列 2–3 个，字段统一
     （协议/登录方式/额度可见性/封号风险/维护活跃度），只收录"官方授权接入"
     合规项，验证码绕过/逆向/cookie 盗用一票否决并写明红线。
  2. **合规红线**：与 T-acct-001 同口径——仅走官方 OAuth/扫码授权；
     凭据密文落库（AccessCipher/RefreshCipher），日志/输出绝不出现 Key（R-sec-001）。
  3. **并入映射**：候选账号→octopus 渠道模型（Channel/ChannelKey/ChannelModel/
     ChannelGrant 四件套）+ 监控凭证映射（baseURL/monitorToken/useProxy，
     复用 quota-001 接线清单）+ 归零停用链路（QuotaZeroStop）+ 跳转页
     （T-acct-005）作为人工兜底。
  4. **推荐优先级**：OpenAI/Gemini 先行、Claude 后续（与 T-pool-002 W3#1 一致）；
     给出"先接哪一个+为什么+风险"一句话结论。
- 验收线：文件在树、非空壳、四节齐、红线明确；过门即解锁 T-deploy-001 D1-D9。

## 2. T-group-003 i18n 冲突避让口径（169 在途，不并行写同一文件）

- 现状：169 会话已把 `web/src/api/group.ts`（嵌套口径注释）+
  三语 locale `group.form.nestedHint` 写 dirty，本轮实测三文件均未提交。
- 指导：本会话**不触碰**上述四文件；169 收口时另需补的键若与
  `nestedHint` 同区，改新键名（如 `group.form.childAvailableHint`）或等 169 落盘
  后再改，避免双写冲突。树形展示/子分组选择器组件属 169 范围，本轮不建新组件。
- 状态：T-group-003 行维持 todo（169 进行中），不代认领、不改状态。

## 3. T-sec-001（唯一 review，只能用户执行，不代办）

- 待用户动作：轮换线上渠道 key + 确认 `../octopus-本地数据/` 旧备份处置；
  T-deploy-002 上线前置"未轮换 key 不得上线"维持。
- 指导：本会话不碰密钥材料，仅重申门禁。

## 4. 停止点

- 本轮零业务代码改动（仅指导报告 + 台账备注 + 四件套同步）。
- 下轮开工条件（按优先级）：① T-pool-001 报告落盘 → T-deploy-001 D1-D9；
  ② 169 收口 T-group-003；iii B-117/C-128 他车道完成即验收 quota-002 链路/acct-001。
