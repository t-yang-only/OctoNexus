# 在途车道指导（NM-CUR-224，201-L1~L8 状态快照 + 三线指导）

- claim：`docs/agents/claims/2026-09-14-cursor-local-T-guidance-224.md`
- 登记台（15:47 实测）：done 25 / review 1（T-sec-001 用户动作）/ doing 4
  （pool-001 收敛出口、quota-001=L3/217 在途、acct-001=L4/128 在途、deploy-001 等拍板）/
  todo 1（pool-002 实现门未过，L8 预研 204 在树）。**无自由可领任务，按用户指令指导后停止。**
- 本轮实测处置的两个全仓级阻塞：
  1. **`route.go` PickGroupItem 重复声明（已修，build 红→绿）**：15:35 `go build` 因
     L5 在途文件整段字节相同的两份 `PickGroupItem` 失败（全仓阻塞级，非单车道问题）。
     已删重复份，`go build ./internal/relay/` EXIT 0。披露：claim 本意零碰代码，
     但此为确定性重复声明，保留首份、删次份，零语义变更；211 知悉即可。
  2. **`route_balance_test.go` 未用 `time` import（已删，vet 红→绿）**：纯 import 清理，
     零行为改动；relay 测试本体语义未动。
- 其余状态：L3 结构环已由 217 自行消解（扫描循环移入 `internal/task/quota_scan.go`，
  实测 op→health 无边、task 层接线成立）——指导确认方向正确，见 §2-1。
- **`backup_raw_test.go` 注释乱码第三次复发**：213 轮（14:15）还原后被 195 会话
  14:20:53 整写再犯（GBK 通道）。文件归 183/193 归档线，224 不再代修（防编辑战），
  指导见 §2-2。

## 1. 全仓验证快照（15:52，本轮实测）

`go build ./...` EXIT 0；`go vet ./...` EXIT 0；`go test -count=1 ./...`：
八包 ok，唯一 FAIL=`relay`：`TestPickGroupItemBalancedFlagOffMatchesLegacy`
报 `flag off cooled pick = 13, want 12`——L5 在途 WIP 语义未收敛，归 211（§2-3），
224 不代改测试语义。task/quota_scan 新包测试 ok（L3 形状成立）。

## 2. 三线指导

### 2-1 → NM-CUR-217（L3/quota-001）：分层方向正确，收尾三件事
- 你已把扫描循环放 `task` 包（`internal/task/quota_scan.go` 引 `op.QuotaScanTargets`+
  `health.ScanOneChannel`），op 不再 import health——`op→health→rhttp→op` 环已断。
  **维持此形状，不要再往 op 里塞采集依赖**（rhttp→op 是 117 期既有边，动不起）。
- 剩余按 163 清单收口：④集成 2 例（归零→停→审计 / 未知→不停）挂 task 层
  （仿 `quota_test.go` 直连内存库 + `resetQuotaScanStateForTest` 指纹隔离）；
  ⑤Auto-Model responses 超时疑点给结论或登记。
- 阈值事件与归零停用是两条判定线：事件走告警（U-alert-001），停用走
  `remaining<=0`（QuotaZeroStop 守卫，147/157 已保负剩余不出现）。
- 新 setting 键 `quota_scan_interval`（0=停用）+ 默认 5min 已注册，形状好。

### 2-2 → NM-CUR-195（L2/group-003）：两个红线
- `internal/op/backup_raw_test.go` **不在你车道**（F 车道禁动 go 文件；该文件归
  183/193 归档线）。你 14:20:53 的整写第三次覆盖回乱码注释。请停手；
  以 213 轮中文注释版为基准（193 若需可再代还原一次）。
- 你的写文件通道有编码缺陷（中文变 `?`）：落盘一律走工具 Write/StrReplace（UTF-8），
  **不要 `echo`/重定向/PS 脚本内嵌中文**——195 产物乱码已两次（本会话线），
  本轮 group-003 终验本身（tsc/eslint/build/vet → review 转 done）按 212-G6 工单继续。

### 2-3 → NM-CUR-211（L5/route 热路径）：合入前四件事
- `route.go` 重复 `PickGroupItem`（整段字节相同）已去重；`route_balance_test.go`
  未用 `time` import 已删（224 代修，零行为改动，知悉即可）。
- **当前唯一测试红（15:52 实测）**：`TestPickGroupItemBalancedFlagOffMatchesLegacy`
  `flag off cooled pick = 13, want 12`。定位方向：用例前段 `recordRouteFailure(…,11,…)`
  写入的冷却/轮转状态延续到 flag-off 断言；且 `ResetRouteState(9201)` 是 defer 而非
  前置——首轮受测试进程内残留状态影响。**建议：断言前 `ResetRouteState(9201)` 先行，
  或冷却断言改用独立分组 ID**——语义归你定。
- `swapSetting` 的 `had == nil` 表达"曾有值"依赖 `SettingGetString` 返回 nil error
  语义——补一行还原断言（`saved` 回填后 flag 仍 false）更稳。
- 合入门槛：`go test -count=3 ./internal/relay/ ./internal/op/ ./internal/task/` 全绿 +
  flag 关闭态与 `pickGroupItem` 原路径等价（即本 §2-3 红例转绿）。

## 3. 门禁重申（无需动作）

- pool-002：实现门 W1#3+W2#2 未全过（acct-001 doing），预研 204 已交，不开工。
- deploy-001/002、sec-001、pool-001 收敛：等用户拍板/轮换，不代办。

## 4. 停止点

本轮交付：全仓 build/vet 阻塞解除（route 去重 + import 清理）+ 三线指导 + 快照。
下轮看：217 集成 2 例、211 flag-off 转绿、195 终验收口、用户 R-pool-003 确认。
