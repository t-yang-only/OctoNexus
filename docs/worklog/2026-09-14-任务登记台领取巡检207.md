# 2026-09-14 任务登记台领取巡检207（NM-CUR-207：无空闲 todo，指导后停止）

- 台账编号：`NM-CUR-207`｜路由：`auto-skills` 仅命中基座 `using-superpowers`
- 认领文件：`docs/agents/claims/2026-09-14-cursor-local-T-patrol-207.md`
- 盘点：ledger 31 行 done 24 / review 2 / doing 4 / todo 2，全部有主或门锁，无自由 todo；claims 与登记表尾部 225 行核对，203/204/205/206 四行"进行中"均有活跃会话，不抢
- 零业务代码改动；未改 ledger 状态行（修改权归 201 调度与对应车道）

## 本轮实测（只读验证，支撑指导结论）

- `go build -tags=jsoniter .`：EXIT 0
- `go vet -tags=jsoniter ./internal/...`：EXIT 0
- `tsc --noEmit`（web）：EXIT 0
- 全量测试未跑（不碰 201 文件锁表内文件的编译态；沿用 183/184/193/198/200/206 落盘实测结论）

## 指导（按 201 八路分工的收口顺序与拍板项，只读指导不代写）

1. **合并顺序维持 201 §2 口径**：L2（web 9 文件）→ L5（handler/route/balance）→ L6（transformer+矩阵，合前必 `go test ./internal/relay -count=1`）→ L3/L4（分属 task/health 与 handlers 包可并行）→ L1（最后合，收全包验收结论）；L7/L8 纯文档随时合。当前 WIP 已全绿（build/vet/tsc 实测），合入顺序不要因并行先后颠倒。
2. **T-deploy-001（L1）**：183 D1–D9 全绿 + 193/198 复核已齐，唯一等的是用户拍板转 done；`internal/op/backup.go` rawCreate 修复与 `backup_raw_test.go` 随 L1 归档入库（commit 信息带 T-deploy-001，密钥扫描必过）。
3. **T-group-003（L2）**：184 复验 + 本轮 tsc 0 错，195 终验通过即可 review→done；web 9 文件是当前唯一跨车道干净可先合的交付。
4. **T-route-002 热路径（L5）**：feature flag 默认关是硬红线——未开 flag 时行为零变化；199 三单测（round 序列/冷却回位/延迟不跨档）必须随 flag 开关两侧都过。
5. **T-pool-001（L7）**：205 已按 182 终裁交付一页 R-pool-003 草稿关 174/176/181，后续不再双写选型报告。
6. **T-pool-002（L8）**：只做 204 预研草稿（轮询/均衡/协议三节），W3#1 门未开禁实现。
7. **用户拍板四项**（不派子 agent）：deploy-001 转 done、R-pool-003 要不要仓库选型报告、T-sec-001 key 轮换时机、T-deploy-002 开工。

## 台账同步

- `agent_word/`：登记表 207 行 started→已完成 + 工作日志节 + 文件清单行 + 技能建议时间戳
- `docs/worklog/README.md`：追加本报告索引行
- 本轮关行后停止，不再另起支线（用户指令：无未完成问题先停止）
