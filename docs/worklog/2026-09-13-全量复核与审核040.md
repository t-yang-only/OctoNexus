# 2026-09-13 全量复核与审核040（/auto-skills+/nm-skills）

- 台账编号：`NM-CUR-040`（本会话；登记表手动附行，避开与并行会话的脚本编号竞态——034/035/036/038 四次空行教训）
- 基线：本地 `master`=`dc0c4ab`，远端 `octonexus/master`；本报告只读核验+台账文档，**无业务代码变更**
- 方法：auto-skills 仅命中基座 `using-superpowers`（单一巡检任务）；按"登记→查看记录→同步工作→测试→审核"执行

## 1. 登记

本会话开始即口头登记（未调脚本：034–038 已证明多会话并发调脚本必产空行）。
关账时登记表手动附 040 行（状态已完成），与 039 同例（039 亦手动附行）。

## 2. 记录核对（本轮实测）

- 登记表：001–040 共 **39 行**（028 从未分配；032 占位行已由 039 合并删除）。状态列 39 行全"已完成"，**无进行中残留**。
- 工作日志节：001–007、009–011、013–019、021–027、029–031、033、036、037、039 有节；
  缺 008/020（侦察行历史惯例）、028（从未存在）、032（占位行已删除）、034/035/038（并行空行无内容可补）。
  本篇补 **032 补记节**（见 §5），034/035/038 维持"无节"（空行确实无内容，强补即造假）。
- ledger：8 任务 7 done + T-sec-001 review（待用户轮换 key），无 todo。
- API变更.md：仅 011 一行；039 的 monitor 三件套复用既有 `/api/v1/channel/stats`，无新接口，无需新增。
- 文件清单：039 行已登 monitor 六文件；034/037/036 行齐备；本篇补 040 行。
- worklog 索引：039 篇已在索引（"new-api 统计建模登记与功能展示"行），036/037/034 行齐备；本篇入索引。

## 3. 测试（本轮实测，Go 1.27）

| 项 | 结果 |
|---|---|
| `go build -tags=jsoniter ./...` | EXIT 0 |
| `go vet ./...` | EXIT 0 |
| `go test -count=1 ./internal/op/ ./internal/server/router/ ./internal/server/handlers/` | 3 包 ok（0.132s/0.143s/0.168s，非缓存） |
| `scripts/check_auto_groups.py` | PASS |
| `scripts/check_protocol_probe.py` | PASS（All static checks passed） |
| `web/ pnpm exec tsc --noEmit` | EXIT 0（含 monitor.tsx 新文件） |

## 4. 缺陷只读抽验（036 纠偏结论的独立复核：M-1、H-4 行号证据）

- **M-1 成立、维持 Medium**：`internal/op/apikey.go:90-109` —— `APIKeyDelete` 用仅含 ID 的空壳 `k`
  调 `Delete` 后，`apiKeyIDMap.Del(k.APIKey)` 删的是 `""` 键（`APIKey` 字段从未填充），真实映射残留；
  `apiKeyRefreshCache` 只增不减。036"401 正确、仅堆积"成立，维持 Medium。
- **H-4 成立**：`internal/server/middleware/cors.go:14` `AllowCredentials=true` 写死，
  `AllowOriginFunc` 中 `allowed=="*"` 直接 `return true`（22–30 行）。组合成立，修法同意 037
 （`*` 时强制 `AllowCredentials=false` + warn）。
- C-1/C-2：036 与 037 结论对立（推翻 vs 恢复成立），属**解释分歧**非事实分歧——调用链事实双方一致
  （UpdateLLMPrice 只刷内存 / 跨天同步落库在 relay 锁内）。本轮不做第三次裁决，转立项后由实施者实测耗时定夺；
  缺陷任务拆分建议沿用 037 的 R-* 五项。

## 5. 032 补记节（占位行已删除，此为历史存档）

> NM-CUR-032（2026-09-13 02:46:05 登记，03:05:00 由 NM-CUR-039 合并删除）：
> new-api 统计建模需求的开工占位行（started 空行）；实质工作由 NM-CUR-039 完成
> （monitor 三件套 + 三语 + 展示 worklog）。占位行删除正确，完工行即 039。
> 此节代原 032 日志节，之后不再追补。

## 6. 本篇变更清单（仅台账）

- `agent_word/工作登记表.md`：手动附 040 已完成行。
- `agent_word/工作日志.md`：补 040 节 + 上述 032 补记节。
- `agent_word/文件清单.md`：补 040 行（本篇+索引）。
- `docs/worklog/README.md`：加入本篇。
- 新增本篇文件。未改业务代码、未调登记脚本（避竞态）。

## 7. 遗留（需用户拍板，持续挂账）

1. T-sec-001 线上 key 轮换；2. 需求三通道合并；3. 缺陷任务立项（037 的 R-* 五项，C-1/C-2 实测定夺）；
   4. M1/M2/M3 批准门；5. 他人未提交改动（monitor 三件套等，已由 039 登记，待验收提交）。
