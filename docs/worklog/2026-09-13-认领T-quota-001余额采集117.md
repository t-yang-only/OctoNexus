# 2026-09-13 认领 T-quota-001 余额采集与阈值告警（NM-CUR-117）

- 台账编号：`NM-CUR-117`｜路由：auto-skills 仅命中基座 `using-superpowers`
- 登记台：`docs/agents/ledger.md` T-quota-001（todo，无认领）→ doing（cursor-local）
- claim：`docs/agents/claims/2026-09-13-cursor-local-T-quota-001.md`（新建加锁）
- 来源：R-quota-001（需求登记.md，待确认）+ T-research-002 移植清单 P1/P2/P4/P5
- 范围：只做采集+告警事件，不含自动停用（停用见 T-quota-002）；只产阈值事件，不做通知通道（通知见 U-alert-001）
- 结论：**实施完成，全绿**

## 1. 未选任务（冲突留源）

- T-pool-001：075/076 claim 在仓，086 等 11 行 owner 在途，不抢；
- T-quota-002：含自动停用动作，需 T-quota-001 数据先行，本轮只做前置；
- T-group-002/003：声明依赖 T-group-001（113 doing 在途），不抢；
- T-sec-001 review：待用户轮换 key；需求六条待确认。

## 2. 实施内容（新增 `internal/health/` 包，4 文件，重写不复制）

| 文件 | 内容 |
|---|---|
| `balance.go` | 包说明 + `BalanceSnapshot/ThresholdEvent` 类型 + 默认 5min 间隔 + `/api/user/self` 路径常量 |
| `balance_parse.go` | `ParseBalancePayload`（宽容字段匹配：精确→模糊、数字/数字字符串、data 嵌套展平、缺剩余推导）+ `FingerprintBalance`（sha256 三元组，P4）+ `BelowThreshold`（阈值<=0 永不触发） |
| `balance_scan.go` | `FetchBalance`（只读 GET，监控凭证 Bearer 分离、代理复用 `rhttp`、30s 超时、1MB 上限、非 2xx/坏 JSON 返回 ok=false）+ `ScanOneChannel`（指纹变化判定+阈值事件） |
| `balance_test.go` | 6 单测：宽容解析 5 分支、指纹同/异、阈值 4 分支、假上游 3 分支、扫描变化+事件+失败零值、URL 拼接 |

- 红线：字段语义借鉴 T-research-002 §1（`newAPIUserHeaders/inferQuota`），逐行重写，未复制 api-monitor 文件；
  失败只记事件（ok=false），不阻断转发热路径；未注册 task（采集调度待 M1 spec 定 channel→凭证映射后接）；
  密钥不入库不贴原文。

## 3. 附带修复（113 会话中间态，不代关 113）

- `internal/op/group_nesting_test.go` 引用 `withOpTestDB`（不存在）致 `go vet/test ./internal/op` 构建失败；
  本轮将其改写为走 `syncGroupItemsOn`（可注入事务的建模函数）+ 补 `gorm` 导入。
  113 的模型层实现（`group.go` 防循环/互斥校验）未动；113 行仍进行中，留其会话收尾。

## 4. 测试（本轮实测，非缓存）

`go build -tags=jsoniter ./...` EXIT 0；`go vet ./...` EXIT 0；
`go test -count=1 ./...` 四包 ok（health/op/handlers/router，含新增 6 单测 + 修复后的 nesting 测试）；
双 check 脚本 PASS；`tsc --noEmit` EXIT 0；密钥扫描 passed。

## 5. 本篇变更清单（未改既有业务代码逻辑，未做提交）

- 新增：`internal/health/` 4 文件 + claim + 本篇；
- 修改：`docs/agents/ledger.md`（T-quota-001 todo→doing）、`internal/op/group_nesting_test.go`
  （中间态修复）、worklog README 索引、agent_word 三件套 + 技能建议。
