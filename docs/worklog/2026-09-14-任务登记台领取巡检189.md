# 任务登记台领取巡检189（NM-CUR-189，只读巡检，无未完成问题则停止）

- claim：`docs/agents/claims/2026-09-14-cursor-local-T-patrol-189.md`
- 目标：按用户指令扫登记台领任务；有问题则登记等待指导+跳过标记未完成后转支线；无未完成问题则停止；有需指导任务则按我方想法指导后完成。
- HEAD：`f0241dc`（docs: dispatch 182 opens T-deploy-001, voids pool-report residual gate as R-pool-003）

## 1. 执行命令/操作

- 读 `docs/agents/ledger.md` 全表（done 23 / review 2 / doing 4 / todo 3）。
- 列 `docs/agents/claims/*.md`（47 个，确认 172/179/183/188 等待巡检行与 174/176 代写行、171/184 前端行、182/183 部署行均有主）。
- 只读验证（不改业务代码，不碰他车道 dirty）：
  - `go vet ./internal/op/ ./internal/relay/ ./internal/model/` → EXIT 0
  - `go test ./internal/op/ -count=1` → ok
  - `go test ./internal/relay/ -count=1 -run "TestRankCandidates|TestPickGroupItem" -v` → 9/9 PASS（含 balance 三例 + pick 六例）
- 新建本轮 claim 加锁 + 本篇 worklog + `docs/worklog/README.md` 索引同步。

## 2. 结果

- 登记台盘点：
  - done 23：docs/env/agents/research×4/quota-002/group-001/group-002/acct-002/003/004/005/test-001/log-002/audit-001~005/route-002（186 加权轮询已 done）。
  - review 2：T-sec-001（用户 key 轮换，不可代办）、T-group-003（171 实施+184 复验待验收）。
  - doing 4：T-pool-001（182 收敛出口，与部署脱钩）、T-quota-001（B/117）、T-acct-001（C/128）、T-deploy-001（182 开工+183 执行 claim 在途）。
  - todo 3：T-pool-002/T-proto-001/T-deploy-002（前置均未过门）。
- 结论：无空闲 todo，无可独立交付的未完成问题。按指令停止，不抢 doing，不碰门禁 todo。
- 上轮偶发 `TestRankCandidatesWeightedOrder` FAIL（rotation covered map[1:true]）本轮复跑已 PASS，判为 flaky/并行污染观察项，不立案（186 claim 内已有同类修复记录）。

## 3. 需指导任务的指导（按我方想法）

- T-deploy-001（182+183 在途）：继续按 152 §4 D1–D9 执行，D2 起以隔离库 rows_affected 对齐 067/136 基线（12/36/259/264/340/615/132）为硬门；D4 余额链与 D5 探测链用 httptest 桩先行，不等 B/C 车道；D8 用旧形状库重跑 001–013 验证幂等。183 执行 claim 归属不变，本轮不另起执行行。
- T-pool-001：维持 182 终裁（残门作废，转 R-pool-003 待用户确认要不要仓库选型报告）；174/176 代写行由 182 会话自行收敛，本轮不代写、不催办。
- T-group-003（review）：等调度/用户验收 171+184 即可 done；后续若返工，只动前端 9 文件，不回写后端。
- T-quota-001/T-acct-001（R2）：按 ledger 163 清单继续，不阻塞 R1 D1–D9。
- T-sec-001/T-deploy-002：sec-001 未轮换 key 不得上线（deploy-002 门禁）维持；deploy-002 前置 deploy-001 过门+用户拍板。
- T-pool-002/T-route-002/T-proto-001：route-002 已 done（balance 纯函数，未接热路径）；proto-001 矩阵先行、pool-002 最后的顺序建议维持（延续 183 指导）。

## 4. 遗留问题

- 无本轮新增未完成问题。观察项：relay 加权单测偶发轮转覆盖 FAIL 一次，复跑即过。
- 工作区 dirty 均为他人在途（Go 8 文件 + web 9 文件 + 大量 untracked claims/health/quota/jump 等），本轮仅新增 claim-189 + 本篇 + README 索引一行，未提交。

## 5. 下一步

- 停止，不轮询等待。下轮开工条件：①183 收口 deploy-001 D1–D9；②group-003 review 验收；③任一门禁 todo 前置过门即按 lane 口径认领。
