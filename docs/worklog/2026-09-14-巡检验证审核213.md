# 登记台巡检与验证审核 213（NM-CUR-213）

- 登记台 31 行：done 24 / review 2（T-sec-001 用户动作、T-group-003 等 201-L2 终验）/
  doing 4（pool-001 收敛出口、quota-001 L3/217 在途、acct-001 L4 在途、deploy-001 等拍板）/
  todo 1（T-pool-002：L8 预研草稿 204 已交付，实现门 W1#3 未过）+T-deploy-002（前置未过）。
  **无自由可领实施任务，按用户指令转验证审核支线。**
- 验证范围：186–212 各会话落盘的全部未提交在树代码（balance/proto/backup/jump/quota/health/
  group 前后端等）。
- 结论：**发现并修复 1 个确认缺陷（195 会话产物乱码注释），其余全链绿，本轮已审项标审核过。**

## 1. 确认缺陷（已修）：`internal/op/backup_raw_test.go` 注释全部乱码

- 来源：NM-CUR-195 会话产物（13:20 写盘）。8 行中文注释全部以 `?` 序列落盘
  （非 UTF-8 通道写入，GBK 控制台重定向所致），语义全丢，违反 R-sec-001"报告中文"规范。
- 修复：按上下文逐行还原中文注释（183 P0 rawCreate 跳过关联防 reflect panic / NM-CUR-042
  count-safe 内存库 / 未知形状拒绝 / intPtrOf 仅此一份），测试代码零改动。
- 验证：`go test -run TestRawCreate|TestCreateRowsRaw -count=1 ./internal/op/` ok；
  `go vet ./internal/op/` EXIT 0。修复后全仓 `?` 乱码扫描零命中（go/claims/worklog/requirements）。
- 提醒 195 会话：其 `TestRawCreateEmpty`/`intPtrOf` 注释同样被还原——若其仍在途并再次整写
  本文件，请以本轮内容为准（避免二次乱码）。

## 2. 全链验证（本轮实测）

| 项 | 结果 |
|---|---|
| `go build -tags=jsoniter ./...` | EXIT 0 |
| `go vet ./...` | EXIT 0 |
| `go test -count=1 ./...` | 八包 ok（migrate/health/model/op/relay/handlers/middleware/router） |
| `check_auto_groups` / `check_protocol_probe` | All static checks passed ×2 |
| `pre_commit_secret_scan` | passed |
| 197 的 balance round 取模修复 | 在树（199 复核后未灭失），`TestRankCandidates*` 含周期回归全绿 |
| 200/209 proto 矩阵（6+2 characterization 例） | 在树全绿；201-L6 已闭环（200 done 行注记） |
| 193/195 deploy P0 单测（backup_raw_test 三例） | 在树全绿（注释乱码已修） |
| gofmt 工作区告警 | `gofmt -l` 命中为 CRLF 签出伪报（cmd/conf 等老文件同样命中），非格式债，不动 |

## 3. 审核标记（无新 bug 项）

- **T-route-002**（186+197）：**审核维持**（197 已审+199 独立复核在树）；
- **T-proto-001**（200/209-L6）：**审核过**（五类+两 characterization 全绿，缺口登记转 201-L6 闭环）；
- **T-deploy-001 P0 单测链**（183/193/195）：**审核过**（rawCreate 三例在树绿；注释乱码系 195 产物缺陷已修）；
- **jump/quota/acct-002~005/health 各链**：146/149/150/153/157/audit-001~005 已审结论维持；
- **T-group-003**：review 中等 201-L2/195 收口，不代验；
- **quota-001 L3（217）**：他车道在途，不碰不审。

## 4. 变更清单

- 修改：`internal/op/backup_raw_test.go`（注释还原，测试代码零改动）、
  agent_word 三件套、worklog 索引；
- 未动：186–212 全部业务代码、其余会话在途文件。
