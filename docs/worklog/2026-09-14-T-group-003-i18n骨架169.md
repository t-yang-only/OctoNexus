# 2026-09-14 T-group-003 三语 i18n 骨架（NM-CUR-169）

- 台账编号：`NM-CUR-169`｜路由：`auto-skills` 仅命中基座 `using-superpowers`，按成员原文执行；`nm-skills` 台账规范同步登记。
- 登记台：`docs/agents/ledger.md` T-group-003（todo，无认领 Agent；F 车道 W2#1，允许开工"三语 i18n 骨架"，禁写 child_group_id 字段）。
- 前置确认：T-group-002 已 done（162 口径：四单测在树、全量绿）；本轮只做 i18n 骨架，不碰树形组件与选择器。
- 结论：**i18n 骨架完成；T-group-003 行保持 todo（树形展示/子分组选择器余量仍在），不虚标 done。**

## 1. 改动清单（只改 4 文件，零后端改动）

| 文件 | 改动 |
|---|---|
| `web/src/locales/zh_hans.json` | `group.form` 追加 `nestedHint`：子分组选路展平/状态挂顶层/可用口径 |
| `web/src/locales/zh_hant.json` | 同键繁体版 |
| `web/src/locales/en.json` | 同键英文版 |
| `web/src/api/group.ts` | `GroupItem` 注释补嵌套口径一句；无字段增删、无形状变更 |

- 红线遵守：未引入/未引用 `child_group_id`（`group.ts` 全文 grep 为 False）；未改任何 Go 文件；未碰 B/C 车道 doing 线。

## 2. 验证（本轮实测）

- 三语 JSON 均可解析（`LOCALES_JSON_OK`），同名同层 `group.form.nestedHint`；
- `pnpm exec tsc --noEmit -p tsconfig.json` EXIT 0；
- `scripts/check_protocol_probe.py` 与 `scripts/check_auto_groups.py` 均 `All static checks passed`。

## 3. 遗留与下一步

- T-group-003 剩余：树形展示/子分组选择器（含 child_group_id 字段接线）。待 F 车道放行全量后再立项，本轮不越线。
- 164/167 行（本会话早前误对已 done 的 T-group-002 重复认领）：标记未完成跳过，原因"162 已交付，重复认领无实施对象"，不删行留痕。
