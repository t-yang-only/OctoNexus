# 2026-09-14 T-group-003 前端复验收尾（NM-CUR-184 独立复验）

- 台账：`NM-CUR-184`（started 巡检）｜路由：`auto-skills` 仅命中 `using-superpowers`｜认领 claim：沿用 `docs/agents/claims/2026-09-14-cursor-local-T-group-003.md`（171 实施）
- 登记台扫描：31 行（done 21 / review 1 / doing 4→3 / todo 5）。doing 中 pool-001/quota-001/acct-001 为他车道在途（不抢）；todo 中 pool-002/route-002/proto-001 前置未过门，deploy-001 已 182 开工，deploy-002 前置 deploy-001+用户拍板。**无可独立认领的空闲 todo。**
- 本轮动作：认领自己的未完成项 T-group-003（171 实施后仍 doing）做独立复验并转 review，不开新线。

## 1. 复验（全部实测）

- `tsc --noEmit`（web 目录）：0 错
- `eslint` 本轮 6 文件（api/group.ts + group Card/Create/Editor/ItemList/utils）：0 错。注：`pnpm lint` 全量另有 2 处既有错（log/FilterBar、ui/chart），非本轮文件
- 后端零改动复验：`go build -tags=jsoniter .` 过；`go vet` 过；`go test -count=1 ./internal/op/ ./internal/server/router/` 绿
- 形状对齐抽查：后端 `model.GroupItem` 有 `child_group_id?/child_group_name` + `GroupItemInput` 双引用（Grep 实证）；前端三语 `addChild/childBadge` 等键三文件在位；`git diff --stat -- web/` 9 文件 219+/35- 在途完整，无灭失

## 2. ledger 同步

- T-group-003：`doing` → `review`（复验证据见上；待调度/用户验收后转 done）
- 未动项：pool-001/quota-001/acct-001（他车道在途）、deploy-001（182 doing 在途）、门禁 todo、172 等待指导行（已有 170/175 两轮指导覆盖，不重复指导）

## 3. 遗留（转 review，不在本轮解决）

- 子分组候选无可用性徽标（后端 GroupList 未带子组 available）
- 成环/自引用无前端即时提示（以后端拒绝为准）
- 手动模式 ActiveItemID 指子分组引用行时沿既有用 `item_id` 路径
