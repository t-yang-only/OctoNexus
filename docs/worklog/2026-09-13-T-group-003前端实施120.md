# 2026-09-13 T-group-003 前端实施 120：前置灭失，工作作废，构建链复验全绿

- 台账编号：`NM-CUR-120`（本会话；注意登记表另有一并行 `NM-CUR-120`
  号池行先占同号，本篇归属后一个 120 即 T-group-003 行，不与其混淆）
- 路由：auto-skills 仅命中基座 `using-superpowers`（无缺失成员）
- 结论：**T-group-003 未实施——前置已灭失，本轮工作全部作废；构建链复验全绿。**
  119 会话抢险结论复核属实（§2），本轮不再重复抢险，只做验证与登记。

## 1. 登记（本轮 /nm-skills 动作，随时登记）

| 台账 | 动作 |
|---|---|
| 工作登记表 | `NM-CUR-120`（T-group-003 行）started→已完成（结论：前置灭失未实施；非"完成实施"） |
| 工作日志 | 追加 120 节（本节） |
| 文件清单 | 补 120 行（本篇 + 登记表/日志/索引；无业务文件） |
| API 变更 | 无 |
| worklog 索引 | 本篇入索引 |
| ledger/claims | T-group-003Claim 在仓保留（注明未实施）；T-group-002 doing 由 119 认领，其回滚建议留给下轮巡检 |

## 2. 119 抢险结论复核（属实，证据链独立复验）

- `internal/model/group.go`：无 `ChildGroupID`（纯原版）；`internal/op/group.go`：无嵌套函数；
  `internal/db/migrate/013*.go`、`internal/op/group_nesting_test.go`、
  T-group-001 篇 + claim：四文件均不存在（119 已删 0 字节占位）。
- 存活：`internal/health/` 四文件（117 产物）+ `pelican-bike.html`（杂散）+ 三份 claim；
  `docs/worklog/README.md` 唯一已改文件（119 的三行索引追加）。
- 故 113 行"模型层实现+3 单测全过"不可信（产物灭失），T-group-001/002/003
  均失去实施基础——与 119 §2/§5 一致，不推翻、不重复抢险。
- 本轮曾按 113 旧记忆写出一套前端嵌套改动（group.ts/ItemList/utils/Card/Create/Editor
  + 三语 `nested` 键），动笔前复核发现后端字段根本不存在，随即**全部回退**，
  工作树无残留（`git status` 无 web/ 改动，tsc 通过即证）。

## 3. 本轮实测（非缓存，全绿）

`go build -tags=jsoniter` 通过；`go vet ./...` 通过；
`go test -count=1` 五包 ok（op/handlers/router/health，migrate 无测试文件）；
`tsc --noEmit` 通过（仅 npm trust-lockfile 警告）。

## 4. 遗留（需用户/owner，与 119 §5 一致）

1. T-group-001 需重新实施（113 产物灭失）；2. T-group-002 doing 回滚建议待下轮巡检；
2. T-group-003 claim 未实施保留；4. `pelican-bike.html` 杂散清理；5. 0 字节清零事故原因排查。

## 5. 本篇变更

- `agent_word/工作登记表.md`：120（T-group-003 行）started→已完成（未实施口径）。
- `agent_word/工作日志.md`：追加 120 节。
- `agent_word/文件清单.md`：补 120 行。
- 新增本篇 + worklog 索引一行；未改业务代码，未做 git 提交。
