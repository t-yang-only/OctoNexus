# T-route-002 L5 均衡热路径收口 230

- claim：`docs/agents/claims/2026-09-14-cursor-local-T-route-002-L5-230.md`
- 前序：213 已完成 flag/热路径接线/RouteState 轮次/开关单测；本轮只做缺口收口。
- 调度：201 L5；L5 ownership 保持 relay handler/route/balance，未触碰 L1/L2/L3/L4/L6 锁文件。

## 目标

完成 T-route-002 L5 热路径接线的可编译、可测试和登记收口，确保均衡功能默认关闭时不改变原有转发选路行为。

## 本轮操作

1. 复核 `route_balance_test.go` 对 `op.SettingSetStringForTest` 的依赖，发现该测试入口此前未在 op 落盘。
2. 在 `internal/op/setting.go` 增加仅用于测试的缓存替换入口：不访问数据库，支持测试恢复设置存在/缺失状态。
3. 复跑 relay、op、全量测试和 jsoniter 构建。
4. 更新 `docs/agents/ledger.md`：T-route-002 定稿为 `done`。
5. 新增本 claim，并同步本 worklog 与 `docs/worklog/README.md` 索引。

## 验证结果

- `go vet ./internal/relay/ ./internal/model/ ./internal/op/`：通过。
- `go test ./internal/relay/ -count=1`：通过。
- `go test ./... -count=1`：全包通过。
- `go build -tags=jsoniter ./...`：通过。
- `ReadLints`：编辑文件无 linter errors。

## 交付状态

T-route-002 L5 热路径收口完成：

- `route_balance_enabled` 默认关闭；缺失或非法设置 fail-closed。
- flag 关闭时复用既有 `pickGroupItem`。
- flag 开启时仅改变候选定序，继续复用原冷却/探测/亲和逻辑。
- 轮转计数绑定顶层 `RouteState`，不会为子分组引入第二套状态。
- T-route-002 已由 `doing` 定稿为 `done`，等待统一提交，不单独创建 commit。

## 遗留与下一步

- 无本轮遗留。
- `T-proto-001` L6 可在 L5 完成后按调度顺序继续归档/复核。
- `T-deploy-002` 仍受用户拍板、`T-sec-001` key 轮换和 deploy-001线上门禁约束。
