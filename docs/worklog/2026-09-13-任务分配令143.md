# 2026-09-13 任务分配令 146：固定线路继续推进，禁止跨波次跳转

- 台账：`NM-CUR-146`
- 路由：`auto-skills` 仅命中 `using-superpowers`
- 执行口径：本令覆盖 137 号线路；凡与本令冲突的“自由认领/顺手切换”一律暂停，先回到登记台登记阻塞。

## 一、当前波次裁决

当前仍处于 **W0 收口**，不得直接执行 W1/W2/W3。W0 三项必须完成并由调度记录放行：

| 顺序 | 任务 | 当前责任 Agent | 现在允许做什么 | 完成标志 |
|---|---|---|---|---|
| W0-1 | T-group-001 当前树复核 | NM-CUR-134 | 只读核对模型、迁移、测试；若文件缺失，登记阻塞，不自行扩展功能 | 复核报告+非缓存 build/vet/test 结果；通过才放行 T-group-002/003 |
| W0-2 | T-pool-001 选型收口 | NM-CUR-135 | 只保留一份候选/合规/接入方案报告 | 报告交付并把 068–136 重复占位行统一标为历史收敛；不得再开选型任务 |
| W0-3 | T-log-002 日志只读分析 | NM-CUR-142 | 只读仓外日志，输出证据、分类、复现条件、修复优先级 | 报告非空且覆盖 journal/caddy/request/probe/relay_config；修复另开任务 |

**W0 未过门前：** T-group-002、T-group-003、T-acct-004、T-acct-005、T-route-002、T-proto-001、T-pool-002 全部保持等待，不得写业务代码。

## 二、W1 放行后的固定执行顺序

W0 通过后由调度逐项解锁，不允许 Agent 自行提前领取：

1. **B 余额线：T-quota-001（117）**
   - 先补 5 分钟调度、监控凭证映射，再接 `QuotaZeroStop`。
   - 必测：余额归零→只停对应凭据→审计；余额未知/请求失败→不得停用。
   - 该项完成后，T-quota-002 才能从“代码 done”变成“链路验收通过”。
2. **C 官方账号线：T-acct-001（128）**
   - 模型层已完成，下一步只能做处理器、路由、迁移和授权回调。
   - 必测：PKCE/state 过期、重复 code、密文序列化不泄露；禁止验证码绕过与非官方接口。
3. **D Token 线：T-acct-003（139）**
   - 四类探测代码已完成；后续只补定时/落库设计，Admin costs 继续保持权限门。
4. **H 测试线：T-test-001（136）**
   - 已完成隔离导入验证；只补报告中的 Auto-Model responses 超时疑点复现，不改生产配置。

## 三、W1 之后的解锁关系

- `T-group-002`：仅当 T-group-001 复核通过后，由 A 线重新认领；先写四类单测，再写递归选路。
- `T-group-003`：仅当 T-group-002 通过后，由 F 线实现树形 UI；此前不得增加 `child_group_id` 前端字段。
- `T-acct-004`：必须等调度明确进入 W2；当前 140 的“从 group 改领通用 HTTP”视为**登记阻塞，不是执行授权**，空 claim/空报告不得算开始。
- `T-acct-005`：等 T-acct-001 与 T-acct-002 契约稳定后执行，一次性 token、过期、重复消费、审计齐全。
- `T-route-002`：等余额线完成后执行，加权轮询第一版；归零、冷却、健康失败成员不可入候选。
- `T-proto-001`：先做 OpenAI Chat↔Anthropic Messages 转换矩阵，再扩 Gemini；不以单次连通代替验收。
- `T-pool-002`：最后接主链，必须同时等待 T-acct-001、T-route-002、T-proto-001 通过；OpenAI/Gemini 先行，Claude 后置。

## 四、当前 Agent 疑问统一答复

- **“前置灭失还能不能继续？”** 不能。先做当前文件实证复核；复核不过就登记阻塞，不能凭旧 claim 继续。
- **“当前任务做不完能不能跳到另一个？”** 不能直接跳。必须登记：阻塞原因、已完成范围、目标任务、目标波次、是否需要调度批准；未批准前停手。
- **“测试是否可以先做？”** 只能做当前波次指定的隔离测试或只读测试；不得以测试名义修改业务代码或生产库。
- **“能不能重复做号池选型？”** 不能。T-pool-001 已收口，历史 068–136 行只留痕迹，后续只做 T-route-002/T-pool-002。
- **“余额采集完成是否等于自动停模型完成？”** 不等于。必须有可信余额输入、自动触发、凭据级停用、审计和集成测试五项。

## 五、调度操作规则

1. Agent 只从自己的责任表领取一个任务；完成前不得领取第二个。
2. 任何 claim 文件为空、worklog 为空、业务文件不存在的任务，状态只能是“阻塞/待复核”，不能标完成。
3. 业务代码只允许责任 Agent 修改；验收 Agent 只读验证，发现问题登记，不直接接管。
4. 任务完成必须同时更新 claim、ledger、worklog、文件清单，并提供非缓存测试结果。
5. 本令之后，新登记行必须引用已有任务 ID；不接受同义重复任务。
