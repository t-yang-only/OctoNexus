# 2026-09-14 T-route-002 均衡候选器复验（NM-CUR-195/199）

- 台账：`NM-CUR-195`（started，验收登记）/`NM-CUR-199`（started，复验认领）｜路由：`auto-skills` 仅命中 `using-superpowers`
- 认领 claim：`docs/agents/claims/2026-09-14-cursor-local-T-route-002-verify.md`
- 登记台：`docs/agents/ledger.md` T-route-002（done，NM-CUR-186 实施：`internal/relay/balance.go` + `balance_test.go` 三单测）
- 类型：只读复验 + 缺口单测补齐；不改冻结语义，不接热路径，不改模型/迁移/前端

## 1. 复验结论（186 产出无 bug）

- 权重=priority 倒序分（`w=1+n-i`）、冷却压尾保留、Available=false/归零授权剔除、`LatencyProvider` 可插拔、亲和冷却归顶层 RouteState——实现与 claim 口径一致
- `smoothWeightedOrder` 先对 total 取模再重放：round 上亿不挂死，与全量重放语义一致

## 2. 缺口单测（新文件 `internal/relay/balance_verify_test.go`，不碰 186 产出）

| 单测 | 锁定口径 |
|---|---|
| TestRankCandidatesRoundRobinSequence | 权重 3/2/1 下 round0..5 当选序列 `[1,2,1,3,2,1]`，round6 取模回 round0 |
| TestRankCandidatesCooldownReturns | 冷却到期成员回 eligible 区（round1 当选成员 2）；冷却生效时压队尾 `[1 2]` |
| TestRankCandidatesLatencyKeepsWeightOrderAcrossTiers | 延迟排序仅同档内，跨档权重序优先（最低延迟成员 3 不得越过前档） |

## 3. 验证

- `go test -count=1 -run TestRankCandidates -v ./internal/relay/`：6 例全过（186 三例 + 本轮三例）
- `go test -count=3 ./internal/relay/`：绿；`go vet -tags=jsoniter ./internal/relay/`：绿
- 附带：`internal/op/backup_raw_test.go` 去重修复（183 落盘尾部重复粘贴致 `TestCreateRowsRawRejectsUnknown`/`intPtrOf` 各声明两次，op 包编译失败；删除重复块后 `go test -count=1 ./internal/op/` 绿；语义不变）

## 4. ledger 同步

- T-route-002：维持 done，备注追复验行（186 实施 + 199 三单测补齐，全量 relay -count=3 绿）
- 未动项：T-pool-001/T-quota-001/T-acct-001/T-deploy-001（他车道 doing 在途）；T-pool-002/T-proto-001/T-deploy-002（前置未过门）；T-group-003（review 待验收）；并行巡检行（188/189/191/192/196）未触碰

## 5. 遗留（建议，不在本轮）

- 接热路径（pickGroupItem 调用 rankCandidates）属 A 车道 W2#2 后续，未开工
- 权重来源为 priority 倒序分而非配额/成本加权：与 186 口径一致，最低延迟接口已预留
