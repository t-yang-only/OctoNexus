# 2026-09-14 指导 214：前端预览部署 + 新任务分配（L0/L9 两道新增）

- 台账：`NM-CUR-214`｜前序沿用：212 七路裁决、201 八路分工（不重复，只增量）
- 用户指令：再次指导、分配新任务、尽快部署，要看前端状态。

## 1. 前端预览已部署（实测可用）

- 构建链：`pnpm run build`（3.91s，tsc 0 错，产物 `static/out` 含 group-003 全部前端改动）→
  `go build -tags=jsoniter`（72MB 含重嵌前端）。
- 隔离实例：`%TEMP%\oct-front-deploy\octopus.exe`（admin 13399 / relay 11931，空 sqlite 库，生产 3303/1234 未碰）。
- **访问地址：http://127.0.0.1:13399/**（本机浏览器直接打开；登录 admin/admin 为 136 隔离库惯例，未贴任何生产密钥）。
- 已造预览数据：分组 `e2e-preview` 嵌套子分组 `e2e-child`（GroupItem child_group_id=2 引用在库，
  GET /group/get/1 实证：child_group_name 回名正确、空子分组 available=false 符合口径）。

### 前端状态结论（T-group-003，实测）

- 分组编辑器 `ChildPickerSection` 已实现：子分组候选（排除自身）+搜索+已选置灰+`FolderGit2` 徽标；
  members Tab 底部 `nestedHint` 说明文案；提交形状与后端 `GroupItemInput{channel_grant_id|child_group_id}` 对齐（互斥 0 值口径一致）。
- 三语 locales `addChild/childHint/noChildCandidate/nestedHint/childBadge` 在 `web/src/locales/{en,zh_hans,zh_hant}.json`；
  dist 产物中 i18n 为运行时加载不进 bundle（属设计，非缺失）。
- `web/src/api/group.ts` 双引用类型与注释（T-group-002 展平口径）在位。
- **无浏览器环境（本机无 Chrome，DevTools MCP 不可用），截图链路断**；以上为源码+API+构建三层实测证据。

## 2. 指导：212 之后需要指导项的增量裁决

| 项 | 增量指导 |
|---|---|
| deploy-001 归档（G2） | 维持"183 续跑"口径；新增提醒：归档前工作树混有 L4 前端 WIP，**commit 必须点名 add 禁 `git add .`**；预览实例 `%TEMP%\oct-front-deploy` 测后整删（214 建） |
| group-003 终验（G6） | 代码层终验已由 214 完成（tsc/build/API 闭环三层）；**剩余仅浏览器视觉验收**：用户打开 http://127.0.0.1:13399/ 看编辑器即视为验收，或授权 L2 等 Chrome 就绪再补截图链路 |
| quota-001/acct-001（G4/G5） | 维持 L3/L4 开工单不变；新增红线：不得在 13399/11931 预览库上联调（防污染视觉验收基线），各自另起隔离端口 |
| R-pool-003（G1） | 维持用户二选一不变 |
| sec-001（G3） | 维持不代办 |

## 3. 新任务分配（两道新增 + 八路重申）

| # | 代号 | 任务 | 完成判据 | 锁 |
|---|---|---|---|---|
| 新 | **L0 前端视觉验收** | 用户侧动作优先；子 agent 备援：有 Chrome 后补 group-003 编辑器/列表/选择器三屏截图+console 0 错取证，报告入 worklog | 三屏截图+0 错结论+`T-group-003 review→done` 建议 | 只写 worklog/报告，不动代码 |
| 新 | **L9 预览生命周期值守** | 维护 13399/11931 预览实例：重启恢复（config 无 BOM 教训入册）、e2e 数据补种（渠道/授权/模型各 1 条，便于看嵌套与徽标）、测毕停服整删 `%TEMP%\oct-front-deploy` | 值守记录 + 整删确认 | `scripts/`（预览辅助）+`%TEMP%`，禁碰业务树 |
| 重申 | L1–L8 | 201 八路分工不变，逐项开工单见 worklog/2026-09-14-8子agent并行调度201.md | — | — |

## 4. 教训登记（防再犯）

- `Set-Content -Encoding UTF8` 在 Windows PowerShell 5.1 写 JSON 带 BOM → viper 解析静默失败 →
  "unsupported database type"（与 136 同坑，第二次踩）。预览 config 一律用 Python `open(...,'w',encoding='utf-8')` 写。

## 5. 台账同步

ledger 追 214 口径（T-group-003/T-deploy-001 两行）；claims 新增 guidance-214；本 worklog+README 索引；登记表/日志/清单按 nm-skills 全套。
