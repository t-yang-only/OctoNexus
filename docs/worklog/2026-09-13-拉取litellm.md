# 2026-09-13 拉取 litellm 仓库到本地

- 仓库：https://github.com/bestruirui/octopus（本地 `d:\奇怪的软件\octopus`）
- 拉取目标：`https://github.com/BerriAI/litellm`（用户给的是 `https://link.wtturl.cn/?target=https%3A%2F%2Fgithub.com%2FBerriAI%2Flitellm&scene=im&aid=582478&lang=zh`，解码 target 即 BerriAI/litellm）
- 落点：`d:\奇怪的软件\litellm`（与 octopus 同级的独立仓库目录，不在 octopus git 内）
- 台账编号：`NM-CUR-010`
- 分支：octopus 侧 `master` 跟踪 `origin/master`

## 目标

把 litellm 拉到本地一份可用源码，供对照其网关/模型/渠道实现。

## 执行操作

1. 开始前读取 `agent_word/工作登记表.md`、`工作日志.md`、`API变更.md`、`文件清单.md`，确认 CUR 最大序号 009（008 进行中），本次分配 `NM-CUR-010`，无文件冲突（落点在 octopus 仓外）。
2. `dir "d:\奇怪的软件"` 确认父目录布局：当时只有 `AIProxyHub-2.0.0-clean-win64/Cookie-grab/diana-doubao-theme-main/NetScopePLC/octopus/skills-Management/tingapp.zip`，无 `litellm`。
3. 首次 `git clone https://github.com/BerriAI/litellm.git "d:\奇怪的软件\litellm"` 全量克隆 300s 超时转后台（PID 92380），仅输出 `Cloning into ...` 无进度，判定为大仓全量太慢，`Stop-Process -Id 92380 -Force` 终止。
4. 清理残留后改浅克隆：`git clone --depth 1 https://github.com/BerriAI/litellm.git litellm`（working_directory `d:\奇怪的软件`，用相对路径绕开中文绝对路径在 git 侧的解析问题）。
5. 等待约 20s 后验证通过：
   - `git -C "d:\奇怪的软件\litellm" log --oneline -3` → `1c61c26 perf(proxy): one MGET and one pipeline ... (#40841)`
   - `status --short --branch` → `## litellm_internal_staging...origin/litellm_internal_staging`
   - `rev-parse --is-shallow-repository` → `true`
   - `log -1 --format="%H %ci %s"` → `1c61c2606e36061db4fce10f7bb94745d76bc84f 2026-09-12 16:05:50 +0000 ...`
6. 运行登记脚本（octopus 根）：
   `python "skills\nm-skills\scripts\nm_register.py" --client CUR --task "拉取litellm仓库到本地" --changes "浅克隆BerriAI/litellm(depth1)到D:/奇怪的软件/litellm,首次全量克隆超时终止后清理改浅克隆" --api "无" --files "docs/worklog/2026-09-13-拉取litellm.md,docs/worklog/README.md" --skills "nm-skills" --mcps "无" --tools "Read,Shell,Write" --status done`
   输出 `NM-CUR-010`，脚本自动追加登记行、日志节并更新技能建议时间戳。
7. 新增本文件；更新 `docs/worklog/README.md` 索引；补 `agent_word/文件清单.md`。

## 结果

- 落点：`d:\奇怪的软件\litellm` 浅克隆完成，HEAD `1c61c26`（2026-09-12），分支 `litellm_internal_staging`。
- 登记：`工作登记表.md` 新增 `NM-CUR-010 已完成`（2026-09-13 00:48:22 开始并完成）。
- 日志：`工作日志.md` 新增 `## [2026-09-13 00:48:22] NM-CUR-010` 一节。
- API 变更：无（`API变更.md` 保持空表）。
- commit：（octopus 仓未提交；litellm 为独立仓，不计入 octopus commit）
- `git status --short --branch`（octopus 侧）：`## master...origin/master`，`M internal/conf/config.go, internal/op/channel.go, internal/op/group.go, internal/server/router/router.go`，未跟踪 `agent_word/ docs/ internal/op/auto_group_test.go internal/server/router/group.go scripts/check_auto_groups.py skills/ src/ tests/ 数据备份.json`。

## 遗留问题

- 浅克隆只有单提交历史，如需完整历史/切分支需 `git fetch --unshallow` 或按需加深。
- 默认分支落在 `litellm_internal_staging` 而非 `main/master`，对照上游文档时注意分支差异。
- `数据备份.json` 仍在 octopus 工作区且含明文 key，绝不提交，需移出并轮换（最高优）。
- NM-CUR-005/NM-CUR-008 仍为进行中，与本次拉取无关。

## 下一步

1. 按需 `git -C d:\奇怪的软件\litellm fetch --unshallow` 或 `git checkout main` 对齐要看的版本。
2. 用 litellm 的 provider/model/网关实现对照 octopus 的渠道与中继逻辑。
3. 继续推进 octopus 侧 T-sec-001（备份移出+轮换）与进行中的 005/008。
