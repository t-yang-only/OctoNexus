# T-group-002 分组嵌套选路递归展开实施（NM-CUR-161）

- claim：`docs/agents/claims/2026-09-14-cursor-local-T-group-002-161.md`
- 前置：T-group-001 done（158 指导收口，T-group-002 门开）；登记台行 todo 无认领 Agent
- 结论：**实施完成，四类门单测 + relay 纯函数五单测全绿，build/vet/test 全包绿。**

## 1. 交付

| 文件 | 变更 |
|---|---|
| `internal/op/group.go` | 新增 `FlattenGroupItems`（DFS 展平，引用行位置 splice；路径回边剪除/超 `GroupItemMaxDepth` 截断/成员行按 ID 去重/缺失引用跳过）；`groupSnapshot` 子分组成员 Available 按展平后代回填（兑现 148 线注释承诺：子树有可用授权即真） |
| `internal/model/group.go` | 新增 `Group.WithItems(flat)`（选路喂展平表的外壳）+ `WithItemsForTest`；`Available` 注释口径刷新（T-group-002 落地，恒假旧注删除） |
| `internal/relay/route.go` | 新增 `PickGroupItem(group, flat)` 可测外壳（手动/failover 双分支统一走展平；状态按顶层 group.ID，键为展平叶子行 ID） |
| `internal/relay/handler.go` | 转发热循环改为 `pickGroupItem(group.WithItems(op.FlattenGroupItems(group)))`（同包直调；外壳供单测）+ 注释更新（移除"子分组不展开/T-group-002"旧注） |

冻结口径（与 158 claim/登记台一致）：亲和/冷却/上限按顶层分组持有，
键为展平后具体成员行 ID；子分组 manual 模式不下钻，按 Priority 并入引用位置；
缺失引用跳过（GroupDel 写侧已级联）；消费侧无缝——`GroupGetByName` 快照后展平。

## 2. 测试（全部在树，新增 0 个文件，本轮无新文件）

- op 四类门（在树 `group_flat_test.go`，并行会话已落）：展平顺序/防循环/
  深度截断/空树，全绿（`-count=3` 全包 ok）。
- relay 五单测（在树 `group_pick_test.go`，并行会话已落）：冷却跳位/亲和保持/
  手动指定/空平面零值/去重稳定命中，全绿。
- 注：这两组测试文件由并行会话（158 claim）先行落盘，与本实现同口径互证；
  本轮复核逐条读过用例语义，均为有效口径测试，无重复实现。

## 3. 验证

`go build -tags=jsoniter` 通过；`go vet ./...` 通过；`go test -count=1 ./...` 全包 ok；
op/relay/model `-count=3` ok；双 check 脚本 + 密钥扫描全过。

## 4. 状态

- `T-group-002`：todo→**done（NM-CUR-161 实施过门）**。
- `T-group-003` 前端门开（decor：前置 T-group-001+T-group-002 均过门；门过前禁写
  `child_group_id` 字段的旧注已失效，F 车道可开工）。
- 未改：group-001 模型/迁移；B/C doing 线；T-group-003 前端。
