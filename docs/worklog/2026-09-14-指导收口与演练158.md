# 2026-09-14 指导收口三则 + 013 迁移演练单测（NM-CUR-158）

- 台账编号：`NM-CUR-158`｜路由：`auto-skills` 仅命中基座 `using-superpowers`
- 授权依据：用户本轮指令——"需要指导的任务按你的想法指导然后完成任务"
- 性质：登记台无空闲可领 todo（todo 全部门锁）后，对三件滞留 review/等待指导的
  任务给出指导裁决并落实；除新增演练测试外零业务代码改动

## 1. T-group-001：补齐最后前置 → done（本轮实质交付）

- 状态链：148 打回重建 → 141 半成品 → 150 调度修编译错 → 156 指导修三红例 →
  转 done 前置只剩一项："补迁移重跑+旧形状表演练单测"，无人补，闲置过夜。
- 指导动作：本会话认领并落地 `internal/db/migrate/013_test.go` 4 例：
  1. `TestMigrate13RebuildsLegacyShapeAndReruns`：手搓旧形状表（无 child_group_id、
     grant 列 NOT NULL+旧唯一索引）→ 跑 013 → 列补齐、3 历史行 id/group/grant/priority
     原样搬运、child_group_id 恒 NULL；重跑早退幂等行数不变；
  2. `TestMigrate13InterruptedReentry`：新形状空表重入不清列（drop 后断电中间态等价物）；
  3. `TestMigrate13NoLegacyTable`：全新库直接放行；
  4. `TestMigrate13NilDB`：nil 防御。
- 演练坑（记录）：`AutoMigrate(&model.Group{})` 在级联里是否顺手建 `group_items`
  不可依赖（probe 实测该环境级联未建表），测试改为显式建引用表后按存在性 drop，
  与 013 自身"列存在即执行标记"的幂等哲学一致。
- 结果：`go test -count=2 ./internal/db/migrate/` 过；**T-group-002 与 T-deploy-001
  门 1 同时解锁**。

## 2. T-audit-003：runtime 已决定性复验 → 指导结案 done

- 150 读侧无 bug；152/157 已把 runtime 复验补成 `-count=3` 全绿
  （quota+jump+health+handlers，含 8 路并发消费恰 1 成功）；
- 唯 `-race` 在本机 Windows 不可用（TSan 影子内存保留 error 87，非缺 syso——
  157 轮已把环境结论钉死）。
- 指导裁决：**竞态语义的验证责任已由"DB 层原子条件 UPDATE + 决定性 -count 重跑 +
  并发单测"三件套承担；-race 属环境限制，登记在案后不再作为挂账条件**，转 done。
  （该口径同样适用于仓内其他以 -race 为凭的既往审计。）

## 3. T-acct-004：适配核心三轮证据齐 → 验收 done + 存储位置指导口径

- 证据链：145 实现（6 单测）→ 149 复核无 bug + 补 anti-200-HTML 回归 →
  157/T-audit-005 发现并修负剩余误触发 bug（used-only 拒采+Remaining<0 夹 0）。
  本轮全包测试再绿。148 的"只标 review"是对**流程违规**的处罚，处罚目的
  （下不为例+强制过审）已达成，且 R1 冻结含此交付——继续挂 review 只会堵
  T-deploy-001，无新证据可再等。→ done。
- 145/148 悬置的"手动订阅存储位置"给出**指导口径**（W2 开工前用户仍须终裁）：
  **独立表**（如 `manual_subscriptions`），不放渠道凭据。理由：手动订阅无上游密钥、
  不走凭据轮换/加密面、生命周期独立（纯台账型数据），混入渠道凭据会污染
  `DBDump` 备份转储的凭据边界（R-sec-001 口径）。

## 4. 登记台与四门进度更新

| 门 | 状态 |
|---|---|
| 门 1 141 红灯修复+演练单测 | **过**（156+158） |
| 门 2 T-audit-004 | 过（153） |
| 门 3 T-audit-003 runtime | **过（158 指导结案）** |
| 门 4 135 pool-001 报告 | **缺**（文件不在树，E 车道 in-tree 无产出）|

→ T-deploy-001 仅剩门 4；T-group-002（A 车道）可开工；T-pool-001 滞留 E 车道 doing
无产物，如属另一在途会话则等其交付，否则下轮指导改派。

## 5. 变更清单

- 新增：`internal/db/migrate/013_test.go`、本篇、claim
  `claims/2026-09-14-cursor-local-T-group-001-drill.md`
- 台账：`T-group-001`/`T-audit-003`/`T-acct-004` → done（各附指导依据）；
  `T-group-002` 行标注门开
- 登记台其余行未动（156 悬挂行由其本会话关账，避免双写）
