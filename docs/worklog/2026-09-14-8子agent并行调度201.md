# 2026-09-14 调度 201：8 子 agent 并行分工（部署收尾 + R2 六线）

- 调度：cursor-local（NM-CUR-201）｜路由：auto-skills → using-superpowers
- 用户指令：指导分配下一步，8 个子 agent 并行同步、每人不同工作。
- 基线：HEAD `f0241dc` 起 18 文件 WIP 未提交（group 前端 9 + relay/model/op/db/middleware）；`go build/test` 以各 lane 落盘报告为准，调度不代跑全量。
- 原则：**文件互斥**（下表 Ownership 即锁），跨组文件先读后问，ledger 状态机由调度统一改，子 agent 只写自己的 claim+worklog+代码文件。

## 1. 八路一览（互斥分工）

| # | 代号 | ledger 任务 | 目标（完成判据） | Ownership（只许碰这些） | 前置/红线 |
|---|---|---|---|---|---|
| L1 | deploy-收尾 | T-deploy-001 doing | 183 验收 D1–D9 + 193 三单测收口：把 `internal/op/backup.go` rawCreate P0 修复 + `backup_raw_test.go` 去重态提交态归档，补《R1 验收结论》拍板行，deploy-001 doing 维持等用户拍板 | `internal/op/backup*.go`、`docs/worklog/2026-09-14-R1*`、`docs/agents/claims/*deploy-001*` | 不碰 web/relay/handlers；commit 信息带 `T-deploy-001`；密钥扫描必过 |
| L2 | group-003 验收 | T-group-003 review | 171 实施 + 184 复验 → 终验转 done：tsc 0 错 + eslint 本轮 0 错 + go build/vet 绿， worklog 验收行 | `web/src/api/group.ts`、`web/src/components/modules/group/*`、`web/src/locales/*` | **禁动一切 go 文件**；195 行即验收主体，後來者接力 195 不另起 claim |
| L3 | quota 接线 | T-quota-001 doing | 163 清单 5 步：task 5min 注册 + channel→监控凭证映射 + 阈值事件→QuotaZeroStop 消费 + 2 集成测试 + 超时疑点复现结论 | `internal/task/init.go`、`internal/health/balance_scan.go`、`internal/op/quota*.go`（集成测试） | 属 R2，不阻塞 deploy；不碰 relay/ |
| L4 | 官方账号处理器 | T-acct-001 doing | 163 清单 5 步：authorize/callback/读取三接口 + 路由挂载 + PKCE state + official_account AutoMigrate + 密文落库 + 3 单测 | `internal/server/handlers/official_account*.go`、`internal/server/router/*`（仅挂载行）、`internal/op/official_account*.go`、`internal/model/official_account*.go` | 合法 OAuth，禁验证码绕过；响应 JSON 禁明文；不碰 task/health |
| L5 | 均衡热路径 | T-route-002 done→落地 | balance.go（186/197/199 已 done 审计过）接 relay 热路径：feature flag 默认关 + 亲和/冷却仍归顶层 RouteState，单测覆盖 flag 开/关 | `internal/relay/handler.go`、`internal/relay/route.go`、`internal/relay/balance*.go` | 未开 flag 时行为零变化；不碰 transformer/ 子目录 |
| L6 | 协议矩阵 | T-proto-001 todo（验证） | OpenAI Chat ↔ Anthropic Messages 五类矩阵（非流式/流式/usage/错误/tool call）：只读验证优先，确认缺口才补 httptest；200 claim 即主体 | `internal/relay/transformer/**`、`internal/relay/proto_matrix_test.go` | **不改转发热路径**；不碰 handler.go/route.go/balance.go（L5 的锁） |
| L7 | 号池收敛 | T-pool-001 doing | 182 终裁出口：R-pool-003 待确认——交付一页草稿（要不要仓库选型报告 + 若要则候选/红线/映射/优先级大纲），关闭 174/176/181 三行 | `docs/requirements/2026-09-13-号池管理选型.md`（新建草稿）、`docs/agents/claims/*pool-001*` | **纯文档**，禁碰代码；与 deploy 脱钩重申 |
| L8 | pool-002 预研+巡检 | T-pool-002 todo（预研） | W3#1 未开门：只做设计预研（官方账号→渠道轮询转发方案草稿：轮询/均衡/协议三节），+ 登记台巡检（只读，只写自己的 worklog，不改 ledger） | `docs/requirements/2026-09-14-pool-002预研.md`（新建）、`docs/worklog/2026-09-14-巡检201-*.md` | **禁改 ledger/禁动代码**；ledger 修改权归调度 |

## 2. 合并顺序（防双写）

1. L2 前端先合（只 web/，与 go 零交集，可随时合）；
2. L5/L6 relay 双路：L5 合 handler/route/balance，L6 合 transformer+matrix——**同属 relay 包，禁止同时 `git commit`，L5 先、L6 后**，后者合前必 `go test ./internal/relay -count=1`；
3. L3 task/health/op-quota 与 L4 handlers/op-official 分属不同包，可并行合；
4. L1 deploy 收尾**最后合**（收所有包的验收结论）；
5. L7/L8 纯文档，随时合。

## 3. 统一 Done 口径（每路交付）

- claim 文件：`docs/agents/claims/YYYY-MM-DD-cursor-local-<TASK>-<路>.md`（加锁一行）；
- worklog：`docs/worklog/YYYY-MM-DD-<主题>.md` + README 索引一行；
- 台账：登记表一行 started→done + 日志节 + 文件清单行（agent_word 被 gitignore，只做工作区同步，不提交）；
- 验证：`go build -tags=jsoniter ./...` + `go vet` 相关包 + 相关包 `-count=1`（L2 另加 `tsc`+eslint）；
- 密钥：`py scripts/pre_commit_secret_scan.py` 必过；备份 JSON 只读引用、不贴原文。

## 4. ledger 同步（调度已改）

- T-deploy-001：追加 201 收尾口径（L1 归档责任）；
- T-group-003：追加 201 验收口径（L2 接力 195）；
- T-quota-001 / T-acct-001：163 清单维持，追加 201 并行口径（L3/L4）；
- T-route-002：done 维持，追加 201 热路径落地口径（L5）；
- T-proto-001：todo 维持，追加 201 验证口径（L6 只读优先）；
- T-pool-001：doing 维持，追加 201 收敛口径（L7 草稿关三行）；
- T-pool-002：todo 维持，追加 201 预研口径（L8 设计草稿，不开工实现）。

## 5. 用户待办（不派给子 agent）

- T-deploy-001→done 拍板（R1 验收结论）；
- R-pool-003 要不要仓库选型报告；
- T-sec-001 线上 key 轮换时机；
- T-deploy-002 开工拍板。
