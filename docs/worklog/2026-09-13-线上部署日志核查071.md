# 2026-09-13 线上部署日志核查（NM-CUR-071 认领收尾）

- 台账编号：`NM-CUR-071`（认领自任务登记台在途行；073 为本会话并行会话的同题实质完工行，本篇为 071 的收尾落盘，结论与之互证一致）
- 基线：日志包 `D:\奇怪的软件\octopus-本地数据`（导出 2026-09-13 02:32:39 服务器时间；线上 `octopus v0.13.4/6dce286`，`api.yangonly.com` 经 Caddy 反代 `127.0.0.1:8080`）
- 方法：逐份只读（journalctl 194 行全文 + caddy syslog 316 条 + context canceled 251 条 + channels_health 12 行 + request log 6 条 + 探针三件套 + relay_config），交叉验证；未改线上任何文件
- 结论：**线上部署无 P0 故障；octopus 本体干净；问题在外围三处（Caddy 15s 掐 SSE / 部分渠道真实高失败 /探针污染统计）**

## 1. 认领说明（登记台领取）

- 接手时状态列精确核对：在途 5 行 = 068/070/075（号池选型三行，他会话）+ 071（本任务）+ 074（日志富化，他会话）。
- 可执行项仅 071（查线上部署日志）：068/070/075、074 均为他会话实质在途任务，按"冲突留源"不代关不代做。
- 073（同题实质完工行）已关行但其 worklog 篇缺失（`2026-09-13-线上部署日志核查.md` 不在仓），本篇即 071 的收尾落盘，
  与 073 行内结论互相印证（063"报告即载体"惯例）。

## 2. octopus 本体：干净（journalctl 194 行全文）

- 9/9 v0.13.2（`27aa40d`）启动 → 9/12 22:04 升级 v0.13.4（`6dce286`）→ 9/13 02:31:18 优雅停机
 （`Shutdown completed successfully`）后重启成功。
- 全文件**零 ERROR、零 panic**。仅有 WARN 两类：9/10 自更新下载 `context deadline exceeded` +
  `proxy url is empty`（自更新未配代理直连超时）；其余全是正常 `terminated` 重启记录。

## 3. Caddy 侧：真正的异常（syslog 316 条，246 条 context canceled）

- **78 条整齐的 15.000 秒超时，URI 全部是 `GET /api/v1/group/events`（SSE），错误全是
  `reading: context canceled`**，9/9–9/12 天天有（15/68/57/103）。SSE 流每次 15 秒整被掐断，
  浏览器（`111.58.209.146`）不断重连。判定：Caddy 侧（或其上游）15s 代理超时掐 SSE 长连接，
  `group/events` 实时推送被反复打断重建。修法：Caddy `reverse_proxy` 给 SSE 单独放行
  （超时放宽/缓冲关闭/心跳对齐），见遗留①。
- **131 条 ~0 秒取消**：127 条 `log/overview/stream` + 4 条 `group/events`，全 `context canceled`。
  页面跳转/刷新时浏览器主动断开，**正常噪音**，不管。
- **转发本身正常**：79 条 `/v1/*` 里 53 条是 120 秒以上、无错误正常结束的长流式请求
  （最长 761 秒）；取消仅 15 条分散在 5–120 秒。relay 转发链路是好的。
- 另有几条对 `64.90.0.139` 的 reset/broken pipe/timed out，属外部扫描噪音。

## 4. 渠道健康：失败率半真半假（channels_health 12 行）

- 探针 14 轮 354 请求（231 成功/123 超时失败）**全部计入渠道统计**，失败率被注水，
  不能直接当 upstream 质量用。误判实锤：Forestapi/deepseek-v4.1-flash 被探针降级，
  实际失败率 0%。
- 剔除污染后仍需关注的真实高失败：pipixia 89.7%（已禁用，基本已死）、可茶 86.4%、THQ 79.5%、
  53HK-L 66.4%、AI.AAA 67.1%、OKai 54.3%；53HK 27.9%（3479 次，基数大相对可信）。
- "平均等待"列数值巨大（如 53HK 55567014），单位不明，若为微秒则平均 55 秒——
  与 M-3（失败轮耗时口径）相关，口径核对后再信。
- relay_config：`stream首包25s / 非流120s / 重试2轮间隔1s / 冷却600s / 亲和30s`，配置合理。

## 5. 请求日志（重启后仅 6 条）：无系统性问题

- 5 成功 1 取消。唯一取消是 02:31:56 `Mid-flash → THQ/gemini-3.6-flash`，
  0.88 秒、`sending:false`、零 token——客户端建连后立刻断开，个案。
- 其余 5 条成功延迟 1.25–6.87 秒正常；内存环形缓冲（约 50 条）02:33 重启已清空，符合预期。

## 6. 探针事件：已闭环

- `probe_changes.md` 11 条降级 + `probe_runs.txt` 644 行 + `probe_hang_probe_state.json`
  与 README 互相印证：探针 23:05–00:13 运行，cron 已删、进程已杀、脚本已改名 `.disabled`，
  11 条降级已撤销并重启生效。`caddy_context_canceled.log` 251 条取消多为其贡献。**不用再动**。

## 7. 遗留（按优先级，需用户/运维）

1. Caddy 对 `/api/v1/group/events` 的 15 秒掐流（`reverse_proxy` 超时/缓冲，SSE 单独配置）；
2. THQ/可茶/pipixia 真实上游质量（等探针污染窗口过去后重看，或清统计重算）；
3. 自更新代理为空（`proxy url is empty`）——配代理或关自更新；
4. `channels_health` 平均等待单位/口径核对（M-3 相关）；
5. `octopus-accept.exe` 未跟踪残留（067 会话验收产物，收尾时删或追 gitignore）。

## 8. 本篇变更清单（仅台账，未改业务代码，未碰线上）

- 新增本篇；登记表 071 翻已完成；工作日志补 071 节；文件清单补 071 行；
  技能建议更新时间；worklog README 加入本篇索引行。
