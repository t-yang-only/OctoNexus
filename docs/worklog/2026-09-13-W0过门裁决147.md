# 2026-09-13 W0 过门裁决 148：group-001 复核打回重建，pool-001 空交付打回，W1 部分放行

- 台账：`NM-CUR-148`｜路由：`auto-skills` 仅命中 `using-superpowers`
- 职责：波次放行门裁决；实证依据为当前树 `rg`/`go test`/文件存在性，不采信任何 claim 文字。

## 1. W0 三门终裁

### W0-1 T-group-001 复核（A 车道/134）——**不过门，打回重建**

实证（2026-09-13 本轮复核）：

1. `internal/model/group.go` 当前树**无 `ChildGroupID`**（rg 全 internal 零命中 Nesting/ChildGroup）；
2. `internal/db/migrate/` 无 013 嵌套迁移文件；
3. `internal/op/group_nesting_test.go` 不存在；
4. `go test -count=1 ./internal/model ./internal/op ./internal/health` 三包 ok——但这只证明**现有代码干净**，
   不存在嵌套模型层的任何痕迹。

结论：113 会话声称的"模型层完成"产物再次确认灭失且**从未重建**；134 复核行至今无产出落盘。
T-group-001 状态由 review 改为 **todo（重建）**：按原 107 拆分口径重做
（`ChildGroupID` 可空+互斥校验+防循环+013 迁移+单测），仍由 A 车道认领，重建完成才放行 T-group-002/003。

### W0-2 T-pool-001 收口（E 车道/135）——**不过门，打回**

`docs/requirements/2026-09-13-号池管理选型.md` 不存在（文件长度 0/缺失），135 行 started 无产出。
裁决：135 重新执行——一份报告即可过门：候选项目清单（含合规红线：官方授权接入、无验证码绕过/逆向）、
与 octopus 渠道/凭据模型的并入映射、推荐优先级。禁止再开任何新选型行。

### W0-3 T-log-002 日志分析（143）——**已过门，维持 done**

报告在仓且覆盖五类日志；其提出的四个修复任务（probe-4xx/cancel/model-refresh/health-window）
**维持"待立项"**，不进 ledger，待用户拍板后由调度登记。

## 2. W1 放行裁决（与 W0 门解耦的独立线）

W1 中不依赖 group 链的四项**放行**，其中两项已实质完成进入验收：

| 任务 | 车道 | 状态裁决 |
|---|---|---|
| T-acct-003 五类探测 | D/139 | **done 维持**：146 审计发现"2xx HTML/空体误判健康"已修复（JSON 可解析校验+回归测试），审计通过 |
| T-acct-004 手动/HTTP 余额 | D/145 | **review**：145 实质完成 custom_balance.go+6 单测（与 146 审计修复有一处同文件交叉，已互证）；但 145 违反 146 号令"W2 锁定"提前执行——**承认其产出、记录违规一次**；手动订阅落库/UI 部分待 W2 |
| T-acct-001 处理器/路由/迁移 | C/128 | **放行执行**：本轮唯一新开工项；交付口径不变（三接口+PKCE state+密文落库+单测） |
| T-quota-001 调度接线 | B/117 | **放行执行**：5min task 注册+channel→监控凭证映射+QuotaZeroStop 集成测试 1 例；完成即 T-quota-002 链路验收 |
| T-test-001 隔离测试 | H/136 | done 维持；其登记的 Auto-Model responses 超时疑点**并 B 车道集成测试一并复现**，不单开任务 |

## 3. 收敛与纪律

1. 140（group-002 改领转投 acct-004）、141、142、144（重复/空支线）四行统一标"已收敛关行"备注；
2. 146 审计行对 T-acct-004 的提前执行**不予追加处罚**（产出真实且测试全绿），但下不为例：W2 未开门
   而动 W2 任务，产出只标 review 不标 done；
3. T-acct-004 的"存储位置待用户确认"（手动订阅录入放渠道凭据还是独立表）列为 W2 开工前必答问题，
   由用户在验收时拍板；
4. 下轮调度只需盯两件事：W0-1 重建（A 车道）与 W0-2 报告（E 车道）；两者过门即 W0 全收，W2 解锁
   group-003 与 route-002。

## 4. 台账同步范围

- ledger：T-group-001 review→todo(重建)、T-acct-003 done 确认、T-acct-004 done→review、
  T-audit-001 doing→done（审计通过+1 修复）；
- 登记表/日志/清单：追加 148 节；worklog 索引入本篇；
- 业务代码：本轮零改动（146/145 的代码改动归属其会话，验收归调度）。
