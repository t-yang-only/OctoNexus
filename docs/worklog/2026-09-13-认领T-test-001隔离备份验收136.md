# 2026-09-13 认领 T-test-001 本地备份隔离测试（NM-CUR-136）

- 台账编号：`NM-CUR-136`｜路由：auto-skills 仅命中基座 `using-superpowers`
- 登记台：`docs/agents/ledger.md` T-test-001（todo，无认领）→ doing → done（cursor-local）
- claim：`docs/agents/claims/2026-09-13-cursor-local-T-test-001.md`（新建加锁）
- 用户指令：领取任务；遇阻登记等待指导；继续其他支线
- 来源：R-test-001（需求登记.md，用户原话）
- 结论：**隔离导入 + 全链路验证通过，临时产物已清理，全绿**

## 1. 未选任务（冲突留源，随时登记口径）

- T-pool-001：075/076 claim 在仓 + 30+ 行号池链 owner 在途 + 135 刚认领，不抢；
- T-quota-001/T-acct-001/T-group-003：doing 在途（117/128/120 会话）；
- T-acct-003~005/T-pool-002/T-route-002/T-proto-001：需设计稿或前置落地，本轮不硬领；
- T-log-002：用户指派但依赖日志包路径确认，本轮按 T-test-001 执行（用户上轮已指派过 T-quota-002，
  本轮 T-test-001 为 ledger 备注"分配待用户指派"项中唯一无阻塞可独立交付的支线）。

## 2. 隔离环境（绝不碰生产）

- 二进制：本轮工作树构建 `go build -o %TEMP%\oct-t001\octopus.exe .`（含 124 归零停模型 + 129 中转站登录等全部在树改动）；
- 配置：`admin_port=3399 / relay_port=1931`（生产默认 3303/1234 完全错开），host 127.0.0.1；
- 数据：`%TEMP%\oct-t001\data\data.db` 空库启动，auto-migrate 全新表；
- 备份：`d:\奇怪的软件\octopus-本地数据\数据备份(勿提交).json`（364131 字节，仓库外，全程只读引用，未入 git 未贴原文）。

## 3. 执行过程与结果

| 步骤 | 结果 |
|---|---|
| 空库启动 | 首次失败：`Set-Content -Encoding UTF8` 写出的 config 带 BOM，viper 解析 database.type 为空 → `unsupported database type: `；去 BOM 后 ALIVE=True（**坑已记：Windows PowerShell 5 UTF8 默认带 BOM**） |
| 登录冒烟 | `POST /api/v1/user/login`（admin/admin 初始口令）→ 200 |
| 备份导入 | `POST /api/v1/setting/import` → 200，`rows_affected`：channels 12 / keys 36 / models 259 / grants 264 / groups 340 / items 615 / llm_infos 132 / api_keys 5 / settings 4 / stats 1+4+320+5 —— 与 067 基线**逐行一致，无丢行** |
| 渠道核对 | 12 渠道全 enabled；259 模型；渠道 1（53HK）详情 1 凭据/13 模型/13 授权，响应仅含凭据名与启停位，**无明文 key 泄出** |
| 分组核对 | 340 组（202 斜杠 + 138 手工）；615 成员**全部 available=true**；协议位 {14,10}（chat+response+message 全开 / 缺 message）与备份声明一致 |
| /v1/models（1931） | 导入的 API Key（橘宝，`sk-oct***` 掩码打点）Bearer → 200，**340 模型**与分组数一致 |
| Chat 转发 | `53HK/Auto-Model` 非流式 → 200，真实上游响应（sensenova 模型，finish=length） |
| Anthropic 转发 | `/v1/messages` x-api-key → 200，`type=message` 结构正确 |
| Responses 转发 | `/v1/responses` 非流式 → 200 `status=completed`；流式 → 200，72 行 SSE 至 `response.completed`，usage 带缓存 token 明细 |
| 流式 Chat | `/v1/chat/completions` stream=true → 200，78 行 SSE 正常收尾 |
| 遗留异常 | `/v1/responses` 打 `53HK/Auto-Model` 组 60-90s 超时无响应（HTTP:000），换 `53HK-L/MiniMax-M2.7` 组立通 → 判定该 Auto-Model 组上游对该协议不兼容/慢，非网关缺陷（channel stats 该组 req_fail 与此吻合）；`GET /api/v1/log/history` 返回 total=0 —— relay_logs 落库路径在 state.go 在树（L223 RelayLogSave），隔离实例进程被杀时 WAL 未 checkpoint 即删库，历史查询留待提交链上复核（不阻塞本验收） |

## 4. 清理

隔离实例已停止（进程确认退出）；`%TEMP%\oct-t001\` 整目录已删除（含带导入凭证明文的 sqlite + WAL）。
生产库/生产端口全程未触碰；仓库工作树零业务代码改动。

## 5. 台账同步

- 登记表 136 翻已完成；工作日志补 136 节；文件清单补行；技能建议更新时间；
- ledger T-test-001 doing→done（备注：隔离验收通过，遗留 Auto-Model 组 responses 协议疑点转登
  需求登记「待确认」区供用户拍板是否立项排查）；worklog 本篇 + README 索引。
- 构建链复验：`go build` / `go vet` / `go test -count=1` 四包 ok / 双 check PASS / 密钥扫描 passed
  （本轮只读测试未改代码，复验以确认树未被污染）。
