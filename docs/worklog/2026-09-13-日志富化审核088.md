# 2026-09-13 日志富化审核088：四件套落地 + 并行断裂修复 + 全绿回归

- 台账编号：`NM-CUR-085`（总任务）+ `NM-CUR-088`（收尾校验）｜路由：auto-skills 仅命中基座 `using-superpowers`
- 用户指令：卡片三指标 / 十开关+自动刷新 / 内存筛选 / relay_logs 合并建模 / license 口径——小孩才做选择，全都要
- 来源：`NM-CUR-064` 参考报告（lingyuins/octopus `54f7eb7`，`reference/` 不入库）；实施主体为并行 074 会话（claim `T-log-001`）
- 结论：**四件套全部落地，全绿，已提交**（`f1d068f` + `6831a78` + `30bb95f`）

## 1. 四件套落地对照

| 用户项 | 落点 | 说明 |
|---|---|---|
| 卡片 TPS/缓存命中率/首字 | `internal/relay/state.go`（`FirstByteAt` + `markCommitted` 打点）→ `web/src/api/log.ts`（`first_byte_at?` 可选追加）→ `web/src/lib/log-metrics.ts`（`formatTPS/formatCacheHitRate/formatCNYCost/formatFirstByteMs`）→ `Item.tsx`（`LogMetrics` + `visible` 过滤） | 公式语义借鉴 fork，逐行重写；SSE 只追加可选字段，旧客户端兼容 |
| 十开关 + 自动刷新 | `web/src/components/modules/log/store.ts`（zustand persist 双 store）+ `FilterBar.tsx`（视图选项弹窗：十开关/重置/0-5-10-30s）+ `index.tsx`（`refresh` 重建 SSE 兜底）+ 三语 `log.filter` 21 键 | 与 025 偏好弹窗批准门合并考虑，后续可收敛；本地偏好不动业务接口 |
| 内存筛选 | `filter.ts`（`LogMemoryFilter/matchLogMemoryFilter`）+ `FilterBar.tsx`（`LogToolbar/useFilteredLogs` 桥接）+ `index.tsx`（状态+关键字本地过滤 + `已筛选 x/y`） | 持久化筛选等 relay_logs 分页稳定后再接（`useRelayHistory` 已备好未启用） |
| relay_logs 合并建模 | `internal/model/log.go`（`RelayLog/Filter/RetentionDays=7/PageMaxLimit=200`）+ `internal/op/log.go`（可注入 `dbConn` 的 Save/List/Clean）+ `internal/op/log_test.go`（主路径单测）+ `internal/db/db.go`（AutoMigrate）+ `internal/task/init.go`（`relay_log_clean` 同统计周期）+ `handlers/log.go`（`GET /history`）+ `api/log.ts`（`useRelayHistory`） | 与 UsageHourly 同周期清理；UsageHourly 小时桶聚合未动（另立 M-spec），本次只做"建模+写入+查询+清理"最小闭环 |

## 2. license 口径（确认）

- fork 与本仓库同为 **AGPL-3.0**（`LICENSE` 头均为 FSF AGPL v3），但本任务仍按"重写不复制"执行：
  `log-metrics.ts` 四公式、`store.ts` 双 store、`FilterBar` 组件、`RelayLog` 建模均为逐行重写实现，
  未复制 fork 文件；注释中"语义借鉴"仅为来源标注。
- `reference/lingyuins-octopus` 在 `.gitignore` 内，不入库，不提交。

## 3. 并行断裂修复（本轮实质动作）

- 现象：并行会话把 `matchLogMemoryFilter/useFilteredLogs/LogMemoryFilter` 拆到 `filter.ts`，
  但 `index.tsx` 仍从 `FilterBar` 导入 → `tsc` 报 4 错（TS2305/TS2459/TS18046/TS2322）。
- 中间态：`6831a78` 误删了 `FilterBar` 内的两函数（当时误判为"注释残留"，实为被 `index.tsx` 引用的实现）。
- 修复：`FilterBar.tsx` 恢复 `useFilteredLogs` 桥接实现 + `export type { LogMemoryFilter }` 重导出
  （`filter.ts` 保留判真函数为单一事实源），`30bb95f` 提交后 `tsc --noEmit` EXIT 0。
- 另修：`op/log.go` 并行合入时出现重复函数头（`relayLogListOn` 双声明）致 `go vet` 失败，已删重行；
  三语 locale 整文件重排已回退为 25 行最小增量（`log.list` 5 键 + `log.filter` 16 键）。

## 4. 测试（本轮复验，非缓存）

| 项 | 结果 |
|---|---|
| `go build -o NUL .` | EXIT 0 |
| `go vet ./...` | EXIT 0 |
| `go test -count=1` op/handlers/router | 三包 ok（含新增 `TestRelayLogSaveListClean`） |
| `npx tsc --noEmit` | EXIT 0（断裂修复后） |
| `check_auto_groups.py` / `check_protocol_probe.py` | 均 PASS |
| `pre_commit_secret_scan.py` | 每次提交前 PASS |

## 5. 提交链

- `f1d068f` feat(logs)：27 文件 +1204/-24（四件套主体，含 074/075 claims 与 067/072/069/071/083 他会话篇归档）
- `6831a78` fix(logs)：删 `FilterBar` 重复实现（中间态，后被 088 纠正为桥接恢复）
- `30bb95f` fix(logs)：恢复 `useFilteredLogs` 桥接 + 类型重导出，tsc 转绿

## 6. 台账同步

- 登记表：077/079 占位行关行（实质归 085/088）；085 总任务 done；088 收尾 done。
- 工作日志：追加 088 节（本节）；文件清单补 088 行；技能建议更新时间。
- ledger：`T-log-001` 仍为 074 会话认领态（本轮只做校验收尾，不抢认领）；待其会话或用户将其置 done。
- 本篇 + 索引行随下一归档提交（当前树干净，无需即时提交）。

## 7. 遗留（需用户）

1. `T-log-001` ledger 状态由 074 会话（或用户）置 done；
2. 持久化筛选 UI 接 `useRelayHistory`（等分页口径稳定）；
3. UsageHourly 小时桶聚合 M-spec（与 relay_logs 合并建模的第二阶段）；
4. 十开关与 025 偏好弹窗的收敛；
5. 日志页浏览器目检（新三指标/筛选/偏好弹窗渲染）。
