# 2026-09-13 巡检 119：登记台领取 T-group-002 失败 → 转抢险 + 构建链全绿

- 台账编号：`NM-CUR-119`｜路由：auto-skills 仅命中基座 `using-superpowers`
- 认领：`docs/agents/claims/2026-09-13-cursor-local-T-group-002.md`（新建）；
  ledger T-group-002 todo→doing（本轮认领动作）
- 结论：**T-group-002 未实施（前置已灭失）；本轮转为抢险：恢复被清零的已提交文件 + 剔除孤儿嵌套改动；构建链全绿**

## 1. 领取动作（随时登记）

- 登记台扫描：在途 40 行几全号池链（owner 在途，不抢）；ledger 空闲：T-pool-001（已认领）、
  T-quota-001（117 doing）、T-group-001（113 review）、T-group-002/003（todo）。
  按"依赖刚就绪"选 T-group-002，claim 加锁、ledger 置 doing。
- claim 落盘后进实施侦察，发现前置灭失（§2），T-group-002 失去实施基础，**未写一行选路代码**。

## 2. 前置灭失（证据链，ground truth 以 `rg`/diff 为准）

- `rg ChildGroupID internal/` 唯一命中 `internal/op/group.go`（113 会话的中间态改动，未提交）；
  `internal/model/group.go`、`internal/db/migrate/013*.go`、`internal/op/group_nesting_test.go`、
  worklog T-group-001 篇、claim T-group-001 篇**全是 0 字节空文件**（并行会话清零事故）。
- 构建直接失败：`internal/model/log.go:1:1 expected 'package', found 'EOF'` 等。
- 台账与事实背离：登记表 113 行写"模型层实现+3 单测全过"，ledger 113 备注写"ChildGroupID+013 迁移"，
  但模型文件根本不存在——113 的"已完成/review"不可信，其会话产物已灭失。

## 3. 抢险动作（本轮实质工作，未改任何业务语义）

- `git checkout --` 恢复被清零的已提交文件：`internal/model/log.go`、`internal/op/log.go`、
  `internal/op/log_test.go`、`internal/relay/state.go`、`internal/task/init.go`、`internal/db/db.go`、
  前端 log 四件套（`api/log.ts`、`FilterBar/Item/index/store/filter`、`log-metrics.ts`）、
  被删的 10 篇 worklog + 需求草稿 + T-log-001 claim。
- 删除 0 字节未提交文件：`internal/db/migrate/013*.go`、`internal/op/group_nesting_test.go`、
  T-group-001 篇 + claim（内容已灭失，占位无意义）。
- 回退 `internal/op/group.go` 的孤儿嵌套改动（`syncGroupItemsOn/gormTx/checkGroupNestingNoCycle/
  groupHasAvailableGrant` 等，模型层已不存在，留之必坏构建）+ 前端 `group/` 六文件的孤儿
  `child_group_id` 改动（后端无字段，留之必错）。回退脚本用后即删。
- 未动：`internal/health/`（117 自己的 T-quota-001 产物，4 文件完好）、`pelican-bike.html`（他会话杂散，不碰）。

## 4. 测试（本轮实测，非缓存，全绿）

`go build -tags=jsoniter ./...` EXIT 0；`go vet ./...` EXIT 0；
`go test -count=1 ./...` 四包 ok（health/op/handlers/router）；双 check 脚本 PASS；
`tsc --noEmit` EXIT 0；密钥扫描 passed。

## 5. 台账同步与遗留

- 登记表 119 翻已完成（领取失败转抢险）；日志补 119 节；清单补 119 行；技能建议更新时间；
  worklog README 加入本篇索引。
- ledger T-group-002 当前为 doing（本轮认领动作），但前置灭失故**建议改回 todo**：
  待 T-group-001 重新实施并合并后再领。改回动作留给用户/下轮巡检（本轮不擅自回滚认领，
  claim 在仓注明未实施）。
- 遗留（需用户/owner）：①T-group-001 需重新实施（113 产物灭失，行状态不可信）；
  ②T-group-002 待 001 落地后重领；③T-group-003 claim（120 会话新建）同样失去前置；
  ④`pelican-bike.html`/`fktest_main_tmp.go` 杂散文件清理；⑤0 字节清零事故的原因排查
  （并行会话写文件机制）。
