# 2026-09-14 登记台领取巡检 216：全量套件实测绿，指导沿用不造新口径后停止

- 台账编号：`NM-CUR-216`｜路由：`auto-skills` 仅命中基座 `using-superpowers`
- 认领文件：`docs/agents/claims/2026-09-14-cursor-local-T-patrol-216.md`
- 盘点：ledger 31 行 done 23 / review 2（T-sec-001、T-group-003）/ doing 4（pool-001、quota-001、acct-001、deploy-001）/ todo 2（pool-002、deploy-002），全部有主或门锁，**无自由 todo**
- 冲突避让：不抢 209（L6 复查）/212（指导）/213–215、217–219（并行会话在途）；不改 `docs/agents/ledger.md` 状态行；不碰 201 文件锁表；零业务代码改动

## 本轮实测（只读，全量）

- `go build -tags=jsoniter .`：EXIT 0
- `go vet -tags=jsoniter ./internal/...`：EXIT 0
- `go test -count=1 ./...`：全量绿——db/migrate、health、model、op、relay、handlers、middleware、router 全 ok，无 FAIL 包（较 207/218 的局部实测覆盖面更大，可作为 L1 归档与 L2 终验的链路基线）
- 草稿在树复核：`docs/requirements/2026-09-13-号池管理选型.md`（205/L7）与 `docs/requirements/2026-09-14-pool-002预研.md`（204/L8）均存在

## 指导（沿用 212 七路裁决口径，不重复造新口径）

1. **L1（deploy-001 归档）**：基线已全量绿，按 212-G2 工单归档：add `internal/op/backup.go` + `backup_raw_test.go` + 183 报告 + claim → 密钥扫描 → commit 带 `T-deploy-001`；doing 维持等用户拍板转 done
2. **L2（group-003 终验）**：web 9 文件跨车道干净可先合；195 接力终验 tsc+eslint+build/vet+op/router 全绿即 review→done；禁动 go 文件
3. **L3（quota 接线）**：217 在途未交付（task/init.go 无接线，218 已核）；按 212-G4 五步开工单，防误停集成测试是第一验收项；红线不碰 relay/
4. **L4（acct-001）**：212-G5 五步开工单不变；响应 JSON 禁明文是硬红线；禁碰 task/health
5. **L5/L6（relay 双路）**：L5 先合（flag 默认关+开/关单测），L6 后合且合前必 `go test ./internal/relay -count=1`；禁同时 commit
6. **L7/L8**：均已在树闭环，不再双写；pool-002 todo 维持预研不开工实现
7. **用户拍板四项**（不派子 agent）：deploy-001→done、R-pool-003 二选一、sec-001 key 轮换时机、deploy-002 开工

## 台账同步

- `agent_word/`：登记表 216 行 started→已完成 + 工作日志节 + 文件清单行 + 技能建议时间戳
- `docs/worklog/README.md`：追加本报告索引行
- 结论：无空闲 todo、无可独立交付的未完成问题；指导沿用有效口径，本行关行后停止（用户指令：无未完成问题先停止）
