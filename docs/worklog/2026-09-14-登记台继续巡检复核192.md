# 2026-09-14 登记台继续巡检复核192（NM-CUR-192，修op测试重声明+只读复核后停止）

- 台账：`NM-CUR-192`（本会话；191 并行巡检在途不抢）｜路由：`auto-skills → using-superpowers`｜
  认领：沿用 191 巡检口径（`docs/agents/claims/2026-09-14-cursor-local-T-patrol-191.md` 只读复核，不另开 claim）
- 盘点：`docs/agents/ledger.md`——done 23 / review 2（T-sec-001 待用户 key 轮换；T-group-003 待验收）/
  doing 4（pool-001/quota-001/acct-001/deploy-001，均有主）/ todo 2（pool-002/proto-001，前置未过门）。
  **无自由 todo，无可独立认领任务。**

## 1. 可做任务（唯一）：修 op 测试重声明（阻塞全量 test）

- 现象：`go test -count=1 ./...` 中 `internal/op [build failed]`——`backup_raw_test.go` 尾部
  `intPtrOf` 函数重复声明两次（183 会话 append 残留，与 `group_flat_test.go` 的 `intPtr` 改名注释同因）。
- 修复：删重复定义、留单份（注释保留改名说明），未改业务代码。
- 验证：`go vet ./internal/op/` EXIT 0；`go test -count=1 ./internal/op/` ok；
  全量 `go test -count=1 ./...` 八包 ok（migrate/health/model/op/relay/handlers/middleware/router）；
  `check_auto_groups.py` / `check_protocol_probe.py` 均 PASS。`pnpm tsc` 由 184/191 复核 0 错，本轮不重复。

## 2. T-deploy-001 / T-group-003 只读复核（不抢 183/184/191 车道）

- deploy-001：验收报告 183 证据链完整（D1–D9 全绿，D7/D9 轻量版如实标注），`backup.go` rowCopy 修复已在树；
  转 done 需用户拍板，本轮不翻 ledger 状态。
- group-003：184 复验 + 191 tsc 复核在位，后端零改动；待验收转 done，本轮不碰 web/ 9 文件。

## 3. 其余 doing / todo 口径（不抢车道，只重申门禁）

- pool-001：182 脱钩裁决维持（R-pool-003 待用户确认）；quota-001/acct-001 按 163 清单属 R2；
  pool-002/proto-001 前置未过不开工；deploy-002 前置 deploy-001 过门+用户拍板；sec-001 不代办。

## 4. 台账同步

- 登记表：192 started→done；191 并行巡检在途不代关。
- 日志：追加 192 节；文件清单：补 192 行（op 测试修复+本篇+索引）；技能建议：更新时间戳。
- 新增本篇 + worklog 索引一行；未改业务代码（仅测试文件去重），未做 git 提交。
