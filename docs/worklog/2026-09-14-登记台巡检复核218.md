# 登记台巡检复核218（NM-CUR-218：无空闲 todo，指导沿用 212 后停止）

- 台账编号：`NM-CUR-218`｜路由：`auto-skills` 仅命中基座 `using-superpowers`
- 认领文件：`docs/agents/claims/2026-09-14-cursor-local-T-patrol-218.md`
- 盘点：`docs/agents/ledger.md` 31 行——done 23 / review 2 / doing 4 / todo 2，全部有主或门锁，无自由 todo。
  - review：T-sec-001（用户动作，不代办）；T-group-003（171+184 证据齐，L2 接力 195 终验在途）。
  - doing：T-pool-001（205 草稿在树等用户 R-pool-003 拍板）；T-quota-001（217/L3 在途）；T-acct-001（C 车道/163 清单）；T-deploy-001（183 全绿+193/198 复核，L1 归档等用户拍板）。
  - todo：T-pool-002（201-L8 预研已交付 204，不开工实现）；T-deploy-002（三门前置未齐）。

## 1. 本轮实测复核（只读）

1. **209-L6 闭环确认**：`internal/relay/proto_matrix_test.go` 现 8 例在树——200 原五类矩阵 6 例 + 209 补 `TestProtoMatrixValidateResponseTerminal` / `TestProtoMatrixChatHTTP200ErrorBodySurfacesInParsedError` / `TestProtoMatrixAnthropicHTTP200ErrorBodyPassesTransform`（200-error 体两路不对称行为已锁口径）。`go test -tags=jsoniter ./internal/relay/ -count=1` ok（0.141s）、`go vet -tags=jsoniter ./internal/relay/` EXIT 0。
2. **L7 草稿确认**：`docs/requirements/2026-09-13-号池管理选型.md` 已在树（205 落盘，R-pool-003 建议搁置+四节大纲+收敛动作清单）；`2026-09-14-pool-002预研.md`（204）同窗口在树。均 untracked，属待归档文档，182 终裁与 212 G1 裁决维持。
3. **L3 现状确认**：`internal/task/init.go` / `task.go` 无 balance/quota 接线（关键词零命中）——217 在途未交付，接线清单仍按 163 五步执行，不抢不催。

## 2. 指导

- 七路裁决与开工单已由 `NM-CUR-212` 完整下发（G1-G7），本轮复核其口径全部仍然有效，不重复造口径、不代写、不代关他人行。
- 陈旧 started 行收敛名单（212 §2）建议调度层统一执行：本巡检会话只登记不动账。

## 3. 停止点

- 无空闲 todo 可领，无可独立交付的未完成问题；按用户指令指导后停止，不轮询等待。
- 下轮开工条件：① 217 交付 L3 接线（含 2 集成测试，防误停第一验收项）；② C 车道交付 L4；③ 用户拍板 deploy-001/R-pool-003/sec-001/deploy-002 四项。

## 4. 台账同步

- `docs/worklog/README.md`：追加 218 索引行。
- `agent_word/工作登记表.md`：218 started→done。
- `agent_word/工作日志.md`：追加 218 节；`文件清单.md` 追加 claim+worklog 两行；`技能建议.md` 刷新时间戳。
- 零业务代码改动，未改 `docs/agents/ledger.md`。
