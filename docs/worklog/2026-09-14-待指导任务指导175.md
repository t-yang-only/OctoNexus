# 待指导任务技术指导（NM-CUR-175，只读指导 + 树状态验证）

- claim：`docs/agents/claims/2026-09-14-cursor-local-T-guidance-175.md`
- 盘点：登记台 31 行——done 21 / review 1（T-sec-001，用户动作）/ doing 4
  （T-pool-001/T-quota-001/T-group-003/T-acct-001，他车道在途）/ todo 5
  （T-pool-002/T-route-002/T-proto-001/T-deploy-001/T-deploy-002，全部前置未过门）。
  **无自由 todo，无可独立交付的未完成问题，按用户指令指导后停止。**
- 本轮零业务代码改动（仅指导报告 + 台账四件套同步）。
- 树验证（本轮实测）：`go build -tags=jsoniter` EXIT 0；`go vet ./...` EXIT 0；
  `go test -count=1 ./...` 八包 ok（migrate/health/model/op/relay/handlers/middleware/router）；
  双 check PASS；密钥扫描 passed。

## 1. T-pool-001 号池选型报告（T-deploy-001 唯一残门）指导

- 现状：135 会话在途持有，报告唯一口径 `docs/requirements/2026-09-13-号池管理选型.md`
  在树但仍为空/未成稿（163 收口口径：文件在树且非空壳才过门；160 已授权"代写或收回改派"）。
- 指导（代写或 135 交付均按此四节写，一份报告即过门，禁再开新选型行）：
  1. **候选清单**：ChatGPT/Gemini 官方账号池开源候选各 2–3 个，字段统一
     （协议/登录方式/额度可见性/封号风险/维护活跃度）；只收录"官方授权接入"
     合规项，验证码绕过/逆向/cookie 盗用一票否决并写明红线。
  2. **合规红线**：与 T-acct-001 同口径——仅走官方 OAuth/扫码授权；
     凭据密文落库（AccessCipher/RefreshCipher），日志/输出绝不出现 Key（R-sec-001）。
  3. **并入映射**：候选账号→octopus 渠道模型（Channel/ChannelKey/ChannelModel/
     ChannelGrant 四件套）+ 监控凭证映射（baseURL/monitorToken/useProxy，
     复用 T-quota-001 接线清单：task 5min 周期→阈值事件→QuotaZeroStop→审计）+
     跳转页（T-acct-005）作为人工兜底。
  4. **推荐优先级**：OpenAI/Gemini 先行、Claude 后续（与 T-pool-002 W3#1 一致）；
     给出"先接哪一个+为什么+风险"一句话结论。
- 验收线：文件在树、非空壳、四节齐、红线明确；过门即解锁 T-deploy-001 D1-D9。
- 若 135 持续零交付：按 160 授权由调度会话代写或收回改派，本会话不代写（避免双写同一报告）。

## 2. 其余 doing / todo 口径（不抢车道，只重申门禁）

- T-quota-001（117/B 车道，R2）：接线清单见 ledger 行（task 周期+凭证映射+QuotaZeroStop
  集成 2 例+Auto-Model responses 超时复现）；属 R2，**不阻塞 R1 D1-D9**。
- T-acct-001（128/C 车道，R2）：实现清单见 ledger 行（三接口+PKCE+密文落库+单测）；
  合法官方 OAuth，**不阻塞 R1 D1-D9**。
- T-group-003（171/F 车道）：门已开，在途实施中；171 会话持有 `group.ts`+三语
  locale dirty，本会话不触碰（170 口径延续，避免双写冲突）。
- T-pool-002/T-route-002/T-proto-001：各自前置（W1#3/W2#2/T-proto 矩阵/W1 全过）未过门，不开工。
- T-deploy-002：前置=T-deploy-001 过门+用户拍板；T-sec-001 未轮换 key 不得上线。
- T-sec-001（唯一 review）：待用户动作（轮换线上渠道 key + 确认旧备份处置），不代办。

## 3. 停止点

- 下轮开工条件（按优先级）：① T-pool-001 报告落盘 → T-deploy-001 D1-D9；
  ② 171 收口 T-group-003；③ B-117/C-128 他车道完成即验收 quota-002 链路/acct-001。
- 本会话指导后停止，不轮询等待。
