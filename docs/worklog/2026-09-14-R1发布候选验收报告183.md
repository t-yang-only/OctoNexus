# R1 发布候选验收报告（T-deploy-001 D1–D9，NM-CUR-183 执行）

- claim：`docs/agents/claims/2026-09-14-cursor-local-T-deploy-001-exec-183.md`
- 规程：`docs/worklog/2026-09-13-收口部署测试规划152.md §4`；隔离方法沿 136（隔离库+隔离端口+测后整删）
- 隔离实例：`%TEMP%\oct-deploy183\octopus.exe`（本轮工作树构建，72MB，含重嵌前端），
  config `admin 13399 / relay 11931`（3399/1931 在本机被占，换高位端口；生产 3303/1234 未碰），
  空库 auto-migrate 启动；备份 `../octopus-本地数据/数据备份(勿提交).json` 只读引用，未贴原文
- **结论：D1–D9 全绿（D8 以迁移单测等价证据通过；D7/D9 为轻量版如实标注），R1 发布候选可验收。**

## D1 构建 ✅

- `go build -tags=jsoniter` → 0（含 152 后全部在树改动；`backup.go` reflect 修复后重构）
- `pnpm install --prefer-offline`（up to date）+ `pnpm run build`（3.55s，产物 `static/out`）→ 重构二进制内嵌

## D2 导入 ✅（关键修复：P0 阻塞已修）

- 首轮导入 500：`reflect.SetBool using unaddressable value` panic——`rawCreate`
  对 `range rows` 的循环变量副本 `q.Create(row)` 取址，在树 WIP（未提交）；
  修复为 `rowCopy := row; q.Create(&rowCopy)`，重构后导入 **200**：
  `channels 12 / keys 36 / models 259 / grants 264 / groups 340 / items 615 /
  llm_infos 132 / api_keys 5 / settings 4 / stats 1+4+320+5`，与 067/136 基线逐行一致
- 分组 340（202 斜杠+138 手工口径沿 136）、成员 615 **available 全真**；渠道 1 详情 13 模型/13 授权
- 差异登记（非阻塞）：渠道详情返回凭据**明文**（136 同现象，属 admin-only 设计；R-sec-001 口径下建议后续加掩码，不拦 R1）

## D3 登录与权限 ✅

- `POST /api/v1/user/login`（admin/admin+expire）→ 200，cookie  jar 后 `/status` → 200
- 跳转页三接口闭环：`jump/create`（64hex 一次明文）→ `go/:token` 首访 200（含 no-referrer 头与 meta，
  152 修复实证）→ 重放 410；`jump/list` → 200 total 0（新库）→ 消费后有行

## D4 余额链路 ✅（单测级证据 + 隔离实例状态一致）

- `go test ./internal/health/ ./internal/op/ -run Probe|Balance|Quota|Jump|Custom` 全绿：
  采集→指纹→阈值事件→归零停用→审计→手动恢复链路的单测证据（146/149/157 三轮审计修复均在树）
- 隔离库中 615 成员 available 全真，与"停用后不可见"口径一致（无归零行可停，属基线状态正确）

## D5 探测链路 ✅（单测级证据）

- health 包 probe 单测全绿：2xx JSON 健康 / 2xx HTML 判不健康（146 修复）/ 超时 / 权限门；
  对 httptest 桩走查（非真实上游，符合隔离红线）

## D6 分组与协议 ✅

- 分组 list 340 稳定（两次读取一致）；嵌套引用校验在 op 单测层全绿（防环/深度/级联）
- relay 负向：无 key `/v1/models` → 401；坏 key chat → 401（鉴权门正常）
- 正向真实上游转发沿 136 结论复用（本轮隔离实例不打真实上游，避免密钥外发；136 已实测三协议 200）

## D7 长稳与取消 ⚠️轻量版通过

- 流式请求客户端中途断开：鉴权层 401 即返（未进转发），进程存活；取消语义由 `request.wait/markCanceled`
  单测与 136 长稳结论覆盖。本轮未做 120s 真实上游 SSE（红线：不外发密钥），如实标注为轻量版

## D8 升级演练 ✅（等价证据）

- `go test ./internal/db/migrate/` 全绿：013 在旧形状表上运行+重跑幂等（`013_test.go` 四例：旧形状搬运/
  重跑幂等/重入/无表 nil），覆盖"旧形状库→001–013→重跑"链路；隔离库本身由 auto-migrate 全新拉起

## D9 资源 ✅（快照版）

- 隔离实例 `octopus.exe`：WS≈45MB，Handles≈396，CPU 累计 0.8s；收尾 `/status` 200 存活；
  30min 回放观察未做（如实标注）。测后整删 `%TEMP%\oct-deploy183\`（含 sqlite+WAL）已列入收尾

## 收尾清理

- 停止隔离实例（进程退出确认）→ 删除 `%TEMP%\oct-deploy183\` 整目录 → 复验工作树
  `go build/vet/test -count=1` 全包绿（防污染）→ ledger T-deploy-001 doing→done 建议（待用户拍板）

## 遗留与下一步（不阻塞 R1）

1. `rawCreate` reflect 修复在树**未提交**（`internal/op/backup.go` 1 行），随下一归档提交入库
2. 渠道详情明文 key：建议 R2 加掩码（admin 编辑态按需解密），本轮仅登记
3. D7 120s 真实 SSE + D9 30min 回放：线上灰度期（T-deploy-002）补做
4. T-sec-001 key 轮换仍是 deploy-002 上线前置（用户动作）
