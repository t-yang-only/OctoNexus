# T-proto-001 L6 缺口复查（NM-CUR-209，观察项闭环：characterization 锁定+修复建议）

- claim：`docs/agents/claims/2026-09-14-cursor-local-T-proto-001-L6-209.md`
- 调度：NM-CUR-201 §1 L6（锁文件 `internal/relay/proto_matrix_test.go`；禁碰 handler.go/route.go/balance.go L5 锁；只读优先、确认缺口才补 httptest）。
- 对象：`internal/relay/protocol.go`（`inspectStreamEvent`/`validateResponse`）+ `internal/relay/upstream.go`（passthrough/converted 双路径）+ axonhub `llm@v0.0.0-20260909170523-3786f2c5c8de` transformer 源码。

## 1. 观察项复查结论（200 报告登记项，属实）

- `validateResponse` 只覆盖 Responses 终态（`FinishReason` error/length/cancelled）；Chat/Anthropic 非流式 200-失败终态依赖 `TransformResponse` 侧抛错（`upstream.go:92` 有 `err: body` 包装）。
- 实证 axonhub 源码：
  - openai `TransformResponse`（`transformer/openai/outbound.go:239`）：仅 `StatusCode>=400` / 空体 / JSON 解析错抛错；其余走 `ToLLMResponse` 常返 nil err——但 200-error 体会进 `parsed.Error`（`model.go:311 Response.Error` → `outbound_convert.go:427` 透传到 `llm.Response.Error`）。
  - anthropic `TransformResponse`（`transformer/anthropic/outbound.go:304`）：同式三抛错；其余走 `convertToLlmResponse` 常返 nil err——200-error 体（`{"type":"error",...}`）会被当成无 content 的空 Message 吞掉，**连 `parsed.Error` 都没有**。

## 2. 本轮交付：2 例 characterization（现状锁定，非修复）

- `TestProtoMatrixChatHTTP200ErrorBodySurfacesInParsedError`：Chat 200-error 体 `TransformResponse` 返 nil err 但 `parsed.Error != nil`，`validateResponse` 现状放行——锁定"信息不丢、门未设"现状。
- `TestProtoMatrixAnthropicHTTP200ErrorBodyPassesTransform`：Anthropic 200-error 体 `TransformResponse` 返 nil err 且 parsed 非 nil，`validateResponse` 现状放行——锁定"信息被吞、门未设"现状（比 Chat 更弱）。
- 过程修正记录：首版曾误判"200-error 应在 TransformResponse 被拒"并写了 2 例断言拒绝，实测双 FAIL 后读源码纠正为现状锁定——误判本身证明了"凭印象补测试"的风险，已按 L6 只读优先口径收回，不改转发语义。

## 3. 修复建议（留给转发语义 owner，不在本 lane 执行）

- 在 `validateResponse`（relay 自有函数，非 transformer）补 `parsed.Error != nil → ResponseError` 门：Chat 200-error 立即闭环；Anthropic 需先让 `convertToLlmResponse` 把 `{"type":"error"}` 映到 `parsed.Error`（或在 relay 侧对 anthropic error 体做预检），否则门补了也接不住。
- 两例 characterization 已写明"修复后须同步改为断言拒绝"，届时改动面：`protocol.go` 1 函数 + 本文件 2 例，不碰 handler/route/transformer。

## 4. 验证

- `go test -tags=jsoniter ./internal/relay/ -count=1` 全绿（含矩阵 8 例）；`go build -tags=jsoniter ./...` 过；`go vet -tags=jsoniter ./internal/relay/` 过；`py scripts/pre_commit_secret_scan.py` passed。
- 只动 L6 锁文件 1 个（`internal/relay/proto_matrix_test.go`：import +2 例），L5 三文件零触碰。

## 台账

- T-proto-001：done 维持（200 矩阵 + 本轮缺口闭环）；待用户/调度拍板归档。
