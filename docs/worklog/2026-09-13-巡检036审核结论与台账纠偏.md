# 2026-09-13 巡检 036：审核结论与台账纠偏（bug 022 补充裁决）

- 台账编号：`NM-CUR-036`｜路由：auto-skills 仅命中基座 `using-superpowers`（无缺失成员）
- 前置：`NM-CUR-037` 已于 02:57:38 关行（登记表 034/035/036/037、日志 037 节、worklog 索引 23/23 对齐）。
  本轮 036 是并行巡检会话之一——当时只登记了 started 空行（无产出、无日志节），
  被 037 误判为"并行巡检空行：无修改内容、无文件产出"。事实上**本会话有实质产出**
  （源码级复核 + `go build/vet/test` 实测证据），故本轮以"审核结论"纠正 036 行，
  并补上日志节与文件清单，使台账恢复一致。

## 1. 登记（本轮 /nm-skills 动作）

| 台账 | 动作 |
|---|---|
| 工作登记表 | `NM-CUR-036` started→done（本任务：源码级复核 037 裁决 + 实测三件套，输出本篇纠偏） |
| 工作日志 | 追加 036 节（本节） |
| 文件清单 | 补 036 行（本篇） |
| API 变更 | 无变更（纯研究/审核） |
| worklog 索引 | 本篇入索引（24/24 对齐） |

## 2. 本轮实质工作（037 关行之后的新增量）

1. **双台账一致性**：登记表 38 行（001–038，034/035/036/037/038 均为已完成，无"进行中"）；
   工作日志覆盖 001–033、036、037（008/020/032 为审核关行的侦察行，无独立节，属既定惯例）；
   API 变更 5 行；文件清单补至本轮；ledger 8 任务无 todo。
2. **worklog 索引**：23 条目 ↔ 23 文件，双向对齐（索引有但文件缺：无；文件有但索引缺：无）。
3. **git 工作区**：`master...octonexus/master`，业务改动 = 并行会话两处
   （`handlers/channel.go` 探测口径注释+签名、`home/index.tsx` ModelMonitor 区块），
   其余为台账/docs；未提交属正常（项目惯例台账随任务攒批提交）。
4. **构建/测试实测**（Go 1.27，`C:\Program Files\Go\bin\go.exe`，PATH 外但存在）：
   `build -tags=jsoniter` EXIT 0；`vet ./...` EXIT 0；`test -count=1 ./...` 全过
   （op/handlers/router ok，其余无测试文件——测试面仍是缺口）；
   `scripts/check_auto_groups.py` + `check_protocol_probe.py` 全 PASS。
5. **037 对 022 报告的裁决复核**（逐条打开源码 + `rg` 调用链枚举核对）：
   - 同意 037：M-1 **成立但不升级**（见 §3）、M-3 口径问题、M-5 成立、M-7 成立、H-4 成立。
   - 纠正 037：**C-1 恢复成立（Critical）**——`go list -deps ./internal/price` 显示
     price 包已依赖 `internal/op`（为读 `SettingGetString(proxy_url)`），但全仓调用链仍是
     `UpdateLLMPrice`（task 定时 + update-price 按钮）→ 只刷 price 包内存 `llmPrice`，
     而计费 `usageMetrics`（`relay/state.go:233`）读 `op.LLMGet` → DB 回填 `llmModelCache`；
     唯一写库入口 `LLMBatchSave` 只有"重建价格"在调，定时路径对"已同步价格"零消费。
     所谓"渠道创建时消费"（`addChannelModelPrices`→`LLMBatchCreate`）只发生在新增模型瞬间，
     之后价格漂移永不回填——报告原文"C-1 计费长期用旧价"成立；037"已推翻"不成立。
   - 纠正 037：**C-2 恢复成立（Critical），降级"丢数"子项为 Warning**——报告原文的
     (a) 阻塞（relay 全局 `mu` 内同步全量 flush）成立；(b) 丢数子项不成立：
     `persistStatsSnapshots` 出错确会 `restoreStatsDirty`（`stats.go:105-113`）恢复脏 ID，
     但恢复的是** ID 集合**而非数据快照——前一天增量躺在已 swap 出去的 `prevDaily`
     局部变量里，本轮 `StatsSaveDB` 的后续 flush 拿的是新日期快照，`prevDaily` 永远不再被写。
     结论：C-2 标题成立（阻塞+丢数），037"维持打回"过度，"失败即丢"不成立改为部分成立。
   - 037 未复核但本轮确认仍成立：H-1（`handler.go` 四处 `wait+continue` 永不返回）、
     H-2（双端口零超时）、H-3（登录 bcrypt 无限制）、M-2、M-4、M-6、M-8。
     H-2 修正：relay 写超时不能一刀切——流式长尾合法存续数分钟，`WriteTimeout` 会掐断正常流，
     正确做法是 `ReadHeaderTimeout` 必加 + 整请求 deadline（与 H-1 同一修复）。

## 3. M-1 定级：维持 Medium（不同意 037 升 High）

`op/apikey.go:94-113`：`Delete(&k)`（k.APIKey 为空串）→ `apiKeyIDMap.Del("")`，
真实 key→id 映射永久残留——**机制确认成立**。但定级 High 的理由不充分：
残留映射指向的 id 经 `APIKeyGet` 查缓存必 miss → 401，无法鉴权、无越权、无数据错；
影响仅 map 条目堆积（删 key 才触发一次， personal 网关量级可忽略）+ 需重启清理。
按"可利用性×影响面"应维持 **Medium**（与内存泄漏同级，修法不变：删除前先 Get 取旧串）。

## 4. 修正后的 022 报告状态总表

| ID | 022 原文 | 037 裁决 | 036 终裁 |
|---|---|---|---|
| C-1 | 成立 Critical | 已推翻降级 | **恢复成立 Critical**（§2.5 调用链证据） |
| C-2 | 成立 Critical | 维持打回 | **恢复成立 Critical**（阻塞成立；丢数→部分成立，脏 ID 恢复≠数据快照恢复） |
| H-1/H-2/H-3 | 成立 | 确认 | 确认（H-2 修法修正：ReadHeaderTimeout+整请求 deadline，不设 WriteTimeout） |
| H-4 | 成立 | 确认 | 确认 |
| M-1 | 成立 Medium | 确认**升 High** | **确认，维持 Medium**（§3：无越权/无数据错，仅堆积） |
| M-2/M-4/M-6/M-8 | 成立 | 确认 | 确认 |
| M-3 | 口径问题 | 口径问题 | 同意（拆 `attempt_failed` 列或 UI 注明） |
| M-5/M-7 | 成立 | 确认 | 确认 |

转 ledger 缺陷任务建议沿用 037 的分组（R-sec-002/R-relay-001/R-stats-001/R-backup-001/R-models-001），
另加 **R-price-001（C-1：定时同步落库回填）** 与 **R-stats-002（C-2：翻天落旧日异步化）**，
立项仍待用户拍板（需求登记三通道合并事项未决，不擅自立项）。

## 5. 遗留（需用户，与 037 一致 + 新增）

1. T-sec-001 线上 key 轮换（用户执行）；
2. 需求三通道合并（018 requirements / 019 worklog / 需求登记.md 定单一事实源）；
3. 缺陷任务立项（含新增 R-price-001/R-stats-002）；
4. M1/M2/M3 批准门；
5. **新增**：022 报告建议追加"勘误 §C-1/C-2/M-1"小节，或由缺陷任务立项时直接引用本篇 §4 总表。

## 6. 本篇变更

- `agent_word/工作登记表.md`：036 行 started→done（含本篇结论摘要）。
- `agent_word/工作日志.md`：追加 036 节。
- `agent_word/文件清单.md`：补 036 行。
- 新增本篇 + worklog 索引一行；未改任何业务代码。
