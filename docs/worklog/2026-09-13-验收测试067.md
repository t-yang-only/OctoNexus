# 2026-09-13 验收测试 067：已完成功能后端+部署+隔离实例全链路

- 台账编号：`NM-CUR-067`｜路由：auto-skills 仅命中基座 `using-superpowers`（无缺失成员）
- 结论：**全部通过**。后端三件套+双 check 脚本+tsc 全过；
  隔离实例（13303/11234，空库启动，`../octopus-accept-tmp`，事后实例已停、二进制已删）
  验证：双端口分流、登录+鉴权、渠道创建→自动分组→统计、fetch-model 探活失败语义、
  relay 未授权 401、前端类型检查。

## 1. 登记（本轮 /nm-skills 动作）

| 台账 | 动作 |
|---|---|
| 工作登记表 | `NM-CUR-067` started→done（本任务） |
| 工作日志 | 追加 067 节（本节） |
| 文件清单 | 补 067 行 |
| API 变更 | 无（验收未改接口） |
| worklog 索引 | 本篇入索引 |

## 2. 后端验收（非缓存，Go 1.27）

| 项 | 结果 |
|---|---|
| `go build -tags=jsoniter -o NUL .` | EXIT 0 |
| `go vet ./...` | EXIT 0 |
| `go test -count=1 ./...` | op/handlers/router 三包 ok（含 048 seenMu 修复与 045 隔离修复） |
| `check_auto_groups.py` / `check_protocol_probe.py` | 均 PASS |
| `tsc --noEmit`（web） | EXIT 0（仅 npm trust-lockfile 警告） |

## 3. 部署验收（隔离实例，admin=13303 / relay=11234）

| 检查 | 结果 |
|---|---|
| 空库启动 + 初始用户 admin/admin | 启动日志 `initial user: admin`，`Program started` |
| 管理面板根 `/` | 200（len 4249，HTML） |
| 登录 `POST /api/v1/user/login` | 200，Set-Cookie auth=JWT |
| `GET /api/v1/group/list`（空库） | 200，`data:[]` |
| `GET /api/v1/channel/stats`（空库） | 200，`data:[]` |
| `GET /api/v1/model/list` / `apikey/list` / `channel/grants` | 均 200 空数组 |
| 不存在的管理接口（setting/get、stats/overview） | 404（符合预期：该接口本就不存在，非回归） |
| 创建渠道 `POST /api/v1/channel/create` | 200，id=1 落库 |
| 自动分组（006 功能） | `group/list` 出现 `accept-test/gpt-x`（failover，成员 priority=1，与授权联动） |
| 渠道统计（039 功能口径） | stats 出现该渠道+模型零值行（累计口径可用） |
| fetch-model 探活坏上游（011 功能） | 502 并带上游原文（`openai:/anthropic:` 双侧错误语义符合 handler 注释） |
| relay 未授权 `/v1/models` | 401（鉴权门禁有效） |
| 未登录调管理接口 | 401（管理侧鉴权有效） |
| 数据隔离 | 验收库/二进制/日志全在 `../octopus-accept-tmp`，生产 `data/` 未动；事后进程已停、二进制已删 |

## 4. 功能→验收映射

- 006 渠道模型自动创建分组：渠道创建后自动出现 `渠道名/模型名` 分组——**通过**。
- 009 双端口拆分：admin 与 relay 分端口响应、鉴权各自生效——**通过**。
- 011 拉取实测三协议：坏上游 502 带双侧原文；好上游路径由单测三例覆盖——**通过**。
- 027/031 部署验证：本次即隔离部署复验——**通过**。
- 039 home 监控区块：tsc 通过；运行时数据口径（stats 快照）已验证有数——**通过**（页面目检待用户侧浏览器确认）。

## 5. 遗留（需用户）

1. 064（lingyuins 日志分支）/066（号池选型）为独立进行中任务，本轮未动；
2. 前端页面目检（浏览器内 ModelMonitor 渲染/三语切换）待用户侧确认；
3. 本篇及 061 草稿归档提交与否由用户定（065 留置口径延续）。

## 6. 本篇变更

- `agent_word/工作登记表.md`：067 started→done。
- `agent_word/工作日志.md`：追加 067 节。
- `agent_word/文件清单.md`：补 067 行。
- `agent_word/技能建议.md`：更新使用时间戳。
- 新增本篇 + worklog 索引一行；未改业务代码，未做 git 提交。
