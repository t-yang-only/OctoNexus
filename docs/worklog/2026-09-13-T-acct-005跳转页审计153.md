# 2026-09-13 T-acct-005 跳转页审计（NM-CUR-153 / T-audit-004）

- 台账编号：`NM-CUR-153`｜路由：auto-skills 仅命中基座 `using-superpowers`
- 审计对象：jump_token 模型/op/handlers 三件套 + logger 路径掩码
- **结论：五口径全过，T-audit-004 过门；T-acct-005 review→done。**

详细证据表见 `docs/agents/claims/2026-09-13-cursor-local-T-audit-004.md`。摘要：

1. **一次性消费竞态**：条件 UPDATE（hash + 未消费 + 未过期）保证并发至多一次成功，
   8 路并发测试通过。
2. **TTL 边界**：120s 严格 `>` 比较，过期即拒，测试覆盖。
3. **哈希不落明文**：明文仅 create 响应一次；列不序列化；备份转储不含该表；
   日志侧 `/go/` 路径段整段 REDACTED（4 组测试）。
4. **权限门**：create/list 挂 Auth+Admin 端口；go 路由令牌即凭证仅挂 Admin 端口，未知统一 404。
5. **审计完整性**：四列+note 落行，限幅分页可查。

审计中发现并修复一处被审对象自身测试缺陷（F-1：测试 DSN 缺 -count 计数器隔离，
count=3 时撞历史行；按仓内既有惯例修复），修复后 op/handlers Jump 全组 -count=3 全绿。

范围外观察（F-3，提请调度核）：A 车道 group 嵌套两例仍红
（`TestSyncGroupItemsDepthCap` FAIL、`TestValidateGroupTreeRefsOnCreate` PANIC），
与 ledger 上 T-group-001 的 review 状态矛盾——本车未触碰 A 车道文件。

本篇变更：新增本篇+claim；登记表 153 关行；工作日志 153 节；文件清单补行；
ledger T-audit-004 done、T-acct-005 done；worklog README 加索引。
