# 2026-09-13 全量巡检补完与审核测试（NM-CUR-037）

- 台账编号：`NM-CUR-037`｜路由：auto-skills 仅命中基座 `using-superpowers`
- 结论：**无未完成工作，补完 5 项待复核/缺漏后审核通过**（遗留 4 项需用户拍板）。

## 1. 登记（本轮 /nm-skills 动作）

| 台账 | 动作 |
|---|---|
| 工作登记表 | `NM-CUR-037` started→done（本任务）；034/035/036 三个并行巡检空行关行（started 空行、无日志节、无产出，备注关行）；032 行已由 030 关行 |
| 工作日志 | 追加 037 节；034/035/036 无节可补（空行） |
| 文件清单 | 补 037 行（本篇） |
| ledger | 8 任务无 todo，无需改动 |

## 2. 未完成项核查（逐项）

- 登记表 36 行：001–033 全已完成；034/035/036 为并行会话 started 空行（无修改内容、无文件、无日志节）→ 本轮关行；037 本任务 → 关行。
- 工作日志：001–033（除 008/020 侦察行历史惯例）全有节；034–036 无节（空行无内容可补）；037 本节。
- 文件清单：截至 033 齐备；本轮补 037 行。
- ledger：6 done + T-sec-001 review（等用户轮换密钥）+ T-agents-001 done，无 todo。
- claims：001–004 与 research 报告对应。

## 3. 022 待复核项补完（本轮逐行打开源码验证）

| 项 | 结论 | 证据 |
|---|---|---|
| C-2 跨天首请求阻塞转发+失败丢前一天数据 | **维持打回** | `op/stats.go:222-237 StatsDailyUpdate`：跨天走 `statsSaveDBWithDailyOverride(ctx, prevDaily)` 同步落库（`state.go:205` 在 relay 全局 `mu` 内调用）；但"阻塞转发"需量化（DB 本地写入通常毫秒级），"失败即丢"不成立——`persistStatsSnapshots` 出错会 `restoreStatsDirty` 恢复待写标记（`stats.go:105-113`），下次 flush 重试。结论：跨天同步落库路径**存在**（性能隐患成立），"丢数据"不成立。请作者改写为"跨天同步落库性能隐患"并给出耗时实测，否则不立项 |
| M-1 APIKeyDelete 删错映射 | **确认成立，升级为 High** | `op/apikey.go:94-113`：先 `Delete(&k)`（k.APIKey 为空串），再 `apiKeyIDMap.Del(k.APIKey)` 删的是 `""` 键，真实 key→id 映射永久残留（重启才清）。修复：删除前先 `APIKeyGet` 取出旧串再 Del（与报告建议一致）。建议转 ledger 缺陷任务 |
| M-3 通道侧失败计数含重试轮次 | **确认属实，定性为口径选择（非 bug）** | `relay/handler.go:211-214` 每轮失败 +1（渠道/模型/凭据），请求级终态在 `state.go:finishLocked` 记一次。建议：UI 注明口径或拆 `attempt_failed` 列（同意报告建议） |
| M-5 `/v1/models` 按请求头选形状 | **确认成立（Medium）** | `handlers/model.go:59-106`：带 `x-api-key` 头 → Anthropic 形状，否则 OpenAI 形状；但两种鉴权头都可调两种协议。按路由端点定形状（`/v1/models` 归 OpenAI 侧）或同时返回两套字段 |
| M-7 备份导入无上限 ReadAll | **确认成立（Medium）** | `handlers/setting.go:106/116` 两条路径无界 `io.ReadAll`。修复：`http.MaxBytesReader` 设限（如 256MB）超限 413（同意报告建议） |
| H-4 CORS `*`+凭据组合 | **确认成立** | `middleware/cors.go:14` `AllowCredentials=true` 写死 + `allowed=="*"` 时直接 `return true`（22-30 行）。修复：`*` 时强制 `AllowCredentials=false` 并打 warn |

022 报告状态更新：C-1（已推翻降级）/ C-2（维持打回，理由已细化）/ H-1/H-2/H-3/M-2/M-4/M-6/M-8（确认）/ M-1（确认，升 High）/ M-3（口径问题）/ M-5/M-7/H-4（确认）。建议转 ledger 缺陷任务：R-sec-002（H-3/M-8/H-4）、R-relay-001（H-1/H-2/M-2）、R-stats-001（M-1/M-3/M-4）、R-backup-001（M-6/M-7）、R-models-001（M-5）。

## 4. 测试（本轮实测）

`go build -o NUL .` EXIT 0；`go vet ./...` EXIT 0；`go test ./...` 全过
（op/handlers/router ok，其余无测试文件）。前端上轮已验（pnpm build 通过），本轮 Go 侧无变动不重复。

## 5. 遗留（需用户）

1. T-sec-001 线上 key 轮换；2. 需求三通道合并；3. 上述缺陷任务立项；4. M1/M2/M3 批准门。

## 6. 本篇变更

- `agent_word/工作登记表.md`：034/035/036 关行，037 登记+关行。
- `agent_word/工作日志.md`：追加 037 节。
- `agent_word/文件清单.md`：补 037 行。
- 新增本篇 + worklog 索引一行；未改任何业务代码。
