# 登记台巡检指导208（只读巡检+指导，不抢 lane）

- 台账：`NM-CUR-208`｜路由：`auto-skills` → 基座 `using-superpowers`
- 登记台：31 行（done 24 / review 2 / doing 4 / todo 2，经 201 调度后口径）
- claim：`docs/agents/claims/2026-09-14-cursor-local-T-patrol-208.md`
- 初判认领 L7（R-pool-003 草稿）→ 实查 `docs/requirements/2026-09-13-号池管理选型.md` 已于 13:42 由 205 落盘（R-pool-003 草稿，201-L7 收敛），L8 `2026-09-14-pool-002预研.md` 同窗口落盘；双写避让，不复写。
- 结论：**无自由 todo 可领（pool-002/proto 前置未过门→proto已done剩pool-002；deploy-002 前置未过），doing/review 均有主，按用户指令输出指导后停止。零业务代码改动。**

## 1. 盘点（门锁核验）

| 行 | 状态 | 门锁/归属 | 本轮动作 |
|---|---|---|---|
| T-pool-001 | doing | 201-L7 收敛出口，R-pool-003 草稿已在树待用户确认 | 不碰，见 §2 指导 |
| T-quota-001 | doing | B-117 在途（201-L3） | 不抢，见 §3 指导 |
| T-group-003 | review | 171 实施+184 复验，195 验收在途（201-L2） | 不抢，见 §4 指导 |
| T-acct-001 | doing | C-128 在途（201-L4） | 不抢，见 §3 指导 |
| T-deploy-001 | doing | 183 D1–D9 全绿待拍板（201-L1 收尾） | 不抢，见 §5 指导 |
| T-pool-002 | todo | W3#1 前置（acct-001 未 done）未过门；204 预研已落盘 | 维持 todo，见 §6 |
| T-deploy-002 | todo | 前置 deploy-001+用户拍板+sec-001 轮换 | 维持 todo，见 §5 |
| T-sec-001 | review | 只能用户执行（key 轮换） | 不代办，见 §5 |
| T-route-002/T-proto-001 | done | 199/200 已关行 | 复用结论，不重验 |

203/205/206/207 均为"领取巡检"started 行（ patrol 泛滥），本轮不代关他人行，仅关自己 208 行。

## 2. 需要指导的任务①：R-pool-003（T-pool-001 收敛出口）——我的想法

- 现状：草稿建议"搁置"，理由成立（R1 冻结不含号池、无消费者、官方账号主线优先）。
- 指导：**同意搁置，但加一条关闭条件**——用户只需回答"搁置/补全文"二选一：
  - 选"搁置"：T-pool-001 doing 维持到 W3#1 开门前，不再巡检催办；
  - 选"补全文"：按草稿 §2 四节（候选/红线/映射/优先级）补，一份报告即过门，禁再开选型行。
- 不需要任何 agent 再写：174/176/181 已按 182 口径收敛（208 复核确认关行语已落），后来者勿重开 L7。

## 3. 需要指导的任务②：T-quota-001 / T-acct-001（R2 双线）——我的想法

- quota-001（B-117/L3）：163 清单 5 步中第④例"未知/失败→不停用"是最关键的防误停门（157 负剩余教训）；指导 B 线先做该集成测试，再做归零→停→审计例。采集凭证映射表（baseURL/monitorToken/useProxy）落盘后 pool-002 直接复用，不另起配置。
- acct-001（C-128/L4）：PKCE state 的"一次消费/replay 拒绝"与跳转页 152 口径同源，建议复用 `jump_token` 的条件 UPDATE 写法；密文落库响应禁明文（R-sec-001），单测必须含 round-trip。
- 两线均属 R2，不阻塞 deploy；本轮不碰 `internal/task`、`health`、`handlers`、`op-official`（文件锁）。

## 4. 需要指导的任务③：T-group-003（review 待验收）——我的想法

- 171 实施 + 184 复验 + 195 在途验收，链条完整。后來者接力 195 即可，不另起 claim。
- 终验口径（给 195/用户）：`tsc 0 错 + eslint 本轮 0 错 + go build/vet 过 + op/router -count=1 绿` + 三语 `addChild/childBadge` 三文件在位 + 手工点一遍"子分组选择器→提交→徽标展示"。全过即转 done，无需再审代码。
- `web/` 9 文件 diff 在途未灭失（184 实证），验收前禁合其他前端分支。

## 5. 需要指导的任务④：T-deploy-001（doing 待拍板）+ T-deploy-002/T-sec-001——我的想法

- deploy-001：183 D1–D9 全绿 + 193 三单测收口，唯一缺口是 `backup.go rawCreate` P0 修复未提交。指导 L1：把该 1 行修复 + `backup_raw_test.go` 去重态打包成 `T-deploy-001` 归档 commit（密钥扫描必过），然后请用户拍板 doing→done。D7/D9 轻量版已如实标注，灰度期补。
- deploy-002：前置三件（deploy-001 done + 用户拍板 + sec-001 轮换）缺二，开工即违规。维持 todo。
- sec-001：轮换线上 key + 确认旧备份处置，只能用户做。建议把"轮换"写进 deploy-002 开工检查单第一项，未轮换不得上线（152 §5 口径延续）。

## 6. T-pool-002（todo）——维持，不开工

- 204 预研已把"形态映射/route 接入点缺口/proto 引用/余额口径差异/合规红线/开工条件"写全，开工条件（acct-001 done + quota-001 done + round 持有者设计 + 矩阵绿）一条未满足。指导：维持 todo，实施时按预研 §2 两步走（`PickGroupItem` 后调 `rankCandidates`），round 计数持有者实施设计时定。

## 7. 停止点

- 本轮零业务代码改动，未改 ledger 状态机（201 调度权），未碰 L1–L8 文件锁。
- 下轮开工条件（优先级）：① 用户拍板 deploy-001 done + R-pool-003 二选一；② 195 终验 group-003；③ B-117/C-128 落地即验收 quota/acct-001。
