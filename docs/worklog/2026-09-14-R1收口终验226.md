# R1 收口终验（L10，NM-CUR-226）

- 对应：`T-deploy-001`，L1 归档后终验；归档 commit：`380cf2c`
- 归档内容：`internal/op/backup.go` rawCreate P0 修复、`internal/op/backup_raw_test.go` 三单测、183 验收报告及 claims
- 密钥扫描：归档前 `secret scan passed (6 staged files)`；终验后 `secret scan passed (0 staged files)`

## 终验结果

| 检查 | 结果 |
|---|---|
| D1 单体构建 | `go build -tags=jsoniter` 通过 |
| D2 导入链 | 183 已在隔离库导入 12/36/259/264/340/615/132 全量基线；rawCreate P0 已归档 |
| D3–D9 | 183 报告全绿；D7/D9 轻量版限制已明确，D8 迁移单测等价通过 |
| Go 全量测试 | `go test -count=1 ./...` 全部通过（db/migrate、health、model、op、relay、handlers、middleware、router、task 等） |
| Go 静态检查 | `go vet ./...` 通过 |
| 前端类型/构建 | `pnpm exec tsc --noEmit` 通过；`pnpm run build` 3.66s 通过 |
| 项目专项检查 | auto-groups 通过；protocol-probe 通过；secret scan 通过 |

## 遗留清单

1. `T-deploy-001` ledger 仍为 doing，等待用户/调度将其拍板为 done；本报告提供过门证据。
2. D7 120s 真实 SSE、D9 30min 回放未做，已降级为 T-deploy-002 灰度阶段观察项。
3. `T-sec-001` 线上 key 轮换仍是 T-deploy-002 硬前置，只能用户执行。
4. 当前工作树仍包含其他 lane 的未归档 WIP，未在本终验 commit 中混入；禁止据此宣称工作树 clean。

## 结论

`T-deploy-001` 的 L10 终验门通过，发布候选 R1 技术验收通过；建议 ledger `doing → done`，
但线上部署仍必须等待 key 轮换与用户明确拍板。
