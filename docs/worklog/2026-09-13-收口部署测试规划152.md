# 2026-09-13 收口部署测试规划 152：冻结范围、补门清单、T-deploy-001

- 台账：`NM-CUR-152`｜路由：`auto-skills` 仅命中 `using-superpowers`
- 用户指令：尽快安排收口、进行最好的部署测试；收口测试工作现在排进日程。
- 本轮事实基线：151 审计实测全量 build/vet/test 全绿（23:43 窗口）；仅剩 A 车道两条 W1 在途 TDD 红灯
  （`TestSyncGroupItemsDepthCap` 深度剪枝疑似 `seen<=depth` 误剪、`TestValidateGroupTreeRefsOnCreate` 未注入测试库致 nil panic），归 141 自修。

## 1. 收口策略：两刀切

- **收口一（R1，现在启动）**：冻结"余额监控+归零停模型+账号读侧五类探测+跳转页+分组嵌套"为发布候选，
  完成 T-deploy-001 部署收口测试，产出**可部署构建**。
- **收口二（R2，R1 后）**：W2 余项（route-002 均衡、acct-001 官方账号处理器完整版、group-003 前端）+
  W3 主链（pool-002 号池转发、proto-001 协议矩阵）继续走原波次门，完成后再做一次 R1 全流程回归作为 R2 收口。
- 原则：R2 不许拖 R1；R1 测试期间除缺陷修复外**冻结功能合并**。

## 2. R1 冻结范围（发布候选内容）

| 能力 | 状态 | 备注 |
|---|---|---|
| 余额采集+指纹+阈值事件（health/balance*） | done | 117+147 修余单字段 bug |
| 归零停用+手动恢复+审计（quota） | done | 150 读侧审核过 |
| NA/S2 用户登录读侧（relay_account*） | done | 149 审计过 |
| 五类 Token/Key 探测（probe_token*） | done | 146 审计过（JSON 校验修复） |
| 通用 HTTP 余额适配（custom_balance） | review→随 R1 收 | 149 补 HTML 回归 |
| 一次性跳转页（jump_token 三接口） | review→随 R1 收 | 待 T-audit-004 过门 |
| 分组嵌套模型层+迁移 013 | review→随 R1 收 | 待 141 修两条红灯+演练单测 |
| 号池选型报告（pool-001） | doing | 135 唯一未关在途行 |
| 官方账号 OAuth 处理器（acct-001） | doing | **划入 R2**：模型层已在 R1 冻结库中，处理器未齐不进 R1 |

## 3. R1 收口前补门清单（全部过门才开 T-deploy-001）

1. **141/A 车道**：修两条 TDD 红灯（深度剪枝口径、测试库注入），补"迁移重跑+旧形状表演练"2 单测；
   完成 → T-group-001 done；
2. **T-audit-004**：跳转页五项审计（竞态/TTL/哈希/权限门/审计完整性）→ 过门后 T-acct-005 done；
3. **T-audit-003 续**：quota-002 runtime `-race -count=3` 复验（阻断已解除）→ T-quota-002 链路盖章；
4. **135/E 车道**：pool-001 报告落盘（唯一交付物）→ T-pool-001 done；
5. **调度（152 线）**：以上 1–4 全绿后，给 R1 打 tag 基线并启动 T-deploy-001。

## 4. T-deploy-001 部署收口测试规程（最好的部署测试）

环境：仓外备份 `数据备份(勿提交).json` 副本 → 隔离 SQLite → 双端口（管理端+转发端）→ 测后整删。

| # | 阶段 | 内容与判定 |
|---|---|---|
| D1 | 构建 | `go build -tags=jsoniter` 产单体二进制（内嵌 web）；`pnpm build` 产物嵌入核验；版本号注入 |
| D2 | 导入 | 备份导入逐行对齐 067 基线计数（12/36/259/264/340/615/132），失败即停 |
| D3 | 登录与权限 | admin 登录、API Key 鉴权、非管理员 403 抽样；跳转页三接口 create/list/go 闭环+一次性消费实测 |
| D4 | 余额链路 | 假上游：正常采集→指纹变化→阈值事件→归零→凭据级停用→审计→手动恢复→转发口径生效（ChannelGrant 不可见） |
| D5 | 探测链路 | 五类 probe 对 httptest 桩全走查：2xx JSON 健康 / 2xx HTML 判不健康 / 超时 |
| D6 | 分组与协议 | 分组 CRUD+嵌套引用校验（防环/深度）；relay 三协议非流式+流式 SSE 全 200（340 模型抽查 20） |
| D7 | 长稳与取消 | SSE 持续 ≥120s 心跳观察；客户端中途断开 → 日志正确记 canceled 不计失败 |
| D8 | 升级演练 | 用**导入前的旧形状库**跑迁移 001–013 全链，重跑一次验证幂等；再 D2 |
| D9 | 资源 | 内存/句柄 30min 观察（D7 流量回放）；结束删库删目录，报告落 worklog 附脱敏证据 |

通过门：D1–D9 全绿 → 产出《R1 发布候选验收报告》→ 用户拍板部署线上。

## 5. R1 后线上部署清单（另立项 T-deploy-002 执行，不混入测试）

1. T-sec-001 遗留：**线上渠道 key 轮换**（备份曾明文期）——用户动作，未轮换不上线；
2. Caddy SSE 修复：`/api/v1/group/events` 15s 掐流（T-log-002 P1-1 修法：reverse_proxy 超时/缓冲单独放行）；
3. 自更新代理口径：配代理或关闭自更新（T-log-002 P1-3）；
4. 灰度：低峰切换二进制，保留上一版一键回滚；观察 channels_health 一轮无异常再关单。

## 6. 台账同步

- ledger 新增 T-deploy-001（todo，前置=§3 四门）、T-deploy-002（todo，前置=T-deploy-001 过门+用户批准）；
- 137/146 线路的 W4 收口段由本篇 §4 替代细化；
- 在途 agent 不动：141 红灯、135 报告、audit-003/004 归各自车道。
