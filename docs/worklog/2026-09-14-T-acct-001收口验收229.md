# T-acct-001 官方账号授权接入完成报告（NM-CUR-228/229）

- 实现已入库：`1e644b1`、`71c12e7`
- 状态：代码验收完成，ledger `T-acct-001` → `done`
- 端点：`POST /api/v1/account/official/authorize`、`POST /api/v1/account/official/callback`、`GET /api/v1/account/official/list`、`POST /api/v1/account/official/usage/:id`
- 安全：PKCE S256、state 120 秒一次消费、账号绑定、AES-256-GCM、`OCTOPUS_OFFICIAL_KEY` 未配置拒绝授权、密文 AAD 绑定 provider、JSON 不回显密文。
- 测试：官方流程 7 例全绿；全量 `go build -tags=jsoniter ./...`、`go vet ./...`、`go test -count=1 ./...`、`pnpm --dir web run build` 全绿；编辑器 linter 无错误。
- 真实 OAuth 灰度仍需用户提供各 provider 合法 client id/redirect URI，并在隔离环境做 endpoint contract 验证；不阻塞代码收口。

## R1 冻结代码收口入库（同轮）

- `78a5bb5` quota 链（task 注册/映射/消费/热更新装配 + health 采集探测层）
- `7f51eb1` 分组嵌套模型+011 兼容+013 演练+testdb+db 注册
- `fa6f066` relay 展平选路+均衡热路径默认关+协议矩阵+日志脱敏
- `49a8332` 跳转页（模型/op/handlers/审计测试）
- `d433ff5` 分组嵌套前端（双引用/选择器/徽标/三语）
- 复验：`go build -tags=jsoniter ./...`、`go vet ./...`、`go test -count=1 ./...` 九包全绿；密钥扫描各 commit 前通过。
