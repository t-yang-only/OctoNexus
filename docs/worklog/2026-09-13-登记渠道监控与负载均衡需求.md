# 2026-09-13 登记渠道监控与负载均衡需求

- 仓库：https://github.com/t-yang-only/OctoNexus（基线 `dc0c4ab`，跟踪 `origin/bestruirui/octopus`）
- 本地：`d:\奇怪的软件\octopus`，分支 `master`
- 台账编号：`NM-CUR-018`
- 触发：`/nm-skills` `/auto-skills` 登记需求

## 目标

登记一条跨仓库缝合需求：以 **bestruirui/octopus**（Web 面板 + 渠道管理 + 协议互转 + 转发）
为前端核心，借鉴 **BerriAI/litellm**（模型路由/协议转换、下游虚拟子 key、余额/Token 计费、
调用明细），本次升级**新增上游渠道监控 + 负载均衡**，形成完整 LLM API 中转聚合平台。
只做需求登记与设计前置，**brainstorming 获批前不实施**。

## 执行过程

1. 跑 `auto_router.py` 路由：命中 `using-superpowers`（基座，第 0 步）→ `brainstorming`（匹配"新增"）。
   按基座要求先打开 `brainstorming/SKILL.md` 再动作。
2. 判定为 **Architectural**（新子系统、改选路/统计接口形状），走完整流程且受"获批前不实施"硬门约束。
3. 读现有接缝确认落点（未改任何业务代码，只读）：
   - `internal/relay/route.go`：`pickGroupItem` 单一选路点 + `RouteState`（冷却/亲和/探测）+ SSE
     `publishRouteLocked` → 负载均衡加策略维度、监控喂健康信号的挂载处。
   - `internal/model/group.go`：`GroupMode=manual|failover`、`GroupRelayConfig` → 扩策略处。
   - `internal/model/apikey.go`：已含 `MaxCost/ExpireAt/SupportedModels` → 虚拟子 key 预算基座已在。
   - 双端口（admin 3303 / relay 1234）、`op/ChannelStatsList`、`web/modules/*` 均为可复用面。
4. 产出 `docs/requirements/2026-09-13-渠道监控与负载均衡.md`：能力矩阵（已有/借鉴/新增）、
   三子系统拆分（M1 主动监控 / M2 负载均衡策略 / M3 子 key 限流与调用明细）、约束红线
   （GPL 逐文件重写、密钥不入库、reference 不入库、双端口归类）、待确认问题。
5. `nm_register.py` 登记 `NM-CUR-018`；补 `agent_word/文件清单.md` 两行；写本篇 worklog。

## 结果

- 需求已登记，设计前置就绪；**未提交、未实施**（等用户确认拆分与优先级）。
- commit：无（本批次为本地台账与需求文档，待设计获批后随实现一并提交）。

## 遗留问题

- 三子系统拆分与实施顺序未经用户确认；M1/M2/M3 各自还需一轮 brainstorming → spec。
- 负载均衡粒度（分组级 vs 渠道级 vs 按 key/模型）、主动探活成本、计费口径 三个开放问题待答。

## 下一步

1. 等用户就"先做哪个子系统 + 拆分是否接受"给方向（brainstorming 澄清问题，一次一个）。
2. 选定后对首个子系统走完整 brainstorming：提 2-3 方案 → 分节设计 → 写 `docs/superpowers/specs/` →
   获批 → `writing-plans`。
