# 2026-09-13 波次审计与收敛 150：关号池僵尸行，T-acct-005 降 review 立审计，T-group-001 重建缺陷修复

- 台账：`NM-CUR-150`｜路由：`auto-skills` 仅命中 `using-superpowers`
- 职责：波次过门审计 + 在途行收敛 + 构建链救火（最小修复，不改语义）。

## 1. T-group-001 重建（A 车道/141）——实质交付，但带 4 处编译缺陷（已修）

实证：当前树已有 `ChildGroupID *int`（model/group.go，指针+NULL 不参与唯一判重）、
`ValidateGroupItemRef` 互斥校验、`GroupItemMaxDepth=8`、`GrantRef/ChildRef` 访问器、
`validateChildGroupRefs`（存在性/自引用/防环/深度截断，按提交后最终形状校验）、
`validateGroupTreeRefs`（新建场景）、迁移 `013.go` 整表重建搬旧数据。

缺陷（并行会话中间态，本轮最小修复）：

1. `013.go` 引用未定义的 `modelGroupItemsV12` → 改 `db.Migrator().DropTable("group_items")`；
2. `013.go` 缺 `internal/model` 导入 → 补；
3. 模型 `ChannelGrantID/ChildGroupID` 已改 `*int`，`013.go`/`011.go` 仍按 `int` 赋值 → 取址修复
   （011 是旧迁移文件，随新模型形状同步修，否则全链构建红）；
4. `auto_group_test.go` 两处 `*int` 字面量不匹配 → `GrantRef()` / 取址修复。

修复后实测：`go build -tags=jsoniter ./...` EXIT 0；`go vet ./...` EXIT 0；
`go test -count=1 ./internal/...` 五包 ok（health/model/op/handlers/router）。

裁决：**T-group-001 实质过门**（模型/互斥/防环/迁移/既有测试全绿），状态 doing→review；
013 迁移幂等性（重跑跳过）与旧库演练单测仍缺——**验收条件**：A 车道补
"迁移重跑 + 旧形状表迁移"单测后转 done；补齐前 T-group-002 不得开工（其 claim 仍冻结）。

## 2. T-acct-005 跳转页（149-jt）——提前执行，产出真实，降 review + 立审计

149 在登记台已无空闲 W1 任务的情况下开了 W2 的 T-acct-005（第二次提前执行）。
产出经核实在树且非空壳：`model/jump_token.go`（哈希存储+TTL120s）、`op/jump_token.go`
（条件 UPDATE 并发至多一次消费）、`handlers/jump.go`（create/list/go 三接口，权限门）、
op 单测 7 组 + handlers 单测 4 组；API 变更已登 `API变更.md` 三行。

裁决：承认产出，**done→review**；新增 **T-audit-004**（跳转页审计：一次性消费竞态、
TTL 边界、令牌哈希不落明文、权限门、审计完整性），审计过门才转 done。
记录违规第二次：W2 未开门而动 W2 任务，按 148 规则只标 review。
（注：T-audit-003 编号已被 150 审计会话的 quota-002 复核占用，跳转页审计顺延为 T-audit-004。）

## 3. T-pool-001 改判：done 撤销，回 doing

148 判"报告不存在打回"后，ledger 仍留有 137 线的 done 行（矛盾行）。
本轮撤销 done 恢复 **doing（135 责任）**：交付物唯一口径仍是
`docs/requirements/2026-09-13-号池管理选型.md` 一份报告；文件在树并核验非空壳才过门。

## 4. 登记表僵尸行批量收敛（44 行）

068–135 的号池选型占位行与 074/130/131/132/133/134/140/142/144/145/147 等空转行，
在登记表状态列标注"已完成（NM-CUR-150 收敛关行：号池选型统一由 T-pool-001 报告收口/空转行无产出关行）"。
台账"只追加不删除"原则不破坏：改状态列备注，不删行。

## 5. 当前波次全景（本轮后）

| 波次 | 状态 |
|---|---|
| W0 | group-001 review（差迁移演练单测）；pool-001 doing（报告未交）；log-002 done ✅ |
| W1 | acct-001 doing（C 线 OAuth 处理器）；quota-001 doing（B 线调度接线）；acct-003 done；acct-004 review；test-001 done |
| W2 | acct-005 review（差 T-audit-004）；acct-004 手动订阅部分待用户拍板存储位置 |
| W3 | pool-002/route-002/proto-001 全部冻结待前置 |

## 6. 台账同步

- ledger：T-group-001 doing→review（附验收条件）、T-acct-005 done→review、T-pool-001 done→doing、
  新增 T-audit-004（跳转页审计，quota-002 复核占用 003 号顺延）；
- 登记表：44 行批量收敛 + 本行 150；日志/清单追加；
- 业务代码：本轮仅修 013/011/auto_group_test 三文件编译缺陷（最小修复，归属 141 重建任务名下）。
