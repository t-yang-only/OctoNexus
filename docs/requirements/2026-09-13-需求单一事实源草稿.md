# 需求单一事实源：new-api 看板 + 渠道监控负载均衡 + 对标研究（NM-CUR-061 草稿，待用户拍板）

- 台账编号：`NM-CUR-061`（060 任务"需求三通道合并"的补完产出；060 行由本轮关行）
- 状态：**草稿**——合并关系已梳理、缺陷任务已起草转 ledger 候选，均**待用户拍板**后才改状态；
  在此之前 `需求登记.md` 与 requirements 四篇**一字不改**（只追加不删除规范）。
- 三通道原文：
  1. `docs/requirements/2026-09-13-渠道监控与负载均衡.md`（NM-CUR-018 登记，文头编号误写 017；
     拆 M1 主动监控 / M2 负载均衡 / M3 子 key+限流+明细）；
  2. `docs/requirements/2026-09-13-new-api模型调用分析看板.md`（NM-CUR-020/021 登记；
     埋点→内存桶→定时入库→`GET /api/v1/stats/perf/summary`→home"模型性能"标签页）；
  3. `docs/agents/需求登记.md`（R-alert-001/R-sec-001/R-quota-001/R-route-001/R-limit-001/R-probe-001 六条；
     源自 T-research-001~004 对标研究）。
- 另有 `docs/requirements/2026-09-13-Ubuntu部署约束.md` 与 `2026-09-13-简洁监控约束.md`
  为**约束类**（部署目标/外观约束），不参与功能合并，仅被引用。

## 1. 合并关系总表（待用户确认后成为事实源）

| 统一需求 | 组成 | 落点 |
|---|---|---|
| U-mon-001 上游渠道主动监控 | 018-M1 ＋ R-probe-001 ＋ R-quota-001（余额/额度/公告归此类告警源） | `internal/task` 定时探活 → op 缓存 → admin 面板 + SSE |
| U-route-001 负载均衡与路由策略 | 018-M2 ＋ R-route-001（lowest_cost 先行，lowest_latency 后续） | `relay/route.go:pickGroupItem` 策略枚举扩 `GroupMode` |
| U-key-001 子 key 预算/限流/明细 | 018-M3 ＋ R-limit-001（RPM/TPM 内存滑动窗口） | `model/apikey.go` 字段扩展 + 请求级明细 |
| U-perf-001 模型调用分析看板 | new-api 看板全文 ＋ 018-M1 被动样本 ＋ U-route-001 延迟数据复用 | 埋点 `relay/handler.go` 终态 → `GET /api/v1/stats/perf/summary` → home 区块 |
| U-alert-001 告警通知 | R-alert-001（含 M1/探测/余额/价格变化事件源，webhook+SMTP+发送前真实测试） | 独立通知模块，消费各统一需求事件 |
| U-sec-001 凭据静态加密 | R-sec-001（AES-GCM 入库 + 导出脱敏，关联 T-sec-001 密钥轮换） | `op` 读写层加解密 |

约束引用：U-mon/U-perf 须遵守简洁监控约束（home 单块、不新增导航页、UsageHourly 小时明细、
180 天滚动删）；交付物须遵守 Ubuntu 部署约束（只出 linux 单二进制）。

## 2. 缺陷任务立项草稿（待用户拍板后进 ledger；转自 022 报告 + 036 终裁）

| 候选 ID | 内容 | 来源 | 建议优先级 |
|---|---|---|---|
| R-price-001 | 自动价格同步落库回填（定时任务末尾接 LLMBatchSave） | C-1（Critical） | P0 |
| R-stats-002 | 跨天统计落旧日异步化（翻天落库移出 relay 全局锁） | C-2（Critical） | P0 |
| R-relay-001 | 整请求 deadline（默认 300s）+ ReadHeaderTimeout（双端口） | H-1/H-2 | P1 |
| R-sec-002 | 登录限流退避 + JWT 密钥派生改造 + CORS `*`+凭据组合禁用 | H-3/M-8/H-4 | P1 |
| R-stats-001 | APIKeyDelete 映射修复 + 失败计数口径（attempt_failed 列或 UI 注明）+ 定时 flush 单飞 | M-1/M-3/M-4 | P2 |
| R-backup-001 | 备份导入清 relay 内存路由 + 导入体量上限 413 | M-6/M-7 | P2 |
| R-models-001 | `/v1/models` 按路由端点定形状（或双形状字段） | M-5 | P2 |

## 3. 与 022/bug 报告的关系

本草稿不改动 `docs/worklog/2026-09-13-潜在bug排查.md` 原文；
口径以 036 终裁为准（C-1/C-2 成立 Critical，M-1 维持 Medium）。
立项拍板后：需求登记.md 六条状态 `待确认`→`已立项`（保留历史行备注），
候选 ID 进 `ledger.md` 为正式任务（认领 + claims 加锁）。
