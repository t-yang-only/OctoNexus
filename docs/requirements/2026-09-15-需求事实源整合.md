# 需求事实源（整合版）：全部零碎需求的现状总账（NM-CUR-230，2026-09-15）

> 本文件是 `需求登记.md` 六条 R-*、`2026-09-13-渠道监控与负载均衡.md`（M1/M2/M3）、
> `2026-09-13-new-api模型调用分析看板.md`、`2026-09-13-简洁监控约束.md`（NM-CUR-025 裁决）、
> `docs/research/2026-09-13-参考仓库对标研究.md`（G1–G6）以及
> `2026-09-13-需求单一事实源草稿.md`（NM-CUR-061 U-* 合并关系）的**单一事实源定稿**。
> 原文一律保留不改（只追加不删除规范），此处以 ✅/🔶/⬜/⏸ 标注每项需求的实际落地状态；
> 用户此后问"某需求做没做"，以本表为准。
>
> 状态图例：✅ 已交付入库 ｜ 🔶 部分交付（余项见说明）｜ ⬜ 已立项未开工 ｜ ⏸ 搁置/待用户拍板

## 1. U-* 统一需求落地状态（接续 NM-CUR-061 草稿合并关系）

| 统一需求 | 组成通道 | 状态 | 交付证据（代码/提交/报告） |
|---|---|---|---|
| **U-mon-001 上游渠道主动监控/余额采集** | 018-M1 + R-probe-001 + R-quota-001 | ✅ 余额采集与主动探活均已交付 | T-quota-001 ✅（task/quota_scan.go 5min 周期、QuotaScanTargets 映射、指纹去重、失败不停用、归零恒判停用）；T-quota-002 ✅（凭据级 QuotaZeroStop+手动恢复+QuotaAction 审计）；**主动探活 T-probe-001 ✅**（relay/probe.go 对冷却成员发 max_tokens=1 的真实请求，恢复即提前解除冷却并推 route_probe_recovered 事件；默认关闭、周期可配、探测失败不改冷却） |
| **U-route-001 负载均衡与路由策略** | 018-M2 + R-route-001 | ✅ 加权轮询与四种策略族均已交付 | T-route-002 ✅（relay/balance.go rankCandidates SWRR + route_balance_enabled 默认关 flag + 热路径 pickGroupItemHot + 装配层注入 + 设置页开关，112306e）；分组嵌套展平选路 T-group-002 ✅（fa6f066）；lowest_cost（1c2c2c7）/ quality_first（1f3b2b7）/ lowest_latency（588f501）/ least_busy（89d97fc）✅；余 lowest_tpm_rpm 需 Key 级限流余量数据 |
| **U-key-001 子 key 预算/限流/明细** | 018-M3 + R-limit-001 | 🔶 预算+限流已交付；请求级明细导出未做 | MaxCost 预算 ✅（原有）；RPM/TPM ✅（model/apikey.go 字段 + op/ratelimit.go 内存滑窗 + 转发面 429+Retry-After + 设置页表单，112306e）；请求级明细（输入/输出 token/费用/耗时/命中成员导出）🔶 RelayLog 已有明细数据（log.go），报表化导出 ⬜ |
| **U-perf-001 模型调用分析看板** | new-api 看板 + 018-M1 被动样本 + 025 裁决 | ✅ 按 025 简洁裁决交付 | home/monitor.tsx 单块监控（五指标卡+健康徽标行+分模型明细表+消耗分布）；UsageHourly 小时桶（model/usage.go + op/usage.go，180 天滚动删，搭车 StatsSaveDBTask 落库）+ `GET /api/v1/stats/usage?range=24h\|7d\|30d`（112306e）；24h/7d/30d 前端切换；不做三 Tab 看板/不新增导航页（025 红线维持） |
| **U-alert-001 告警通知** | R-alert-001（G1） | ✅ 渠道层已交付（5 渠道 + 真实测试）；规则引擎仍待立项 | internal/notify（source.go 设置源注入；channels.go 五渠道 fan-out：webhook 原样 / feishu msg_type+code / dingtalk·wecom msgtype+errcode / smtp RFC2047+base64、465 隐式 TLS）；余额告警 + 归零停用 + 探活恢复三类事件统一走 notify.Send；GET /api/v1/setting/notify/channels 与 POST /api/v1/setting/notify/test（发送前真实测试，逐渠道回报，业务错误码判失败）；前端设置页「通知渠道」卡片（三语）；SMTP 密码只从 OCTOPUS_SMTP_PASSWORD 读；verify = 单测 8/8 + 活体 14/14。规则引擎（NotifyRule/NotifyEvent 去重收敛）与公告/价格事件源 ⬜ 待立项 |
| **U-sec-001 凭据静态加密** | R-sec-001（G3） | 🔶 官方账号已加密；渠道 key 明文未迁移 | 官方账号 AES-256-GCM（op/official_account.go，OCTOPUS_OFFICIAL_KEY，AAD 绑服务商，json:"-" 不回显）✅（1e644b1）；跳转令牌 SHA-256 哈希落库 ✅；备份脱敏/密钥外移 T-sec-001 review（线上 key 轮换待用户）；渠道 ChannelKey 明文改密文 ⬜ 未立项（涉及存量数据迁移） |

## 2. R-* 需求登记条目状态收口（原文见 需求登记.md，此处定状态）

| 需求 ID | 一句话 | 状态 | 说明 |
|---|---|---|---|
| R-alert-001 | 告警通知 webhook/SMTP | ✅ 渠道层 | webhook/飞书/钉钉/企微/SMTP 五渠道已交付并接上三类事件（余额告警、归零停用、探活恢复），带发送前真实测试；规则引擎与公告/价格变化事件源未做，待立项（T-alert-001 done） |
| R-sec-001 | 凭据静态加密 | 🔶 | 官方账号/跳转令牌已按口径交付；渠道 key 存量迁移待立项；T-sec-001 线上 key 轮换仍卡用户 |
| R-quota-001 | 上游余额/额度监控 | ✅ | T-quota-001 全链交付（163 清单五步），done |
| R-quota-002 | 余额归零停模型 | ✅ | T-quota-002 交付（凭据级停用+审计+手动恢复），done |
| R-route-001 | 路由策略扩展 | ✅ | 加权轮询（T-route-002）+ lowest_cost（1c2c2c7）+ quality_first（1f3b2b7）+ lowest_latency（588f501）+ least_busy（89d97fc）全交付；余 lowest_tpm_rpm（需 Key 级限流余量数据） |
| R-limit-001 | Key 级限流 | ✅ | RPM/TPM 内存滑窗已交付（112306e），转发面 429+Retry-After |
| R-probe-001 | 周期健康探测 | ✅ | T-probe-001 交付：task/probe.go 每轮按 route_probe_interval_seconds（默认 300s，0 停用）探冷却成员，relay/probe.go 走生产同一套地址拼接/凭据注入/渠道参数覆盖发 max_tokens=1 的最小真实请求，成功即解除冷却（不等冷却到期）并推 route_probe_recovered 事件；route_probe_enabled 默认关闭（探测按量计费）；探测失败只记日志、不改冷却、不重试。活体 15/15（含"上游恢复后 40s 内提前解除 120s 冷却且流量回切"与"探测失败冷却截止时间不变"） |
| R-group-001 | 分组嵌套 | ✅ | T-group-001/002/003 全 done（模型/迁移/展平选路/前端三语） |
| R-acct-001 | 官方账号 OAuth 接入 | ✅ | T-acct-001 done（1e644b1/71c12e7）+ 前端账号页（72eaaf8）；真实 provider 凭据待用户配置 |
| R-acct-002 | 中转站用户登录 | ✅ | T-acct-002 done（129/149） |
| R-acct-003 | Token/Key 健康监控 | ✅ | T-acct-003 done（139/146） |
| R-acct-004 | 手动与通用余额 | ✅ | T-acct-004 done（145/149/157）；手动订阅独立表存储属 W2（指导口径） |
| R-acct-005 | 手动登录跳转页 | ✅ | T-acct-005 done + 前端账号页"登录跳转"标签（49a8332/72eaaf8） |
| R-pool-002 | 官方账号号池转发 | ✅ | T-pool-002 done（凭据侧物化 + pool/sync + pool/list，8217ccd）；T-pool-003 补统一号池视图 members 明细与前端号池标签页（7683353 之后的号池合并轮） |
| R-pool-003 | 仓库号池选型报告 | ⏸ | 草稿建议搁置（2026-09-13-号池管理选型.md），待用户拍板"补全文/搁置" |
| R-login-001 | （并入 R-acct-005） | ✅ | 同 R-acct-005 |
| R-test-001 | 本地备份隔离测试 | ✅ | T-test-001 done（136 导入基线+三协议转发验证） |
| R-log-002 | 本地日志错误分析 | ✅ | T-log-002 done（143，拆出四修复任务已收口） |

## 3. 约束类文档（不参与合并，持续有效）

| 约束 | 口径 | 效力 |
|---|---|---|
| 简洁监控约束（025） | home 单块、不新增导航页、复用现有图表栈、180 天滚动删 | ✅ 本轮交付全程遵守；后续监控类需求继续受此约束 |
| Ubuntu 部署约束 | 只出 linux 单二进制 | ✅ T-deploy-001 D1 已按此验收 |
| 数据备份*.json 不入库、reference/ 不入库 | 密钥红线 | ✅ 持续有效（secret scan 每次提交前过） |
| 双端口 | 管理 admin / 转发 relay 分离 | ✅ 新接口全部按此归类（stats/usage、account/official、account/jump 走 admin） |

## 4. 尚未立项的剩余项（供用户挑选进入 ledger）

1. ~~**主动探活**（R-probe-001）~~ **已交付**（T-probe-001；relay/probe.go + task/probe.go + 两个设置开关 + 15 例活体审计）。
2. ~~lowest_cost / lowest_latency 路由策略~~ **已交付**（1c2c2c7 / 588f501，另加 quality_first 与 least_busy）。
3. **渠道凭据静态加密迁移**（R-sec-001 余项）：ChannelKey 明文→AES-GCM，需存量迁移方案，建议与 T-deploy-002 上线一起做。
4. ~~**SMTP/多渠道通知**（R-alert-001 余项）~~ **渠道层已交付**（T-alert-001：五渠道 + 真实测试；余规则引擎与公告/价格事件源待立项）。
5. **请求级明细导出**（U-key-001 余项）：RelayLog 数据已齐，缺 CSV/面板导出。
6. **手动订阅独立表**（R-acct-004 指导口径）：W2 待用户终裁存储位置。
7. ~~R-pool-002 官方账号号池转发~~ **已交付**（8217ccd 凭据侧物化；统一号池视图本轮补齐）。
8. **R-pool-003 仓库号池报告**：⏸ 等用户拍板补全文/搁置。

> 本地 API 测试入口：`python scripts/api-tests/run_all.py`（16 套件一键跑，NM-DS-009 搭建 / NM-DS-011 增补 probe 套件）。

> 本文件由 NM-CUR-230 整合落盘；后续新需求仍先进 `需求登记.md` 追加，立项后回写本表状态列。
