# 2026-09-13 号池管理选型（R-pool-003 草稿，NM-CUR-205，201-L7 收敛）

- 状态：**草稿待用户确认**——先回答"要不要补这份报告"，再决定是否展开全文。
- 背景：T-pool-001 原残门（135 认领的"仓库选型报告"）经 NM-CUR-182 核验：需求登记台无"仓库号池设计"需求
  （R-pool-002 是官方账号转发需求），`docs/requirements` 下无此文件（唯一引用是 086 行自证路径），
  135/174/176/181 均零交付。182 终裁残门作废、改立 R-pool-003 待确认；本草稿即收敛出口。

## 1. 要不要补报告：建议"暂不补全文，理由如下"

- octopus 已有官方账号主线（R-acct-001 OAuth 接入 doing + R-pool-002 官方号池转发 todo W3#1），
  与"仓库账号池"（第三方开源池项目纳管）是两条路线：前者合规（官方授权+密文落库），后者涉 ToS/封号/逆向风险。
- R1 冻结不含号池转发（152 §2）；W3#1 前置（W1#3+W2#2+proto 矩阵）未过门，报告即使落盘也无人消费。
- 结论：**R-pool-003 建议置"搁置"，待 W3#1 开门前再按 §2 大纲展开**；用户若坚持要全文，按 §2 四节补即可过门。

## 2. 若要全文的大纲（四节口径，与 170/175 指导一致，一份报告即过门）

1. **候选清单**：ChatGPT/Gemini 官方账号池开源候选各 2–3 个，字段统一
   （协议/登录方式/额度可见性/封号风险/维护活跃度）；只收录"官方授权接入"合规项，
   验证码绕过/逆向/cookie 盗用一票否决并写明红线。
2. **合规红线**：与 T-acct-001 同口径——仅走官方 OAuth/扫码授权；
   凭据密文落库（AccessCipher/RefreshCipher），日志/输出绝不出现 Key（R-sec-001）。
3. **并入映射**：候选账号→octopus 渠道模型（Channel/ChannelKey/ChannelModel/ChannelGrant 四件套）
   + 监控凭证映射（baseURL/monitorToken/useProxy，复用 T-quota-001 接线清单：
   task 5min 周期→阈值事件→QuotaZeroStop→审计）+ 跳转页（T-acct-005）人工兜底。
4. **推荐优先级**：OpenAI/Gemini 先行、Claude 后续（与 T-pool-002 W3#1 一致）；
   一句话结论：先接 OpenAI（官方 OAuth 最成熟、封号风险最低），Gemini 次之，Claude 排后。

## 3. 收敛动作（本草稿落盘即完成）

- T-pool-001 doing 维持（等用户对 R-pool-003 拍板"补全文/搁置"后再转 done/关闭）；
  174/176/181 三行由调度按 182 口径关行，本轮不代关他人行。
- 与部署测试脱钩重申：不阻塞 T-deploy-001（182 终裁有效）。
