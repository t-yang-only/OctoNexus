# 任务登记台账（ledger）

> 认领前先看本表；认领时同步新建 `claims/YYYY-MM-DD-<agent>-<task>.md` 加锁。

| 任务 ID | 标题 | 模块 | 状态 | 认领 Agent | 登记日期 | 备注 |
|---------|------|------|------|-----------|---------|------|
| T-docs-001 | Python 原型隔离（已完成，待验收归档） | docs | done | cursor-local | 2026-09-12 | NM-CUR-021：src/+tests/+pyproject.toml 移入 reference/python-prototype/（gitignore 不入库），原地 .pytest_cache/egg-info 已清理；遗留：docs/python-prototype-README.backup.md 与本任务关系待确认 |
| T-sec-001 | `数据备份.json` 移出工作区（密钥轮换待用户执行） | security | review | cursor-local | 2026-09-12 | NM-CUR-021：文件已移至仓库外 `../octopus-本地数据/`，验证从未入 git 历史/索引；gitignore 早已覆盖 `数据备份*.json`；待用户：轮换线上渠道 key 并确认旧备份处置 |
| T-env-001 | 安装 Go 1.26 + Node18 + pnpm 并跑通构建 | env | done | cursor-local | 2026-09-12 | NM-CUR-021 验证：本机 Go 1.27（C:/Program Files/Go）+ Node v25.2.1/pnpm 12.3.4；`go build -tags=jsoniter` 通过、`go vet ./...` 通过、`go test ./...` 全过(op/router/handlers)；前端 `pnpm install --prefer-offline`（npmmirror）成功 + `pnpm run build` 通过（tsc+vite，产物 static/out），含嵌入二进制构建验证 |
| T-agents-001 | 建立多 agent 协同登记台账 | agents | done | cursor-local | 2026-09-12 | NM-CUR-023 验收通过：README/registry/ledger/worklog 四件套齐备且后续 8 个任务真实流转，worklog 2026-09-13-全面收尾与审核报告 §5 |
| T-research-001 | 参考仓库对标研究第一轮（octopus vs api-monitor vs litellm） | research | done | cursor-local | 2026-09-13 | 报告 docs/research/2026-09-13-参考仓库对标研究.md；差距 G1-G6 已转登 需求登记.md |
| T-research-002 | api-monitor connectors 精读：余额/公告接口与指纹 diff 移植清单 | research | done | cursor-local | 2026-09-13 | 报告 docs/research/2026-09-13-T-research-002-api-monitor移植清单.md（P1–P8）；claim 见 claims/2026-09-13-cursor-local-T-research-002.md |
| T-research-003 | litellm router_strategy/cooldown 精读：pickGroupItem 扩展设计稿 | research | done | cursor-local | 2026-09-13 | 报告 docs/research/2026-09-13-T-research-003-路由策略设计稿.md；claim 见 claims/2026-09-13-cursor-local-T-research-003.md |
| T-research-004 | 告警通知设计稿：事件源盘点+渠道抽象+规则 schema | research | done | cursor-local | 2026-09-13 | 报告 docs/research/2026-09-13-T-research-004-告警通知设计稿.md；claim 见 claims/2026-09-13-cursor-local-T-research-004.md |
| T-pool-001 | 号池管理选型：ChatGPT/Gemini 账号池开源候选+并入 octopus 方案（供用户选择） | pool | doing | cursor-local | 2026-09-13 | 150 改判：撤销 137 线 done；回 doing 由 135 责任交付，唯一口径 docs/requirements/2026-09-13-号池管理选型.md 一份报告。**182 终裁：deploy-001 残门作废——需求台无仓库选型需求（R-pool-002 为官方账号转发），文件自证路径，135+174/176/181 均零交付；报告需求改立 R-pool-003 待用户确认；本行维持 doing 作收敛出口，与部署测试脱钩；NM-CUR-201 调度 L7：交付一页 R-pool-003 草稿关 174/176/181 三行，纯文档禁碰代码** |
| T-quota-001 | 余额/额度采集与阈值告警：上游余额接口定时采集落库，低于阈值告警 | quota | done | cursor-local | 2026-09-13 | NM-CUR-217 L3 交付：163 五步全闭环（task 注册 quota_scan_interval 可配缺省5min/凭证映射 op.QuotaScanTargets 过渡口径=最小启用凭据/消费链 task/quota_scan.go 指纹去重+失败绝不改状态+归零恒判不看告警阈值/集成测试3例走 SetDBForTest+InitCache+httptest 真链路/⑤超时疑点结论=136 定性成立非网关缺陷，加固建议(协议预过滤或轮次上限)指针转 relay 车道 U-route 后续)；架构记录 op→health 成环故消费落 task；task -count=3 绿+model/op/health 绿+vet 绿；全量中 relay 红例属 L5 在途(route_balance_test 15:50 新落)非本车范围；claim 见 2026-09-14-cursor-local-T-quota-001-L3；**NM-CUR-229 验收：L10 终验 226 九包含 task 绿+本次装配层热更新补全（quota_scan_interval 保存即时 task.Update 生效）；relay 红例系 L5 测试时序已被 224 修复；转 done** |
| T-quota-002 | 余额归零自动停模型：归零停用对应模型授权/成员，手动恢复+审计 | quota | done | cursor-local | 2026-09-13 | NM-CUR-124 实施：凭据级停用幂等+手动恢复+QuotaAction审计+2单测；NM-CUR-150 读侧审核过（粒度/幂等/拒绝/审计/分页/缓存联动无 bug，quotaRefreshChannelKeys 留 B 车道接线）；runtime -race 复验被 A 车道 141 WIP 编译错阻断待补；报告 docs/worklog/2026-09-13-T-quota-002复核审计150.md |
| T-group-001 | 分组嵌套数据模型：GroupItem 支持引用子分组，防循环+级联语义+迁移 | group | done | cursor-local | 2026-09-13 | 150 实质过门：ChildGroupID/互斥/防环/013 迁移在树；156 指导修复三红例；**NM-CUR-158 指导收口：最后一项前置"迁移重跑+旧形状表演练单测"补齐（migrate/013_test.go 4 例：旧形状重建搬运/重跑幂等/新形状重入/无表与 nil 防御，-count=2 过，全量七包 ok）**；T-group-002 门开；报告 docs/worklog/2026-09-14-指导收口与演练158.md |
| T-group-002 | 分组嵌套选路递归展开：relay 按树形递归解析，亲和/冷却/上限语义 | group | done | cursor-local | 2026-09-13 | **182 核验：163 冻结令误伤——认领行 162/164 实为实施（非空转），165 已有效收尾转 done；冻结令中涉 group-002 部分撤销**；Flatten+WithItems+handler 接线，四单测+relay 5 例+165 追 TestPickGroupItemManualChildRefZero，全量 vet/build/test -count=1/3 绿；claim 162+165 |
| T-group-003 | 分组嵌套前端：树形展示/子分组选择器+三语 i18n | group | done | cursor-local | 2026-09-13 | F 车道：W2#1。NM-CUR-171 实施（claim 见 claims/2026-09-14-cursor-local-T-group-003.md）：类型对齐+子分组选择器+展示徽标+双引用提交+三语 i18n；**NM-CUR-184 独立复验：tsc 0 错+eslint 本轮 6 文件 0 错+go build/vet 过+op/router -count=1 绿，三语 addChild/childBadge 等键三文件在位，web/ 9 文件 diff 完整在途未灭失；后端零改动。转 review 待调度/用户验收后 done；NM-CUR-201 调度：L2 接力 195 做终验转 done（tsc/eslint/build/vet），禁动 go 文件；120 旧占位留痕；**NM-CUR-214 指导：代码层终验已完成三层实测（tsc 0 错+pnpm build 3.91s+API 闭环：create/update/items 双引用、child_group_name 回名、空子分组 available=false 均实证）；剩余仅浏览器视觉验收——用户可直接开 http://127.0.0.1:13399/（隔离预览实例 admin/admin）看编辑器即视为验收，L0 备援补截图链路**；**NM-CUR-221 收口：用户"尽快收口"授权，代码层三层终验+API 闭环齐备，review→done；浏览器视觉走查降级 R2 观察项（预览实例 down，L9 重启后可随时补看，不拦门）** |
| T-acct-001 | 官方账号授权接入：OpenAI/Gemini/Claude 官方授权扫码+套餐/健康/5H7D窗口读取 | account | done | cursor-local | 2026-09-13 | C 车道/128：W1 放行（148）。163 下发实现清单：①authorize/callback/读取三接口+路由挂载；②PKCE state（签发/过期/一次消费/replay 拒绝）；③official_account AutoMigrate 注册；④密文落库（AccessCipher/RefreshCipher 落库加密+读回解密，响应 JSON 不带明文）；⑤单测：state 过期、重复 code、密文 round-trip。合法官方 OAuth，禁验证码绕过/逆向；属 R2，不阻塞 R1 D1-D9；NM-CUR-201 调度 L4 并行实施；**NM-CUR-228 亲自实施完成（commit 1e644b1+71c12e7）：①三接口+/list 挂载 /api/v1/account/official（Admin+Auth 门，跳转子路由先例）②PKCE S256+state 内存表 TTL120s 条件消费（过期/未知/重放三拒绝，账号绑定校验）③AutoMigrate db.go 注册 ④AES-256-GCM 密文落库（OCTOPUS_OFFICIAL_KEY 未配置即拒绝 authorize，AAD 绑服务商防挪用，json:"-" 双保险）⑤单测 7 例全过（密钥缺失/过期/重放消费/round-trip+错AAD/全链路 callback+usage 回填/重复 code），op 包 11 例连 backup 层全绿；换码/读侧可注入桩解耦真实 OAuth；全量 build/vet/test 九包绿+tsc/vite 绿。转 review 待调度验收转 done**；NM-CUR-229 复核：该实现已随 1e644b1/71c12e7 入库，op 官方流程 7 例+全量九包绿；仅真实 OAuth provider credentials/endpoint contract 需用户提供后做灰度，不阻塞代码验收；建议 review→done |
| T-acct-002 | 中转站用户登录：NA 用户登录+自查；S2 用户登录+Keys/订阅/quotas读取 | account | done | cursor-local | 2026-09-13 | NM-CUR-129 完成；NM-CUR-149 审核过：修复 sub2APIGet 2xx HTML/空体透传误判（s2JSONBody 校验+quotas 回归单测），全链路绿；报告见 worklog/2026-09-13-T-acct-002-004复核审计149.md；落库/UI/定时属后续任务 |
| T-acct-003 | Token/Key 健康监控：NA-Token/S2-Token/Admin-costs/OpenAI-Key/Anthropic-Key 五类探测 | account | done | cursor-local | 2026-09-13 | NM-CUR-139 完成；NM-CUR-146 审核过：修复 2xx HTML/空体误判健康 bug（补 JSON 校验+回归单测），全链路绿；报告见 worklog/2026-09-13-T-acct-003复核审计146.md；Admin-costs 权限门/定时/落库属后续 |
| T-acct-004 | 手动订阅+通用 HTTP 余额：无接口套餐手工录入；自定义 JSON 余额接口适配 | account | done | cursor-local | 2026-09-13 | 145 适配核心+149 复核+157(T-audit-005) 修负剩余误触发 bug 审核过；**NM-CUR-158 指导验收：三轮审计证据齐（校验/超时/1MiB/path 宽容/互推导/负剩余夹零），且 R1 冻结含此交付，转 done**；指导口径：手动订阅建议独立表存储（非渠道凭据：无上游密钥、生命周期与轮换语义不同、避免备份转储混入凭据面），W2 开工用户终裁；手动订阅 UI/落库仍属 W2 |
| T-acct-005 | 手动登录跳转页：NA/S2 生成一次性跳转页防验证码拦截 | account | done | cursor-local | 2026-09-13 | 150 裁决：149-jt 第二次提前执行（W2 未开门），产出真实（jump_token 模型/op/handlers+11 单测+API 变更三行）承认但 done→review；转 done 前置：T-audit-003 审计过门（一次性消费竞态/TTL 边界/哈希不落明文/权限门/审计完整性）；管理 UI 按钮属后续；NM-CUR-153 审计过门（T-audit-004 全过）转 done；管理 UI 按钮仍属后续 |
| T-pool-002 | 官方账号号池转发：Google/ChatGPT 官方账号当渠道做轮询转发 | pool | done | dsh-local | 2026-09-13 | 前置三门已全部过门（W1#3 T-acct-001 / W2#2 T-route-002 / T-proto-001 均 done），201-L8 的「预研 only」是并行调度期锁、已随门开解除。**NM-DS-001 实施（凭据侧物化）：新增 internal/op/official_pool.go——每服务商一个号池渠道 `官方账号池-<provider>`（已存在不改配置）、一账号一条 ChannelKey（Key=解密后 access token）、非 active 账号只停用凭据不删行（重授权后同凭据复位并换新 token）、距到期 5 分钟内且带刷新凭据时自动换新 token 落库、状态快照；单账号失败只记 Notes 且绝不覆盖既有凭据；不新增模型表——模型与协议位仍由既有「拉取模型+三协议实测」流程给出真实结论**。新增 `official_pool_test.go` 7 例、两接口（`POST /api/v1/account/official/pool/sync`、`GET /api/v1/account/official/pool/list`，Admin+Auth）；go build/vet EXIT 0 + 九包 test 全绿 + 隔离实例 API 冒烟（list/sync/带 provider/无 cookie 401）通过；报告 docs/worklog/2026-09-15-接手与台账收口.md。**待用户侧：真实 OAuth provider 凭据与官方端点 Header 需线上配置后灰度（与 T-acct-001 同口径）** |
| T-route-002 | 均衡请求：号池内轮询/加权/最低延迟，老 068/070 选型链收敛到此 | route | done | cursor-local | 2026-09-13 | NM-CUR-186 完成：号池内加权轮询第一版（relay/balance.go 纯函数候选定序）+三单测；NM-CUR-199 复验补齐三单测维持 done；NM-CUR-197 复核修取模 bug 标审核过；NM-CUR-201 调度 L5 热路径落地；NM-CUR-213 完成：新增 route_balance_enabled 默认关闭 flag，handler 接线，RouteState 顶层轮次，flag 开关/轮转/冷却单测，relay vet/test 与 jsoniter build 绿；**NM-CUR-230 收口：补齐 op.SettingSetStringForTest 缺口，relay 全包/全量 test + vet + jsoniter build 绿，flag 默认关红线维持，转 done** |
| T-proto-001 | 连接协议转化：官方账号↔OpenAI/Anthropic/Gemini 协议互转验证 | relay | done | cursor-local | 2026-09-13 | NM-CUR-200 完成：五类矩阵全绿（relay/proto_matrix_test.go 6 例），只读验证零业务改动；relay -count=3绿+build/vet过；报告 docs/worklog/2026-09-14-T-proto-001协议矩阵200.md。**NM-CUR-201 调度 L6：矩阵缺口复查+观察项闭环（transformer 终态不对称登记项），只读优先、确认缺口才补 httptest，不碰 handler/route/balance（L5 锁）** |
| T-test-001 | isolated backup acceptance: import local backup to separate DB and verify auth/channels/groups/protocols | test | done | cursor-local | 2026-09-13 | NM-CUR-136 passed: import rows match 067 baseline, group/proto counts match, relay 3-protocol+streaming 200 on isolated ports; Auto-Model responses-timeout suspicion filed; temp dir wiped |
| T-log-002 | 本地日志错误分析：读 7 份日志包定位历史错误并修复 | log | done | cursor-local | 2026-09-13 | W0#3 过门/NM-CUR-143 完成：P0 无崩溃；P1 代理缺失致模型下载失败+探针把 HTTP400 计入假死降级+Caddy 251 条取消噪音；P2 上游慢+历史失败率缺时间窗；报告 docs/worklog/2026-09-13-T-log-002日志分析143.md；修复拆 T-probe-4xx-001/T-probe-cancel-001/T-model-refresh-001/T-health-window-001 待立项 |
| T-audit-001 | T-acct-003 健康探测复核：鉴权头/超时/上限/权限门/测试覆盖审计 | audit | done | cursor-local | 2026-09-13 | 146 完成：发现 2xx HTML/空体误判健康 1 bug，修复为 1MiB 限读后 JSON 可解析校验+回归测试；build/vet/test 全绿，T-acct-003 审计通过维持 done；报告 docs/worklog/2026-09-13-T-acct-003复核审计146.md |
| T-audit-002 | T-acct-002/004 读侧客户端复核（登录/自定义余额） | audit | done | cursor-local | 2026-09-13 | 149 完成：T-acct-002 修 sub2APIGet 2xx 透传误判（s2JSONBody+回归）；T-acct-004 无 bug 补 HTML 回归；全链路绿；报告 docs/worklog/2026-09-13-T-acct-002-004复核审计149.md |
| T-audit-003 | T-quota-002 归零停用链路复核（读侧+runtime） | audit | done | cursor-local | 2026-09-13 | NM-CUR-150 读侧审核过；152/157 runtime 复验 -count=3 全绿（含 jump 8 路并发恰 1 成功）；**-race 为 Windows TSan 环境阻塞（影子内存保留 error 87），NM-CUR-158 指导裁决：竞态语义已由 DB 层原子条件 UPDATE + 决定性 -count 重跑覆盖，-race 作已登记环境限制结案不再挂账**；报告 docs/worklog/2026-09-13-T-quota-002复核审计150.md |
| T-audit-004 | T-acct-005 跳转页审计：一次性消费竞态/TTL 边界/哈希/权限门/审计 | audit | done | cursor-local | 2026-09-13 | NM-CUR-153 过门：五口径全过（条件UPDATE竞态至多一次/120s严格>边界/哈希不落明文+日志REDACTED掩码/Auth+Admin端口权限门/四列审计+限幅分页）；审计中修复被审对象自身测试DSN缺count隔离（F-1）；详见 claim T-audit-004+worklog 审计153 |
| T-audit-005 | T-acct-004 custom_balance 负剩余复核审计（used-only/超额边界） | audit | done | cursor-local | 2026-09-14 | NM-CUR-157 验证支线（登记台无空闲 todo）：发现 FetchCustomBalance used-only 推导负 Remaining（err=nil,Remaining=-used）与 quota<used 超额负剩余，均误触发归零停用；对齐 ParseBalancePayload 147 口径修复（used-only 拒绝采集 + Remaining<0 夹 0）+2 回归单测；build/vet/test -count=1 全包绿；custom_balance 适配核心审核过，接入调度/落库/UI 属 W2 待用户拍板存储位置；claim 见 claims/2026-09-14-cursor-local-T-audit-005.md，报告 docs/worklog/2026-09-14-验证审核157-custom_balance负剩余.md |
| T-deploy-001 | R1 部署收口测试：隔离导入备份跑 D1-D9 九阶段验收发布候选 | deploy | done | cursor-local | 2026-09-13 | 152 立项，162 收口口径三门已过；**NM-CUR-183 执行完毕：D1–D9 全绿（D8迁移单测等价/D7D9轻量版如实标注），R1发布候选可验收，报告见 worklog/2026-09-14-R1发布候选验收报告183.md；关键修复 rawCreate reflect不可寻址panic（未提交，随归档入库）；建议行 done 待用户拍板**；**NM-CUR-201 调度：L1 deploy-收尾负责 backup.go P0 修复+三单测归档+验收结论拍板行，doing 维持等用户拍板**；**182 开工：pool 报告残门作废（需求台无仓库选型需求），部署测试正式进入 doing；发布候选=R1 冻结全 done 项（余额链/归零停用/读侧/探测/跳转页/分组嵌套+group-002 选路）；claim 见 claims/2026-09-14-cursor-local-T-deploy-001.md，规程见 worklog/2026-09-13-收口部署测试规划152.md §4**；NM-CUR-193 复核：rawCreate P0 修复在树WIP未改一行，三处非缺口核销；唯一真缺口导入链零单测已补 backup_raw_test.go 三例（关联绕过/零行/未知形状）；全量-count=1八包绿+vet绿；commit 动作留183会话归档；**NM-CUR-226 L10 终验过门：归档 commit 380cf2c（点名 add 未混他车道 WIP）+九包 test/vet+tsc/前端 build+双专项+密钥扫描全绿，报告 worklog/2026-09-14-R1收口终验226.md；NM-CUR-229 按用户 221"尽快收口"授权拍板 doing→done；R1 冻结代码分车道入库 78a5bb5/7f51eb1/fa6f066/49a8332/d433ff5，commit 后 build+九包 test 复验绿**；R1 冻结面全 done；遗留（不拦本行）：D7 真实 120s SSE/D9 30min 回放降级 T-deploy-002 灰度观察项，sec-001 key 轮换仍是 deploy-002 上线硬前置 |
| T-deploy-002 | 线上部署执行：key 轮换+Caddy SSE 修复+自更新代理口径+灰度回滚 | deploy | todo | - | 2026-09-13 | 152 立项，前置=T-deploy-001 过门+用户拍板；T-sec-001 未轮换 key 不得上线；清单见 152 §5 |
| T-test-002 | 本地部署实测：导入备份+多格式/超时切换/后台统计核查+用量明细三缺陷修复 | test | done | dsh-local | 2026-09-15 | NM-DS-002 执行：面板 13303/relay 11234（**默认 admin 3303 落在 Windows 排除段 3241-3340，bind 被拒**，用 OCTOPUS_SERVER_ADMIN_PORT/RELAY_PORT 覆盖，config.json 未改；data/ 已整目录备份）；导入数据备份(勿提交).json v5 增量合并 12渠道/36凭据/259模型/264授权/340分组/615成员/132价格/5Key/4设置 全量落库。测试：mock 上游多格式 11/11（Chat/Responses/Messages × 流式非流式 + 两向跨协议转换 + 401/400 负例）、真实上游 6/6（成本逐行与价表算式 1e-9 一致）、超时切换与故障转移 5/5（500 转移/冷却跳过/非流式整响应超时/**流式首事件超时**/全员不可用按设计等待且放弃请求记 canceled）、后台审计 13/13。**修复用量明细三缺陷（internal/op/usage.go）**：①Find(&[]UsageRow) 被推成不存在的 usage_rows 表→接口恒空；②内存桶先整批追加再并入→翻倍+重复行；③落库 UpdateAll 覆盖增量→同小时更早窗口丢失（实测 11:47:13 落库把 mock-good (10,110,70) 覆盖成 (2,22,14)，同日统计 27→33 正常）。新增 usage_query_test.go 5 例；build/vet 0、九包 test 全绿；清空自测流量复测后 usage==relay_logs（22/2/704/270）跨多周期逐项相等。报告 docs/worklog/2026-09-15-本地部署与后台统计修复.md；另修 handlers/setting.go 的 stats_save_interval 改值不热生效（补 task.Update，与 relay_log_clean 同周期跟随；实测接口改 1 分钟不重启即按新节拍落库，日志 12:07:28 interval updated）。报告 docs/worklog/2026-09-15-本地部署与后台统计修复.md；另修 model/apikey.go + handlers/apikey.go 的 `enabled:false` 创建被静默忽略（GORM 对带 default 标签的零值字段写 DEFAULT；改为模型去标签 + 入口用 Enabled *bool 保持「不传即启用」）；新增 op/apikey_test.go 2 例与 Key 级审计 12 例（自助统计/RPM/TPM/过期/禁用/max_cost/越权模型/实时概览 SSE/人工中止本轮），全绿 |
| T-route-003 | 路由策略扩展第一阶段：GroupMode lowest_cost（价格定序） | route | done | dsh-local | 2026-09-15 | NM-DS-003 按用户"Iterative Upgrades"指令实施 R-route-001 第一阶段：新增 GroupModeLowestCost + internal/relay/strategy.go（CostProvider/unitPriceFromPrice/rankByLowestCost/pickGroupItemLowestCost）+ balance.go 抽出 partitionCandidates 共用过滤口径 + route.go pickGroupItemByMode 分发（模式即开关，不改既有行为）+ setting.go 导入校验改用 GroupMode.IsValid()；前端模式下拉/联合类型/冷却亲和判定/三语文案同步。**口径**：单位成本=价表 Input+Output，查不到或 0 价（导入 137 条中 48 条为 0）一律沉底，冷却压尾、不可用与归零剔除、失败/探测语义沿用既有链路。**验证**：新单测 6 例全过（含"同批成员 lowest_cost 选便宜者而 failover 仍选 priority 首个"）、go build/vet 0、九包 test 全绿、前端 pnpm build 通过；活体 A/B（真实上游同渠道两价成员）DS-TEST-costmode→glm-5.3-flash vs DS-TEST-costfailover→deepseek-v4-flash-0731，成本与价表算式逐项吻合（6/6）。报告 docs/worklog/2026-09-15-路由策略迭代-lowest_cost.md；遗留：lowest_latency/least_busy/失败率入权重未做，R-route-001 未整体关闭 |
| T-route-004 | 路由策略第二阶段：lowest_latency / least_busy（成员指标写入点） | route | done | dsh-local | 2026-09-15 | NM-DS-006 交付其中**最低延迟**部分：GroupMode lowest_latency（成员最近一次尝试耗时升序 → priority → ID；无耗时数据按 0 乐观参与，与 quality_first 的中性先验同哲学——未知者先试一次、试出真相后自然沉底）。计时挂在既有上报点（recordRouteSuccess/Failure 增加 latencyMs，handler 用 roundStartedAt / roundWaitTime 填），样本与质量共用 300s 窗口、不落库、无后台协程。顺带把 pickGroupItemByMode 重构为 routeDeps 结构体（原 4 个同形状位置参数再加维度极易传错）。**验证**：新单测 4 例（窗口 / 耗时定序 / 未接线退化为 priority / 冷却压尾 / 模式改变选中者 / 生产 provider 端到端）+ 九包 test 全绿 + pnpm build；活体 5/5（mock-slow 上游 sleep 15s 且 priority 1，mock-good 正常且 priority 2）：第 1 次 15.0s 先撞慢成员 → 第 2 次 0.0s 直达快成员且慢成员 0 次尝试 → 第 3 次仍选最快；对照 failover 组仍先撞慢成员。**least_busy 已拆到 T-route-006**（本行只关最低延迟）。报告 docs/worklog/2026-09-15-最低延迟选路.md。 |
| T-fetch-001 | 模型拉取失败：基地址与端点路径版本段重复（/v1/v1/models） | channel | done | dsh-local | 2026-09-15 | NM-DS-004 执行：用户报 openai 直取 https://api.apikey.fun/v1/v1/models 连接失败。**地面真值**：THQ-gpt 的 /v1/models 返回 200 而同域 /v1/v1/models 返回 404；x5m5x 的 /v1/models 200（首测 403 属 Cloudflare 1010 瞬时）；api.apikey.fun 解析到 31.13.96.195 本网络不可达（需代理，与应用无关）。**根因**：base_url 自带版本段时旧代码仍拼端点路径自带的版本段。**修复**：新增 model.IsAPIVersionSegment / ChannelBaseURL / JoinUpstreamURL / EndpointPathOrDefault，统一模型拉取(modelsURL)、三协议探测、出站转换器 BaseURL 三处口径（空路径落回协议默认值）。**验证**：新单测 5 例（含「任何写法不许出现两段相同版本段」守卫）+ go build/vet 0 + 九包 test 全绿；活体 15/15（53HK 13 模型 / 53HK-L 39 模型 / THQ 14 模型；后两家 base 都带 /v1，正是修复前会 404 的形态；三协议实测、建档、自动分组 渠道名/模型名、真实转发 200 全通）。**顺带修**：op 用量明细回归测试偶发失败（全局内存桶 + 用例同名模型名互相污染）→ 用例自持命名 + 前后清桶 + 断言按前缀过滤，op 包连跑 6/6、全量 2/2 绿。报告 docs/worklog/2026-09-15-模型拉取双前缀修复.md；同批澄清跳转页为 T-acct-005 设计行为非缺陷 |
| T-route-005 | 按质量自动切换：成员质量样本 + GroupMode quality_first | route | done | dsh-local | 2026-09-15 | NM-DS-004 迭代：新增 internal/relay/metrics.go（成员最近 300s 成败样本，写入时衰减、无后台协程、不落库、可注入时钟便于测窗口）+ strategy.go(rankByQuality / memberQualityScore 中性先验 1.0 / pickGroupItemQualityFirst) + route.go 两个上报点写入样本与 quality_first 分发 + model.GroupModeQualityFirst（含 IsValid 与三处 binding oneof）+ 前端模式下拉与三语文案。**验证**：新单测 6 例（窗口衰减 / 成功率定序 / nil provider 保持 priority / 冷却与剔除 / 模式胜过 priority / 写点→选路闭环）+ 九包 test 连跑 2 次全绿 + pnpm build；活体 4/4（mock-bad 500 vs mock-good）：冷启动 [mock-bad, mock-good] 3.01s，第二次 [mock-good] 仅一次尝试 0.0s，对照 failover 仍 [mock-bad, mock-bad, mock-good]。报告 docs/worklog/2026-09-15-按质量自动切换.md；踩坑：漏改 binding oneof 标签导致建档 400（模式判定点共 5 类）。 |
| T-proto-002 | 协议无损转化测试：跨协议字段级保真断言（用户同批提出） | proto | done | dsh-local | 2026-09-15 | NM-DS-005 交付（原 NM-DS-004 立项）。**测试矩阵 13/13 全绿**：3 客户端协议 × 3 个单协议位成员把上游协议钉死——渠道 DS-TEST-proto3 用同一个 mock-good 配三把凭据、授权协议位 2/4/8，三个分组各挂一条；另有 3 例同协议直通逐字段相等 + 1 例 Anthropic→Chat 流式。断言口径：四个文本标记（system/instructions 与三轮消息）全在、多轮顺序一致、max tokens 与 temperature 值相等（认各协议字段名 max_tokens / max_output_tokens / max_completion_tokens）、工具名与 Schema 保留、客户端响应形状正确且透传上游正文；直通除 model（分组名→上游模型名，设计内映射）外逐字段相等。mock 上游本轮补记请求体。**实测发现真·有损并已修**：目标协议为 Responses 时 temperature 丢失（chat→responses 与 messages→responses 实测 None，而同协议 responses→responses 直通有值）→ internal/relay/upstream.go 增加 conversionMiddleware.clientBody + carryOverSamplingParams（只在 Responses 目标、客户端给了、上游没有时补 temperature/top_p；非 Responses / 非 JSON / 已有值一律不动）+ 5 例单测；复跑 13/13。另修我自己的假阴性一处（Responses→Chat 实际映射为 max_completion_tokens）。报告 docs/worklog/2026-09-15-协议无损转化测试.md；未覆盖：各协议语义不等价字段（thinking / logprobs / tool_choice 形态）与多模态。 |
| T-route-006 | least_busy：按成员在途请求数选路（NM-DS-006 拆出） | route | done | dsh-local | 2026-09-15 | NM-DS-007 交付（原 NM-DS-006 拆出）。**做法换了**：不搞 acquire/release 成对计数（那正是本行当初担心的漏释放风险），改成从 state.go 的活动请求注册表派生——RequestState 新增 TargetItemID（json:"-"）、startRound 记下本轮成员行、memberBusyCount 数 Sending 且成员匹配的请求数，**结构上不可能漏释放**。排序：在途升序 → priority → ID；过滤/冷却与其它策略同源；未接线退化为 priority。**验证**：新单测 4 例（在途定序含平手优先级、冷却压尾、未接线退化、模式改变选中者、注册表口径泄漏自检含两请求叠加与哨兵 0）+ 九包 test 全绿 + pnpm build；活体 4/4（mock-slow sleep 15s priority1 vs mock-good priority2）：并发 A 冷启动占住 mock-slow 15.01s → 1.5s 后的 B 因 mock-slow 在途 1 改选 mock-good **0.0s** 返回（mock 全程只收到 1 次 mock-slow）；对照 failover 组同并发时序下两个请求都撞 mock-slow（15.0s / 15.0s）。报告 docs/worklog/2026-09-15-最空闲选路.md。路由策略族至此齐全：lowest_cost / quality_first / lowest_latency / least_busy；余 lowest_tpm_rpm 未做。 |
| T-log-003 | 日志卡片字段容错解析 + 复制格式化（lingyuins 清单第 3 项） | log | done | dsh-local | 2026-09-15 | NM-DS-008 交付：此前提到的 lingyuins 五项目标里，第 1/2/4/5 项已由 f1d068f 落地（TTFT/TPS/缓存命中富卡片、字段可见性开关、内存筛选、relay_logs 持久化），只剩第 3 项未做。新增 web/src/components/modules/log/display.ts：resolveLogDisplay 把"实时快照（嵌套 usage / 纳秒 duration / 首字靠时间戳相减）"与"持久化历史行（扁平 ms 字段）"两套形状归一，并按回退链取缺项（首字 first_byte_ms → 时间戳差 → -1 未提交；耗时 duration_ms → 纳秒 → 进行中按共享时钟；总量 total_tokens → 输入+输出；实际模型 target_model → 请求模型；费用缺失即 0，不在前端按价表推算），绝不凭空造值；另出 formatJsonForCopy（能解析就两空格缩进重排、否则原样去空白、解析失败不抛错）。Item.tsx 的 LogMetrics 改用解析结果（顺带修输入量指标为 max(0, prompt-cached) 避免负数）、两处 actualModel 统一走解析、轮次错误复制按钮走格式化。无后端改动/无新接口/无新依赖。**验证**：pnpm build（tsc --noEmit + vite）通过；lint 本次改动文件 0 错误（仓库余 6 个既有错误在 account/chart/FilterBar，非本次文件）；esbuild 单文件编译后纯函数自检 22/22；**真实日志等价性 24/24**（重启后实际产生的 relay_logs #181/#182：新解析与改动前内联公式数值完全一致，且持久化历史行同样可渲染）；产物日志 chunk 含新格式化特征。限制：admin 需登录且本会话审批禁用，故无界面截图，渲染层以类型检查+纯函数自检+真实数据等价替代；历史列表尚未接进卡片（useRelayHistory 仍无调用方），本次只铺平字段层。报告 docs/worklog/2026-09-15-日志卡片容错解析.md。 |
| T-test-003 | 搭建仓库内本地 API 测试套件（15 套件一键跑） | test | done | dsh-local | 2026-09-15 | NM-DS-009 交付：把散在 %TEMP% 的测试脚本收进 scripts/api-tests/（run_all.py 总运行器 + mock_upstream.py + 12 套件 + 2 个前端自检），一条命令跑完并汇总；mock 落盘前把 Authorization/x-api-key 换成 sha256 指纹（不落凭据），端口/路径/库全部 env 可覆盖，前置不满足快速失败。搭测试过程中修掉 5 处脚手架问题（旧 mock 日志写别处/渠道分页误判/冷启动与冷却跨轮次残留/库历史超一页断言过严）与运行器自身 2 处（正则解包、GBK 解码）。全量 15 套件通过。报告 docs/worklog/2026-09-15-测试套件与号池统一视图.md。 |
| T-pool-003 | 谷歌号池与 GPT 号池统一到同一号池视图 | pool | done | dsh-local | 2026-09-15 | NM-DS-010 交付：OfficialPoolStatus 增加 members[]（逐账号给出账号到凭据的映射、套餐/窗口/健康与凭据启用态，只回名称与状态不回显凭据内容），OfficialPoolStatusList 在有无号池渠道两种情况下都回明细；前端账号页新增「账号号池」标签页把 Gemini/GPT/Claude 三池放在一起（各自同步 + 全部同步 + 逐账号明细），三语 17 键。口径说明：三条号池渠道必须分开（上游地址/鉴权不同），能合的是管理层（一处看全、一处同步）。验证：Go 单测 8/8（含曾 active 后失活凭据保留但停用、从未 active 无凭据行）、九包全绿、pnpm build 通过、新 API 套件 run_pool_audit.py 14/14。 |
| T-probe-001 | 主动探活：冷却成员提前解除冷却（健康信号喂选路） | relay | done | dsh-local | 2026-09-15 | NM-DS-011 交付（R-probe-001 / 需求事实源 §4 第 1 条，U-mon-001 缺口）。**口径三条**：①探成员绑定的真实模型而不是渠道 /v1/models——凭据有效但模型仍 500 的成员只探模型列表会被误判成已恢复；②默认关闭 + 周期可配（每次探测都是真实计费请求）；③探测失败不改冷却、不重试，成功才解除冷却并记一次成功样本。**实现**：relay/probe.go 收集仍在冷却期内的成员（跳过不可用成员与已到期冷却）→ 走生产同一套 buildOutbound/applyChannelConfig/resolveUpstreamClient 发 max_tokens=1 的最小真实请求（超时 20s，单轮最多 10 个）→ 成功即 clearMemberCooldown + recordMemberOutcome + 推 route_probe_recovered 事件，并识别“200 + 顶层 error”的假成功；route.go 的 clearMemberCooldown 只删冷却条目、不动当前路由与亲和（正在服务的成员不因一次后台探测被切走）；op/route_probe.go 两个设置读取入口；task/probe.go 注册探活任务（每轮重读开关，改开关无需重启；改周期经 handlers/setting.go 的 task.Update 即时生效）；前端设置页新增「冷却成员主动探活」开关与「探活周期（秒）」输入 + 三语文案。**验证**：单测 probe_test.go 6/6（真链路落库 + httptest 桩：恢复清冷却且断言打到 /v1/chat/completions、模型为成员模型、max_tokens=1；失败保冷却且只尝试 1 次；跳过已到期与手动模式；授权只支持 Anthropic 时探 /v1/messages；200+error 不算恢复；不可用成员不探）；go build/vet 0、九包 test 全绿、tsc + pnpm build 通过；**活体套件 run_probe_test.py 15/15**（上游恢复后 40s 内把 120s 冷却提前解除、探测请求 1 次且 max_tokens=1、流量回切到恢复成员、28s 内两次探测失败而冷却截止时间戳一字不变、两个设置热写热读并在收尾复原）；全量矩阵 16 套件通过。**测试脚手架**：mock_upstream.py 新增 POST/GET /__control（运行期把某模型从失败翻健康——成员模型名是落库配置，只能改上游行为）。报告 docs/worklog/2026-09-15-主动探活.md；claim 见 claims/2026-09-16-dsh-local-T-probe-001.md |
| T-alert-001 | 多渠道通知：飞书/钉钉/企微/SMTP 原生适配 + 发送前真实测试 | notify | done | dsh-local | 2026-09-16 | NM-DS-012 交付（R-alert-001 余项 / 需求事实源 §4 第 4 条）。**渠道与判据**：webhook 保持原样（POST 原始事件 JSON）；feishu `msg_type/content.text` 且看 `code`（兼容旧 `StatusCode`）；dingtalk/wecom `msgtype/text.content` 且看 `errcode`；smtp 走 RFC2047 主题 + base64 UTF-8 正文（465 隐式 TLS、其余 STARTTLS、全程 deadline）。**三条口径**：①不信任 HTTP 200——三家机器人都用 200 包业务错误码，实测 feishu code=19024 / 钉钉 errcode=310000 均判失败并把业务码带进结果；②投递永不阻断调用方，单渠道失败不影响其它渠道；③SMTP 密码只从 OCTOPUS_SMTP_PASSWORD 读，**不进设置表/库/备份**（README 环境变量表已补）。**改动**：internal/notify/channels.go（Kind/EnabledKinds/Targets/Send/TestSend/renderText/DisplayTitle/sendSMTP）、Event 新增 Title、model/setting.go 新增 10 个设置键与校验（渠道白名单/URL 形状/端口范围/邮箱形状/SMTP host 不带 scheme）、三个既有事件源（余额告警/归零停用/探活恢复）改走 Send、handlers 新增 GET /api/v1/setting/notify/channels 与 POST /api/v1/setting/notify/test、前端设置页新增「通知渠道」卡片（五渠道开关 + 三机器人 URL + SMTP 五项 + 发送测试与逐渠道结果）三语 23 键。**验证**：notify 包单测 8/8（报文形状/业务码判失败/渠道过滤/默认回退/缺项提示/状态接口不泄露凭据/SMTP 真投递含多收件人与中文主题/有用户名无密码提前报缺项）；活体套件 run_notify_audit.py 14/14（含**真实事件端到端**：探活解除冷却后 webhook 桩收到 type=route_probe_recovered、feishu 桩收到渲染标题「冷却成员已恢复」，以及收尾把全部设置原样复原）；go build/vet 0、九包 test 全绿、tsc + pnpm build 通过、全量矩阵 17 套件通过。**未做**：设计稿 §3 的规则引擎（NotifyRule/NotifyEvent 去重收敛）、公告/价格变化事件源、钉钉企微加签、投递历史落库。报告 docs/worklog/2026-09-16-多渠道通知.md；claim 见 claims/2026-09-16-dsh-local-T-alert-001.md |
| T-export-001 | 请求级明细导出：CSV 接口 + 面板导出按钮 | export | done | dsh-local | 2026-09-16 | NM-DS-013 交付（U-key-001 余项 / 需求事实源 §4 第 5 条）。**口径**：与 `/api/v1/log/history` 共用同一套筛选条件（抽出 relayLogQuery），导出不分页=当前筛选全量；逐行 Rows()+ScanRows 流式写出（内存不随条数增长），上限 20 万行；首行前给 UTF-8 BOM（Excel 中文表头不乱码）；未提交首字节导出空串（不给下游留 -1 假值）；**第一列是日志行主键**而非请求 ID（一个请求可留多行，请求 ID 会重复）；协议导出可读名。**改动**：internal/op/log.go 抽查询共用 + dbConn 加 ScanRows、internal/op/log_export.go（RelayLogExportCSV/relayLogExportRow/protocolLabel）、handlers/log.go 新增 GET /api/v1/log/export（text/csv + attachment + X-Accel-Buffering:no）、前端 logs 工具栏「导出 CSV」（内存筛选映射到后端参数）+ exportRelayLogs() + 三语 4 键。**验证**：op 包单测 3/3（BOM/表头/行序与行主键唯一/RFC3339/协议名/-1 转空串/CSV 转义往返/筛选/空结果仍给表头）；活体 run_log_export_audit.py 11/11（下载响应头、BOM、16 列表头、457 行行主键严格升序且唯一、**与 /history 首页 200 行逐字段等价 0 处不一致**、状态筛选 canceled 19/19 与 success 438/438、模型筛选 175 行、关键字筛选 421 行全命中、未登录 401）；go build/vet 0、九包 test 全绿、tsc + pnpm build 通过、全量矩阵 18 套件通过。**未做**：时间范围筛选（与 /history 一起加）、导出审计、超限提示头。报告 docs/worklog/2026-09-16-请求级明细导出.md；claim 见 claims/2026-09-16-dsh-local-T-export-001.md |
| T-route-007 | lowest_tpm_rpm：按最近 60s 消耗（token 优先、其次请求数）选路 | route | done | dsh-local | 2026-09-16 | NM-DS-014 交付，**路由策略族至此齐全**（lowest_cost / quality_first / lowest_latency / least_busy / lowest_tpm_rpm）。**口径**：排序 = 窗口内 token 消耗升序 → 请求数升序 → priority → ID；窗口固定 60s（memberLoadWindowMs，写入时判定过期即开新窗口，无后台协程）；**统计尝试而非成功**（失败同样占用上游配额）；token 只在请求终态记一次并归属最近服务它的成员行；无记录按 0 参与（乐观先验）；与 least_busy 的分工=在途 vs 最近一分钟已消耗；**诚实边界**：上游真实 RPM/TPM 限额不可知，这里排的是我们自己的近期消耗（摊平负载、少撞限流），不是严格剩余额度。**改动**：metrics.go 新增负载窗口（recordMemberAttempt/recordMemberTokens/LoadProvider/memberRecentLoad）、route.go 成功失败两处记尝试 + 分发分支 + 热路径注入、state.go finishLocked 按 TargetItemID 记 token、strategy.go rankByRecentLoad/pickGroupItemLowestTpmRpm + routeDeps.load、model/group.go 新模式（5 点清单全同步）、前端三语。**验证**：单测 rpm_test.go 5/5（窗口计数与过期/排序与平手/全冷却/模式切换/生产接线含失败也计）；活体 run_rpm_test.py 10/10（套件开头删组重建拿干净成员行；冷启动按 priority 15.00s、零消耗成员优先 0.00s、平手按 priority、放大 40 倍 token 后 /history 记到 440/280、**请求数相同时按 token 让开**、failover 对照、63s 后窗口过期重新平等）；go build/vet 0、十包 test 全绿、tsc + pnpm build 通过、全量矩阵 19 套件通过。报告 docs/worklog/2026-09-16-近期消耗最低选路.md；claim 见 claims/2026-09-16-dsh-local-T-route-007.md |

| T-chan-001 | 渠道凭据补录（53HK / 53HK-L 换新密钥、新增 longcat）与「创建渠道时 enabled:false 的凭据被静默忽略」修复 | channel | done |
| | 备注：longcat 基址须带 `/openai`（裸域名 `/v1/*` 是 404）；换密钥走「读 detail → 只替换 keys[].key → 回写」的非破坏路径；缺陷根因是 ChannelKeyConfig.Enabled 的 `gorm:"default:true"`，修法与 APIKey.Enabled 同族（默认值移到请求层，指针区分未提交/显式 false）。验证：新增 internal/op/channel_key_test.go 回归单测 + 活体复验（create enabled:false 落库为 0）+ 19 套件矩阵。未决（需用户拍板）：单成员分组在冷却期内静默等待是否改为快速失败；两站点登录自动化被人机验证挡住，改用手动登录跳转页。已提交。 | 2026-09-16 03:01:42 |

| T-hedge-001 | 首字竞速：提交首字节之前并发请求排序靠前的多个成员，取最快给出有效响应者（用户 2026-09-16 指令）| route | done |
| | 备注：模式无关（候选沿用各模式排序），两个触发条件 hedge_after_ms（慢启动）与 hedge_peak_in_flight（高峰期在途）；落选成员被我们自己取消，不算失败、不进冷却、不污染质量样本；默认关闭（竞速会多打上游、多花钱）。实现 internal/relay/hedge.go + handler.go 接入 + state.go 的 inFlightByModel/retargetRound + model/group.go 四个配置项；前端分组编辑面板已有开关/并发数/慢启动阈值/在途阈值 + 三语文案（含额外费用提示）。验证：scripts/api-tests/run_hedge_test.py 5/5（竞速 0.8s 快者胜出 vs 对照 15s 慢者走完、落选不进冷却、在途触发、宽度越界 400）+ 全量矩阵。已提交。 | 2026-09-16 03:47:42 |

| T-test-004 | 测试门禁：真实上游套件默认跳过（用户要求不要在空闲时间拿真实 API 测试）| test | done |
| | 备注：run_all.py 给 real/costmode 打 real 依赖标记并默认跳过，需 --with-real 或 OCTOPUS_ALLOW_REAL=1；两个套件自身也加门禁（无开关时 SKIPPED 退出码 0）。新增 hedge 套件并注册进矩阵（20 套件）。已提交。 | 2026-09-16 03:47:42 |

| T-diag-001 | 定性「真实上游探针能通、转发无首字节」：对照两条代码路径发出的报文 + 竞速套件补流式用例 | diag | done |
| | 备注：新增 scripts/api-tests/compare_probe_vs_relay_wire.py，用本地 mock 截获探针与转发的真实报文并逐项对比 —— 路径族相同、凭据携带相同、Header 组合相同，body 键的差异只来自客户端是否提交 stream（客户端不带时转发也不带，mock 日志可证）。结论：该现象不是我们构造的请求差异造成的，剩余解释只能是上游侧限流/指纹拦截叠加「单成员冷却等待」；**不需要改代码**，彻底定性需授权打十来个真实请求（用户已要求空闲期不测真实 API，未做）。同批：run_hedge_test.py 增加 T6 流式竞速用例，套件 6/6（首个 SSE 块 0.8s 由竞速胜出的快成员给出）。已提交。 | 2026-09-16 04:00:17 |

| T-weight-001 | 加权综合选路第一阶段：五维权重（成本/质量/延迟/在途/近期消耗）做成可调设置项 + weighted 模式 | route | done |
| | 备注：需求 R-weight-001 的第一阶段。新增 GroupModeWeighted + internal/relay/weights.go（候选集合内归一化、按权重求和、并列按 priority/ID）+ 5 个设置键 route_weight_*（默认 30/30/15/15/10，0..100，越界拒绝而非静默夹紧）。两条口径：缺数据按**乐观先验**（与 lowest_latency 的无耗时按 0ms、quality_first 的无样本中性 1.0 同一哲学）、权重全 0 退化为 priority 顺序。单测 7/7；活体套件 run_weighted_test.py 4/4（延迟权重拉满后 15.0s → 0.0s 改选快成员）；全量矩阵 21 套件 0 失败。**包月/按次/余额/倍率四个维度未做**：仓库里没有存这些数据的位置，需新增渠道字段 + 迁移 + 面板（下一阶段）。三个取舍点（默认权重排序 / 贵而快能否压过便宜但常错 / 包月用尽降权还是剔除）仍留用户决定，本阶段已全部做成设置项，所以无论怎么定都不用再改代码。已提交。 | 2026-09-16 05:30:25 |

| T-weight-002 | 加权综合第二阶段：倍率 / 按次单价 / 余额 / 包月余量 接入加权选路（R-weight-001）| route | done |
| | 备注：渠道新增 5 个可空字段（billing_mode/multiplier/per_call_price/monthly_quota/monthly_used，GORM 启动自动加列，纯增量）；余额不靠用户填 —— 配额扫描本来就取到 remaining，这次把它存进内存快照（op.RecordChannelBalance/ChannelBalance）供选路读；包月用尽的处置做成设置项 route_monthly_exhausted_action（demote 默认 / exclude），即用户点名的第三个取舍点；新增 4 个权重键 route_weight_multiplier/per_call/balance/monthly（默认 15/10/15/10）。**过程中发现并修掉一个真实接线缺陷**：pickGroupItemHot 自己抄了一份 routeDeps 字面量，没带 billing，导致「模式支持、单测全绿、生产该维度静默失效」——热路径改为统一走 hotRouteDeps()，并加守卫测试 TestHotRouteDepsCoversEveryDimension（任一 provider 为空即红）。单测 14/14；活体套件 run_weighted_test.py 6/6（T5 计费字段写入读回、T6 倍率权重拉满改选倍率 1 的渠道）；全量矩阵通过。未做：渠道编辑面板的计费字段输入框、权重设置界面（纯前端，下一轮）。已提交。 | 2026-09-16 06:12:17 |

| T-weight-003 | 面板收尾：渠道计费字段 + 加权权重设置界面（R-weight-001 的可用性收口）| frontend | done |
| | 备注：渠道编辑对话框「高级配置」新增计费方式下拉 + 倍率/按次单价/包月额度/包月已用四个输入与说明；设置页「系统-路由参数」新增加权权重区块（9 个维度权重 + 包月用尽处置下拉，失焦即保存）；api/channel.ts 与 channel state 同步字段、SettingKey 枚举补 10 个键、三语文案补齐；实现用自包含 WeightSettingField 小组件避免抄 10 份 state/ref 样板。验证：pnpm build 通过, 产物分块含 billingHint/routing.weights/weightMultiplier/monthlyExclude；全量矩阵 21 套件通过。诚实说明：未做人工视觉验收（浏览器工具那一步被拒）, 只验证到编译+产物+接口层；繁体 locale 因插入处理失当被重排（98 增 45 删）, 内容经解析与构建双重校验。已提交。 | 2026-09-16 06:39:56 |

| T-test-005 | 组合场景套件：加权 × 计费 × 竞速 × 流式 × 冷却 × 日志统计叠在同一分组上验证 | test | done |
| | 备注：此前各特性都有独立套件, 但没人验证组合。新增 scripts/api-tests/run_combo_test.py（只用本地 mock）: C1 加权选路把慢成员排第一时, 竞速 800ms 后救场（0.8s 胜出, 两条成员都被尝试）; C2 落选成员不进冷却; C3 倍率权重拉满 → 倍率低的渠道胜出且不触发竞速（上游只被尝试 1 次）; C4 每请求恰好一行日志、成功行带首字节; C5 流式 + 竞速 + 加权, 首个 SSE 块 0.8s 由胜出成员给出。5/5 通过, 已注册进矩阵（22 套件）。已提交。 | 2026-09-16 07:16:22 |

| T-pool-ext-001 | 号池扩展层第一批接口：适配器注册表 + 统一视图 + 只读 API（R-pool-ext-001）| pool/api | done |
| | 备注：用户 2026-09-16 指令「号池加入高度扩展性，可基于已有的开发大量 API 接口，后面好做各种反代工具包的扩展」。新增 internal/pool（Adapter/Registry/Capability/FieldSpec/Entry + 内置官方账号池适配器 + safeEntries 的 panic 兜底），官方账号池适配器只投影既有 op.OfficialPoolStatusList（不新增表、不动选路）；新增 GET /api/v1/pool/{kinds,entries,stats}（Admin+Auth；单后端失败进 warnings 且仍 200）；单测 6/6、活体套件 run_pool_api_test.py 8/8（含凭据泄漏探针：往库里写可识别串并断言不出现在响应里）；矩阵扩到 23 套件。设计文档 docs/requirements/2026-09-16-号池扩展性-design.md §4 给出后续批次（单条详情/生命周期操作/运行时声明式适配器注册），§6 列出四个待用户拍板的取舍。已提交。 | 2026-09-16 09:44:00 |

| T-pool-ext-002 | 号池扩展层第二批：按能力位放行的生命周期操作（R-pool-ext-001）| pool/api | done |
| | 备注：新增可选能力接口 Getter/Prober/Refresher/Syncer/Toggler（能力位=对外声明, 接口=对内契约, 两层都满足才放行；缺能力位或声明了没实现都回 ErrUnsupported, 后者消息点名 declared but not implemented）；官方账号池适配器用既有 op 实现 get/probe/refresh/sync（probe 失败也回条目；sync 逐服务商跑, 一个失败不影响其余）；新增 GET /pool/entries/:kind/:id、POST /pool/entries/:kind/:id/{probe,refresh,enable,disable}、POST /pool/kinds/:kind/sync；错误映射即契约（未知 kind/条目不存在 404、无此能力 501、其余 502）。刻意没做：不给官方账号池声明 toggle（无渠道凭据级启停 op, 宁可回 501 也不假装支持）；活体不真跑 official 的 sync（会给实例建号池渠道, 属写副作用）。验证：单测 internal/pool 10/10（新增 4 例）、活体 run_pool_api_test.py 14/14（新增 6 例）、全量矩阵 23 套件 0 失败。已提交。 | 2026-09-16 09:57:28 |

| T-pool-ext-003 | 号池扩展层第三批：查询面（过滤/汇总/导出）与机器可读的操作清单（R-pool-ext-001）| pool/api | done |
| | 备注：新增 internal/pool/query.go（Filter/ParseFilter/ListResult、SummaryOf、ExportRows、Operation/operationsOf）；/pool/entries 加过滤参数（kind/provider/status/q/enabled/healthy/expiring_within/has_expiry，非法取值报错而非静默 false）、新增 /pool/summary 与 /pool/export?format=json|csv；/pool/kinds 每个后端回填 operations[]（由能力位推导，声明与路由不可能漂移）。验证：internal/pool 单测 15/15（新增 5 例）、活体 run_pool_api_test.py 20/20（新增 6 例）、全量矩阵 23 套件 0 失败。已提交。 | 2026-09-16 10:36:00 |
| T-test-006 | 修 stats 套件 usage ≤ relay_logs 断言的基线缺陷（不是产品缺陷）| test | done |
| | 备注：该断言用单页 200 当基线，表超 1700 行后必然误报；改为一页取满 500（翻页在并发写入下会 offset 漂移跳行，已实证 123/123/122 三组读数）；并把断言拆成「当前小时带一个小请求的跨小时归属余量 + 已结算上一小时严格」两条守卫，比原来更强。实证跨小时归属：hour09=(−1,0,−11,−7)、hour10=(+1,0,+11,+7)。结果 stats 14/14、矩阵 23 套件 0 失败。已提交。 | 2026-09-16 10:36:00 |

| T-pool-ext-004 | 号池扩展层第四批：分页排序 + 批量动作 + 单 kind 详情 + OpenAPI 文档（R-pool-ext-001）| pool/api | done |
| | 备注：query.go 加分页/排序（total 与 returned 分开回、越界回空、未知排序字段回退不报错）；新增 internal/pool/batch.go（逐条独立、未声明能力逐条回不支持、需要 ids 或 filter+max、默认 50/硬上限 200、超出计 skipped）；新增 internal/pool/openapi.go（由注册表推导 paths 与 kind 枚举、12 个组件 schema）；路由新增 GET /kinds/:kind、GET /openapi.json、POST /entries/batch。验证：internal/pool 单测 23 例全绿（新增 8 例）、活体 run_pool_api_test.py 30/30（新增 9 条断言）、全量矩阵 23 套件 0 失败。已提交。 | 2026-09-16 10:47:08 |

| T-pool-ext-005 | 号池适配器契约自检工具包 + 适配器开发指南（R-pool-ext-001）| pool/docs | done |
| | 备注：新增 internal/pool/pooltest（Run/Check 七类契约检查：身份/能力位/声明即实现/字段/条目身份/凭据不外泄/列表详情一致/panic 兜底），配套单测 12 例（每条规则都有反例）与可执行示例适配器 example_adapter_test.go；给 internal/pool 加公开助手 KnownCapability/AllCapabilities；新增 docs/guides/号池适配器开发指南.md（九节：定位图/最小适配器/能力位对照/自检/注册/错误语义/常见坑）。本批无行为变更（不重启实例）。验证：pooltest 12 例、pool 23 例、go test ./... 12 包全绿、build/vet 0、gofmt 干净、活体矩阵回归无失败。已提交。 | 2026-09-16 10:59:40 |
| T-test-007 | stats 套件「已结算上一小时」过强断言修复（测试装置自身缺陷）| scripts/api-tests | done |
| | 备注：根因是我上一批自己写下的判据过强——"已结算小时 usage 只会不大于日志（差值=被重启丢掉的未落库桶）"不成立，跨小时归属是**双向**的（骑在小时边界上的请求在 usage 桶与 relay_logs 里按不同时间戳归属），上一批自己的实证证据 hour09=(−1,0,−11,−7)、hour10=(+1,0,+11,+7) 已经两种方向都出现过。修复：settled 小时改用与当前小时同一条双向余量 boundary_tolerance=(1,1,20,20)，并追加**反翻倍比例守卫**（usage 请求数/token 不得超过日志的 1.5 倍＋2 —— 重复计入是 2 倍量级，±1 的边界抖动抓不住它）。实证：修复前 FAIL hour=2026091610 usage=(284,14,5071,3158) vs relay_logs=(283,14,5060,3151)；修复后 14/14 通过（同一读数落在余量内、反翻倍守卫通过）。| 2026-09-16 11:05:00 |
| T-pool-ext-006 | 人工启停（toggle）能力与「谁把它关的」语义（R-pool-ext-001）| op/model/pool | done |
| | 备注：补上"渠道凭据级启停"这块缺口时发现真问题——OfficialPoolSync 对 active 账号无条件 `enabled=true`，人工停用会被下一次同步静默撤销。修法：`ChannelKey.OperatorDisabled` 单独记"人工决定"这一位，同步读到就跳过自动启用并在结论里回 `Held`；新增 `op.SetChannelKeyEnabled`（同时写两列 + 刷新转发缓存）；渠道页保存与号池 API 两个入口规则统一（显式启用=解开人工停用，显式停用=落下）；`pool` 适配器声明 CapToggle 并实现 SetEnabled，凭据未物化时回 409（新增 pool.ErrConflict → HTTP 409）。证据：变异验证（去掉同步里的尊重分支后 TestOfficialPoolSyncRespectsOperatorDisabled 立刻红、报 "sync re-enabled an operator-disabled key"，还原后绿）；op 新增 3 例、op+pool 97 例全绿；活体 poolapi 31/31（P10 按 key_exists 分岔、P10b 404、P12/P19 含 toggle 路由）；全量矩阵 24 套件 0 失败。| 2026-09-16 11:40:00 |
| T-test-008 | 活体套件里三处"官方号池没有 toggle"的旧假设随契约更新（P10/P12/P18b/P19）| scripts/api-tests | done |
| | 备注：契约变了套件就得跟着走——官方声明 toggle 后，原先断言 501「capability not supported」的 P10、断言"清单里不该有启停路由"的 P12 与 OpenAPI 的 P19、以及批量断言未声明能力的 P18b 都会失败。改法：P10 按夹具 `detail.key_exists` 分岔（未物化 → 409 conflict 且不泄漏探针；已物化 → 200 且真的启用），只用 enable 方向以免碰到真实凭据；P10b 未知条目启停 → 404；P12/P19 改为要求含 toggle 路由；P18b 改为断言逐条失败原因是"状态不允许"（能力位已声明）。| 2026-09-16 11:40:00 |
| T-pool-ext-007 | 面板统一号池页（消费 /pool/*，按能力位决定按钮）（R-pool-ext-001）| web | done |
| | 备注：让面板当第一个调用方，接口面的别扭处才算被真用一遍。页面先取 /pool/kinds 再按能力位渲染，后端 tab/条目字段都来自自描述，所以以后接新反代工具包这一页不用改。含 api/pool.ts、modules/pool/index.tsx、nav/懒加载接线、三语 45 键（navbar.pool + unifiedPool）。验证：tsc 0、eslint 0（该文件）、产物实测含页面标记（index-D6vtIVqI.js 494KB）、全量矩阵 25 套件 0 失败。未做人工视觉验收（用户明确拒绝浏览器自动化）。| 2026-09-16 13:20:00 |
| T-test-009 | 面板静态资源套件 run_panel_asset_test.py（4/4，注册进 run_all 第 25 条）| scripts/api-tests | done |
| | 备注：前端没重建/没进二进制时后端套件全绿也发现不了，这条直接看正在服务的产物：首页 HTML 带入口脚本 → 入口脚本可下载 → 构建产物含号池页标记 → 三语文案齐全。| 2026-09-16 13:20:00 |
| T-pool-ext-008 | 面板批量动作（探活/启用/停用）接上 /pool/entries/batch（R-pool-ext-001）| web | done |
| | 备注：口径是"选择就是 ids，绝不隐式全量"——不提供按筛选条件全量执行的入口，因为启停直接影响线上路由。一次调用只针对一种后端（选中键按第一个冒号切 kind，id 自身可能含冒号）；跑完只保留失败/未执行项，避免重复提交已成功的。验证：tsc 0、eslint 0、产物标记（panel P5 断言 批量探活/Probe selected/batchDisable）、poolapi 32/32。踩坑：React Compiler 不接受手工记忆化的 Map/Set（preserve-manual-memoization），改直接算。| 2026-09-16 15:05:00 |
| T-test-010 | CSV 导出契约：BOM + 每行列数（poolapi P15b/P15c）+ 面板批量界面进产物（panel P5）| scripts/api-tests | done |
| | 备注：给表格软件用的 CSV 补了 BOM（否则中文乱码），并把"哪个后端没取到数据"用 X-Pool-Error-Kinds 响应头报出来。加 BOM 会让原 P15b 的表头断言变红 —— 不是回归，是断言要跟着契约走，已改为先剥 BOM；新增 P15c 用标准库 csv 解析校验列数一致（列错位比缺行更难发现）。| 2026-09-16 15:05:00 |
| T-pool-ext-009 | 内置第二个适配器 `channel`：把既有渠道凭据投影成号池条目（R-pool-ext-002）| internal/pool | done |
| | 备注：条目 = (渠道, 凭据)，ID `<channel_id>:<key_name>`；排除官方账号池自动物化的渠道（同一条凭据不两个身份）；能力位 list/get/probe/refresh/toggle，不声明 sync/provision/revoke（没有东西需要物化）。probe 走 health.ProbeToken（按站点族选口径，口径回写 detail.probe_kind）、refresh 走 health.FetchBalance + op.RecordChannelBalance（余额顺带喂给加权选路的 balance 维度）、toggle 走 op.SetChannelKeyEnabled。验证：station_test 8 例 + station_contract_test 用 pooltest 自检 + 活体套件 42/42；为让 pooltest 能取到内置适配器，pool.go 新增 Lookup(kind)。踩坑：/channel/create 的 models 必须是字符串列表。| 2026-09-16 13:45:00 |
| T-test-011 | 号池活体套件新增渠道凭据用例（P21-P27）并修正夹具命名污染（R-pool-ext-002）| scripts/api-tests | done |
| | 备注：P21 能力位、P22 ID 口径/站点族/探活口径、P22b 凭据不外泄、P23 官方号池渠道不重复列示、P24 探活结论、**P24b 从 mock 上游日志反证打的是 GET /api/user/self**、P25 余额 500/120/380、P26 启停写两位（直接查库）、P27 未知凭据 404 → 42/42。新夹具名刻意不含 RUN_TAG 子串，否则 P2/P13b/P17/P18b 会被名称子串过滤连带污染成假失败。mock 上游补 /api/user/self。| 2026-09-16 13:45:00 |
| T-test-012 | 夹具清理方式修正：渠道删除接口只认 DELETE（POST 打不到，残留回不了头）| scripts/api-tests | done |
| | 备注：实证 POST /channel/delete/:id → 404、DELETE → 200 且库里该行消失；套件清理与"建前删旧"统一改 DELETE，并清掉历史残留 3 条，重跑后查库零残留。教训：夹具清理本身要验证——"调了删除接口"不等于"真删了"。| 2026-09-16 14:05:00 |
| T-alert-002 | 通知渠道新增 Server酱（ServerChan/Turbo³）：表单推送 + 业务错误码判定 + 凭据不外显（R-alert-002）| internal/notify, internal/model, web | done |
| | 备注：入口 `POST <base>/<SendKey>.send`，正文表单 title/desp/tags（故新增 postForm）；应答看顶层 code 与 data.errno；SendKey 环境变量 `OCTOPUS_SERVERCHAN_SENDKEY` 优先、设置表 `alert_serverchan_sendkey` 回落；测试缝 `OCTOPUS_SERVERCHAN_BASE_URL` 让套件打本地桩。验证：serverchan_test 6 例 + 活体套件 +3 用例（5→6）+ 一次性真实推送。| 2026-09-16 14:40:00 |
| T-pool-ext-010 | 号池导出口径与列表一致（ExportRows 收 Filter、共用比较器、面板带筛选导出） | done |
| T-alert-003 | 告警链路：notify_problem.py + run_all --notify/--notify-selftest + notifyproblem 套件 | done |
| T-test-013 | 导出口径活体用例 P15d/P15e/P15f；告警链路套件 10 例（含真实 400 应答口径单测） | done |
| T-test-014 | 装置加固：桩日志凭据脱敏（唯一实施点）+ 桩存活判据改真实应答/脱离进程组 | done |
| T-test-015 | 备份隔离导入套件：真备份导进独立实例（独立端口/独立库/脱敏副本）验证计数·编辑·三协议调用，并断言生产库指纹不变（R-test-001）| scripts/api-tests | done |
| | 备注：`scripts/api-tests/run_backup_import_test.py` 22/22（已注册进矩阵）。隔离三层：① `octopus.exe start --config <临时配置>` 起第二实例（13304/11235 + `%TEMP%` 独立库）；② 导入的是脱敏副本——12 个渠道 base_url 全指本地 mock，其余逐字保留；③ 关掉会自己出网的后台任务（quota_scan_interval=0、route_probe_enabled=false、model_info 拉长、5 个告警目标清空）。**导入前有硬安全闸门**：先断言临时库文件确实生成，不满足立即中止绝不导入（否则备份会写进生产库）。判据修正两处：生产库 mtime 不能当"没被改动"的证据（实例自己写统计使 mtime 必变）→ 改六表计数+渠道指纹 sha256；`channel_grants` 无 channel_id 列（经 channel_model_id/channel_key_id 两侧回指渠道）→ 挑分组的 SQL 相应改写。| 2026-09-16 16:20:00 |
| T-migrate-001 | 修掉真实缺陷：全新安装第一轮没有请求日志表（009 迁移把 AutoMigrate 刚建好的 relay_logs 删了）（R-test-001 顺带查出）| internal/db/migrate | done |
| | 备注：009（2026-08-24）职责含"删遗留 relay_logs"，注册在 **AfterAutoMigrate**；2026-09-13 的重做日志卡片又重新引入 relay_logs 并加入 AutoMigrate 列表 → 全新库启动顺序变成 AutoMigrate 建表、009 紧跟着 DROP，且迁移记录已写、后续启动不重跑，要等再启动一次才建回空表。后果不止"页面空"：`RelayLogSave` 是 `_ = Create().Error`（忽略错误），第一轮运行期每条请求日志都被静默丢弃。证据：一次性临时库跑两轮启动，修复前 20 张表/relay_logs 不存在、第二轮 21 张/存在；修复后两轮都 21 张/存在。修法=**原地加形状判据**（表存在且含旧形状独有列 request_model_name/actual_model_name/ftut/use_time/request_content 才删）；不改阶段，因为同批 005 依赖遗留 channels.base_urls 做迁移，009 提前删列会让 005 失去输入、旧库升级丢 base_url。三路径行为：全新库保留新表、旧形状表照旧删、009 记录已在的现有库整条跳过（实测生产库 2757 行日志、列全为新格式、无旧列残留）。| 2026-09-16 16:20:00 |
| T-test-016 | 迁移 009 回归测试 4 例 + 变异验证（去掉判据必须变红）| internal/db/migrate | done |
| | 备注：新增 `009_test.go`——全新形状保留且**能真正落一行并读回**、旧形状被删、无表时不报错、同批 `channels.base_urls` 删列行为没被带偏。变异验证：把 `if legacyRelayLogs(db)` 改成 `if true`（等价修复前行为）→ `TestMigrate9KeepsFreshRelayLogs` 立即失败（报文即缺陷描述），还原后 4 例全绿。判据成立的前提写进注释：GORM AutoMigrate 只加列不删列，所以"旧形状独有列还在"在 AutoMigrate 前后都成立。| 2026-09-16 16:20:00 |
| T-timeout-001 | 修复真实缺陷：流式响应首帧之后没有上限（上游吐完首帧就静默时客户端被挂到 300–760s 才放弃）| internal/relay | done |
| | 备注：线上证据来自用户日志包 Caddy 日志——60 条 error 里 34×502+26×504、时长 min 16.3s / 中位 343s / max 761.5s、39 条 ≥300s、51 条 ≥120s；非流式最坏只 2×120+1=241s ⇒ 只能是流式。根因：`upstream.go` 读到首个事件即返回，`handler.go` 随即 `timeoutTimer.Stop()`，之后转发循环 `result.events.Next()` 是无 deadline 的阻塞读（依赖 `axonhub/llm` 的 SSE 解码器全文无 Timeout/Deadline）。修法：新增分组配置 `member_stream_idle_timeout_seconds`（两次「进展」之间最长间隔，默认 300、**0=关闭**、负数归零；归一化不用默认值兜底故存量分组行为不变），看门狗在「写出一帧」与「读到新事件」两处重置，命中后 `cancelRoundCause(errStreamIdleTimeout)` 并把失败原因还原（记账走 markFailed 而不是 canceled）；首字节已提交 ⇒ 不换目标重试。附带修掉面板 SSE `/api/v1/group/events`「连上不发任何字节 + 心跳正好 15 秒」与客户端空闲上限同刻竞速（日志里 107 条连接全在 15.001–15.015s 被断开）：连接后立即发 `: connected`，心跳 15s→10s（`/api/v1/log/overview/stream` 同口径）。| 2026-09-16 17:10:00 |
| T-test-017 | 流式无进展上限活体套件 6 例 + 面板产物标记 + 两处装置口径修正 | scripts/api-tests | done |
| | 备注：新增 `run_stream_idle_test.py`（已注册进矩阵，28 套件）：mock 新增 `stall` 行为（模型名含 `stall` 或 `/__control` 覆盖＝只发第一个 SSE 帧后保持连接静默、不写 [DONE] 不关连接）；I2 读数 `HTTP 200 首块 0.0s 总 2.01s 收到 214B`、I3 `relay_logs=('failed','DS-TEST-idle',2)`、I4 渠道统计 request_failed 0→1、I5 member_max_attempts=2 但上游只收到 1 次尝试、I6 idle=0 时 3 秒仍在途中——**I2 与 I6 是同一二进制内的对照**（同一静默上游，唯一差别是该配置 2 与 0），即「是看门狗结束的响应」的直接证据，因此不再做摘代码的变异验证。面板加 P7 产物标记（`streamIdleTimeout` + 三语文案）。两处装置口径修正：① 成员统计是内存缓存累加 + 周期落库，直读 `channel_models.request_failed` 永远读到旧值 → 改读 `/api/v1/channel/stats`（首版断言 `before=0 after=0` 即此坑）；② Python `HTTPResponse.read(n)` 对分块响应会读到凑够 n 字节，量到的是流结束时刻而非首块时刻（首版 `首块 2.04s 总 2.04s`）→ 改用 `read1()`。| 2026-09-16 17:10:00 |
| T-retry-001 | 修复真实缺陷：客户端取消被记为成员失败 + 上游持续不可用时无限重试（日志包余项 / 上游 #388 #338 #375 #349）| internal/relay | done |
| | 备注：两处缺陷同一个根：**失败记账与重试边界**。① 取消记账：`handler.go` 首字节**已提交**的流式分支按 `err != nil` 一律 `RequestFailed=1`，与提交前分支「客户端取消不计成员故障」的口径相反 ⇒ 用户假死探针 354 次里 123 次超时取消把它打过的成员判死，最终降级 128 项（README 说 11 项），铁证是 `channels_health.tsv` 里 `Forestapi/deepseek-v4.1-flash` **158 成功 / 0 失败仍被降级**；修法＝`ctx.Err() != nil`（客户端离开）不计成员失败，服务端自己的超时/看门狗照旧计。② 重试边界：上游 #388（报在 v0.13.4＝本项目基线）与 #338 同源——`recordRouteFailure` 对手动模式直接 `return false`（永不冷却）、`pickGroupItem` 手动分支又无条件返回 `ActiveItemID`，于是每 `member_retry_interval_seconds` 打同一个成员且永不停止；用户 `relay_config` 是 `max_attempts=2 / cooldown=600s`，全成员冷却后循环只能 1s 一次反复选路，正是中转站判「攻击行为」的形态。修法：新增 `internal/relay/retry.go`——`attemptCap=clamp(成员数×单成员尝试×2,6,64)`、同成员连续失败退避 `1→2→4→8→16→30s`、到点 `failRequest` 按客户端协议回 502 并带上游错误原文。③ 顺带按 #375 区分确定性错误：`400/405/406/413/414/415/422/501`＝请求本身非法（不计成员失败、不打冷却、记入本请求拒绝集合后换人，全部拒绝才终结），`401/402/403/404/407`＝成员自身问题（记 1 次失败并立即按尝试用尽冷却换人），其余保持可恢复语义。**嵌套分组修正**：拒绝集合比对改用展平成员列表（顶层项是子分组，主键与成员行不同，否则永不满足终结条件）；尝试计数提到处置分支之前，保证所有类别都被上限兜住。| 2026-09-17 11:40:00 |
| T-retry-002 | 重试语义活体套件 6 例 + 取消记账用例（I7）+ mock 确定性错误桩 | scripts/api-tests | done |
| | 备注：新增 `run_retry_test.py`（已注册矩阵，26 套件）：mock 新增 `mock-reject400/mock-reject401`（名字或 `/__control` 覆盖即按状态码拒绝）。读数：R1 `502 / 5.0s / 上游 6 次（上限 6）`、R2 `502 / 1 次尝试 / 成员失败 0→0`、R3 `[reject400, mock-good] → 200 / 0→0`、R4 `[reject401, mock-good] → 200 / 0→1`（立即换人而不耗尽 2 次尝试）、R5 手动模式 `502 / 31.0s / 6 次`（修复前无限）、R6 `developer` 请求到上游变成 `["system","user"]`（读 mock 落盘正文核对，端到端验证 PR #360 移植）。`run_stream_idle_test.py` 新增 I7「客户端首字节后放弃不计成员失败」→ 成员失败计数 `1→1`（I2 服务端看门狗那次仍照旧 +1，构成同一二进制内的对照）。| 2026-09-17 11:40:00 |
| T-port-001 | 移植上游 PR #360：`developer` 角色归一化为 `system`（含同协议透传路径）| internal/relay | done |
| | 备注：本项目确认缺失（`git grep developer` 为空）。`normalizeDeveloperRole` 只对 chat completions 上游生效，Body/JSONBody 同步；上游 PR 只挂在跨协议 `conversionMiddleware` 上，本项目**额外**在同协议透传 `buildPassthroughRequest` 调用——否则「客户端 chat 入 + chat 上游出」这条最常见的路径仍然会把 `developer` 原样发给只认 `system` 的中转站。单测 3 例（归一化/其它协议与畸形正文不动/回退 JSONBody）+ 活体 R6。| 2026-09-17 11:40:00 |
| T-port-002 | 移植上游 PR #344：导入前过滤孤儿子行，避免一行残留让整份备份导入失败 | internal/op, internal/model | done |
| | 备注：本项目外键链与上游不同（没有独立 stats_channel/stats_model，统计并入 channel/channel_grants），按本项目的链适配：`channel_keys/channel_models → channels`、`channel_grants → channel_models/channel_keys`、`group_items → groups/channel_grants/子分组`、`stats_api_key → api_keys`。做法＝基础表就位后**按依赖顺序逐层过滤**，有效父键＝转储里剩下的 ∪ 库里已存在的（增量导入语义），丢弃数量写进导入结果新字段 `Skipped`（`model.DBImportResult`），不静默。踩坑：第一版按「转储里的全部父键」判父，被测试当场抓住（`channel_grants` 该丢 2 行却丢 0 行、`group_items` 该丢 3 行却丢 2 行）——孤儿父行自己会被丢掉，子行必须按**过滤后**的父键集合判定。单测 2 例（只丢插不进去的行 / 库内已存在的父行仍然有效）。| 2026-09-17 11:40:00 |
| T-grant-001 | 修复真实缺陷：模型通讯协议「全选」会给不支持该模型的密钥建授权（上游 #387，本项目同源）| web/src/components/modules/channel | done |
| | 备注：上游 #387（JmPotato，2026-09-09）报告的形态与本项目代码事实一致——`FormGrants.tsx` 的三级授权格由同一个 `GrantCells` 渲染，三态统计与写入都是 `models × keyNames` 全量置位，完全不看某个凭据是否供得了该模型；而「每个凭据支持哪些模型」这份知识本项目**只有探测的返回值里有**（`probe.ts` 拿到后只并入 models 与 grants，用完即弃），故界面此前无从判断。危害不是显示问题而是派发问题：授权表就是转发时的派发依据，存下「model-a 可用凭据 B」就会把请求派给供不了 model-a 的凭据、在上游失败。修法＝新增零依赖纯函数 `grants.ts`（`isPairSupported`/`supportedKeyNames`/`unsupportedPairs`/`recordProbe`/`declareModel`），表单状态增加 `keyModels`（**只收敛授权范围，不进提交载荷**；编辑既有渠道时为空，因为探测结论不随渠道保存），五处落地：探测成功后写入支持清单、`GrantCells` 统计与写入都跳过不可用组合、模型行分母改成可用凭据数、提交前 `pruneUnsupportedGrants` 兜住探测前就已存在的旧组合并用 toast 报条数（不静默改动既有配置）、手填模型走 `declareModel` 人工声明（否则刚加上就会被自己的实测结论锁死）。**口径**：没有探测记录的凭据一律放行（不拿"没探到"当"不支持"，否则会误伤手填模型与未探测凭据）；模型名原样保留不裁剪空白（裁剪会造成"探到了却判不支持"）。| 2026-09-17 12:55:00 |
| T-grant-002 | 授权收敛纯函数套件 26 例 + 面板产物标记 P8 | scripts/api-tests | done |
| | 备注：新增 `check_grants.js`（矩阵第 27 套，`node,esbuild`）：由 `grants-entry.ts` 把界面真正跑的两条链路（`grants.ts` 判定 + `state.ts` 裁剪与提交载荷）用 esbuild 打成单个 cjs（`run_all.py` 新增 `build_grants_cjs`），node 直接跑、无运行期依赖。读数 26/26：未知凭据放行；按凭据隔离（A 供 model-a / B 不供 model-a / A 不供 model-b / B 供 model-b）；`unsupportedPairs` 恰好给出 `[["model-a","B"],["model-b","A"]]`；四条被污染授权裁掉 2 条且合法组合协议位不动；**未探测凭据的授权不被裁**；`toChannelDetail` 载荷里只剩两条有效授权（"保存时不落无效授权"的直接证据）；取消勾选后仍判可用、可再勾；空探测不改判、名字不裁剪。踩坑：esbuild 少了 `--bundle` 会把 `require('../../web/src/...')` 原样留进产物（node 报 Cannot find module），多入口 + `--out-extension` 则会让产物之间的 require 与文件名不一致——最终用单入口单文件。面板加 **P8**（`grantUnsupported` 键被引用 + 三语文案）→ 8/8，证明改动真的进了随二进制发布的产物。| 2026-09-17 12:55:00 |
| T-upstream-002 | 上游剩下三条报告（#363/#372/#373）与落后上游 3 个提交的逐条核对 | docs | done |
| | 备注：三条此前判为"含糊"的报告读完并给出可复核结论：**#363**（chat→message 转换后没有提示缓存）同源存在但属转换能力边界，维护者处理意见是"chat 已被官方淘汰、重点是透传"，本项目同样支持同协议透传故建议一致，要"转换路径也有缓存"须由本项目合成 `cache_control` 断点（新功能，未做）；**#372**（客户端 message + 上游 message 报 401）不是缺陷——该上游 message 接口用非标准的 `Authorization` 头（标准是 `x-api-key`），绕过手段「自定义请求头」在本项目**代码核实可用**：自定义头在 `applyChannelConfig` 注入且位于 `FinalizeAuthHeaders` 之后（`protocol.go:48→52`），敏感头仅在"已被转换器写入"时拒绝覆盖（`internal/relay/channel.go:89-99`），anthropic 路径未写入 `Authorization` 故允许自定义；**#373**（流式中途故障的下放 + 后两个模型没参与循环）两条都不是缺陷——首帧已提交则无法续传流式，成员冷却期内被跳过是设计行为。落后上游的 3 个提交均为**功能/样式**而非修复：44f63cb 手动取消请求（含路由改名 `/stop/:id[/:round]`、`/request-body/:id`）、00e0739 token speed、08d80c3 Logo，**均未移植**（44f63cb 附带的"取消与上游成功同时到达则不提交"守卫，本项目的等价保护是 ctx 即客户端上下文、且记账已按提交与否分开处理）。| 2026-09-17 13:35:00 |
| T-test-018 | 凭据隔离活体断言 R7（客户端 API Key 不得透传给上游）| scripts/api-tests | done |
| | 备注：`run_retry_test.py` 新增 R7——上游 #372 的同类头部处理值得有实测证据而不是只读代码。做法：mock 只落盘凭据的哈希前缀（`mock_upstream._redact` = 类型前缀 + sha256 前 12 位），故按同一算法比对哈希，断言上游收到的那一条请求**携带渠道密钥**且**不含客户端凭据**（全程不落凭据原文，符合"测试装置不持久化凭据"的既有纪律）。读数 7/7 通过：「上游请求 1 条, 携带渠道密钥 1 条, 携带客户端凭据 0 条」。踩坑：`rows_since` 需要完整行（含头部字段）而既有 `attempts_since` 只回模型名，新增了前者。| 2026-09-17 13:35:00 |
| T-test-019 | 凭据隔离活体断言: 10 条协议路径上客户端凭据不落到上游 | scripts/api-tests | done |
| | 备注：`run_lossless_tests.py` 新增聚合断言「凭据隔离[10 条协议路径]」= 3 客户端协议 × 3 上游协议位 + 1 条流式（`messages→chat` SSE）。做法：按 `mock_upstream._redact` 的同款算法（`bearer:`/`key:` + sha256 前 12 位）比对哈希，断言窗口内**每一条**上游请求都携带渠道密钥、且一条都不含客户端凭据（不落凭据原文）。读数：「上游请求 10 条, 携带渠道密钥 10 条, 携带客户端凭据 0 条」（lossless 14/14）。意义：mock 上游不校验密钥，凭据带错也不会让功能用例变红——这条是「凭据不外泄」唯一的直接证据；与上轮 R7（chat 同协议透传）互补，覆盖跨协议转换路径。窗口允许并发请求，故条条合规即可，不强求恰好 10 条。| 2026-09-17 13:56:34 |
| T-test-020 | 上游连接生命周期: 客户端放弃即中止上游（I8/I9 反向对照）| scripts/api-tests | done |
| | 备注：`mock_upstream.py` 静默分支改为 `_stall_watch_peer()`（`select` + `recv` 盯这条上游连接），对端一关就记事件行 `stall_peer_closed{closed_after}`（等满静默上限记 null）；事件行不带 `method`/`model`，故不计入各套件的「上游尝试次数」，`attempts_since` 也显式跳过事件行。`run_stream_idle_test.py` 新增 I8（客户端立即放弃 → 上游连接 0.0 秒关闭）与 I9（客户端等满 8 秒，上游连接 8.01 秒才关闭＝客户端在等时不会提前关上游）。三档关闭时刻出自同一次运行：2.0 秒＝服务端无进展上限（idle=2）、8.01 秒＝客户端放弃、0.0 秒＝客户端立即断开；I9 是 I8 的反向对照（若关闭是假象，I9 会看到小于 7 秒的提前关闭）。I8/I9 都用 `idle=0` 的分组（看门狗关闭），故关闭只能归因于客户端断开。读数 streamidle 9/9。| 2026-09-17 13:56:34 |
| | 补充（同日）：凭据判据从「两个凭据列」扩到「任意位置」——① `mock_upstream.py` 每行新增 `headers` 字段（**所有**请求头的小写名 → 值指纹，同一请求 7 条：host/user-agent/content-length/accept/authorization/content-type/accept-encoding），自定义头由此进入证据范围；② 判据加「原文兜底」：上游收到的这一行里出现客户端凭据原文也算泄露（正文、查询串等未单独建列的位置一并盖住）；③ 新增**判据自检（负向对照）**：伪造 4 行（1 合规 + 自定义头泄露 + 正文泄露 + 1 事件行）跑同一判据，要求恰好报出那两条泄露 ——读数 `total=3 carried=3 leaked=['self-leak-header','self-leak-body']`，防止判据日后被改瞎了还一路绿。读数：lossless **15/15**（凭据隔离 10/10 命中渠道密钥、客户端凭据 0 条）、retry **7/7**（R7 同款判据）、完整矩阵 **27 套 0 失败**。仍未覆盖：TLS 层信道、以及凭据被改写形态（如 base64）的情形。| 2026-09-17 14:41:41 |
| T-test-021 | 判据灵敏度（变异检查）：六处守卫改回旧行为/改值后指定用例必须变红 | docs/worklog/2026-09-17-判据灵敏度（变异检查）.md | done | 2026-09-17 15:50:16 |
| | 备注：M1 取消记账 → streamidle I7；M2 尝试上限 6→8 → retry R1；M3 成员故障分类 → R4；M4 转换路径 developer 归一化 → **R9**；M6 同协议直通 developer 归一化 → R6；M5 全成员被拒恒 false → R2 —— 六条全部「如期变红」，还原后 streamidle 9/9、retry 9/9 全绿。变异期间 ServerChan 指向本地 mock，避免坏构建误推告警。工具：%TEMP%\octopus-dstest\mutation_check.py（逐条改/跑/还原）与 run_with_mutation.py（单条变异 + 任意命令）。 | 2026-09-17 15:50:16 |
| T-devrole-001 | 修 Responses 形状上游正文里的 developer 角色（上游 #360 移植的漏网路径） | internal/relay/upstream.go | done | 2026-09-17 15:50:16 |
| | 备注：变异检查发现「注释掉转换中间件的 normalizeDeveloperRole 后 R8 仍绿」，做分协议位探针（客户端 chat + developer → 成员只说 chat / responses / messages）后确认 **Responses 形状上游仍收到 developer**（角色在顶层 input 数组里, 原实现只认 chat 的 messages 数组）；修复为按上游协议选路径并逐项判 role, input 为字符串/空数组/条目无 role 时安全跳过；新增单测 TestNormalizeDeveloperRoleOnResponsesInput、TestNormalizeDeveloperRoleOnResponsesEdgeShapes 与活体用例 R9（上游路径 /v1/responses、developer 残留 False、文本保留 True）。读数：go vet 0、go test 12 包全绿、矩阵 27 套 0 失败（retry 9/9、streamidle 9/9）。 | 2026-09-17 15:50:16 |
| T-devrole-002 | developer 角色契约进 3×3 无损矩阵：每条「客户端协议 × 上游协议位」路径都看上游角色 | scripts/api-tests/run_lossless_tests.py | done | 2026-09-17 16:04:22 |
| | 备注：上一条缺陷（Responses 形状漏 developer）能溜过去是因为没有用例逐个路径看角色 —— 矩阵此前只断言标记文本在不在。本轮补两条按客户端协议分组的聚合判据（chat / responses 客户端 × 3 上游协议位), 读数 6 条路径全部无 developer 残留且指令文本保留（lossless 17/17)；并加 M7（注释 protocol.go 直通钩子）/M8（注释 upstream.go 转换钩子）两处变异确认判据不空转, 两条均如期变红。 | 2026-09-17 16:04:22 |
| T-smart-001 | 智能路由后端：按请求特征的复杂度评分与两档选路（含档内选路 + 档空回退） | internal/relay/smart.go | done | 2026-09-17 17:35:59 |
| | 备注：参考实现=[Step Router V1](https://platform.stepfun.com/docs/zh/guides/models/step-router)（用户 2026-09-17 指定）。三项特征（消息轮数 / 输入量 / 工具数量）等权归一到 0..100，阈值 `relay_config.smart_route_threshold`（1..100，缺省 50，越界夹回）；档位=成员顺序（复杂取靠前的一半、奇数多出来的归决策档）；档内沿用既有选路，档内无人可转发时**回退全体成员**。单测 `internal/relay/smart_test.go` 7/7（评分单调/上限、特征提取与非法正文、阈值边界与越界、切分 1/2/3/4/5 成员、冷却回退、说明文案、模式登记与 JSON 字段名/默认值/夹取）。 | 2026-09-17 17:35:59 |
| T-smart-002 | 智能路由活体套件（同一分组只改请求形状的差分对照 + 阈值两侧 + 档不可用回退） | scripts/api-tests/run_smart_route_test.py | done | 2026-09-17 17:35:59 |
| | 备注：8/8 通过 —— S1 简单→执行档、S2 复杂→决策档、S3 灵敏度阶梯（单项 33 分不够、任意两项 ≥66 分够）、S4 阈值 70/60 两侧翻转、S5 决策档上游必失败时回退到执行档且 HTTP 200、S6 流式同样分档、S7 切回 failover 后不看请求形状。已注册进 run_all.py（矩阵第 28 套）。夹具用两个渠道（强=mock-good / 快=mock-plain）+ 一个必失败渠道（mock-bad）驱动冷却。 | 2026-09-17 17:35:59 |
| T-smart-003 | 智能路由进面板：模式选项 + 阈值输入（仅 smart 模式显示）+ 三语文案 + 面板套件 P9 | web/src/components/modules/group/Editor.tsx | done | 2026-09-17 17:35:59 |
| | 备注：按「加模式要同步的地方」逐处落地 —— 常量/IsValid/三处 binding oneof（后端）、GroupMode 联合类型、Editor 下拉项与 `smart_route_threshold` 输入、三语 `form.smart`/`smartThreshold`/`smartThresholdHint`；`npx tsc --noEmit` 0、面板套件 9/9（新增 P9 断言模式文案与阈值字段真的进了产物）。 | 2026-09-17 17:35:59 |
| T-smart-004 | 首字竞速不越档：竞速候选在智能路由下收敛到选中那一档 | internal/relay/hedge.go | done | 2026-09-17 18:30:11 |
| | 备注：竞速候选原先走 hedge.go 自己的模式分派, smart 落默认分支会把全体成员当候选, 简单请求因此可能把强成员拉进竞速（成本控制被绕过）。新增 rankedHedgeCandidatesWithFeatures：档内竞速（failover 口径）+ 档内无人时与选路一致回退全体；其余模式逐字不变。单测 TestRankedHedgeCandidatesStaysInsideTier + 活体 S8（简单请求尝试 ['mock-plain','mock-plain']、复杂请求 ['mock-good','mock-good']）。 | 2026-09-17 18:30:11 |
| T-smart-005 | 智能路由判据的变异检查（档位切分/评分维度/回退/竞速收敛四处守卫） | internal/relay/smart.go | done | 2026-09-17 18:30:11 |
| | 备注：SM1 严格执行档返回全体 → S1 红（简单请求命中 mock-good）；SM2 工具维不计分 → S3 红（两条依赖工具数的阶梯掉到执行档）；SM3 去掉回退 → S5 红（复杂请求 HTTP 502、不再回退到执行档）；SM4 竞速不收敛档位 → S8 红（复杂请求尝试里出现 mock-plain）。四条均如期变红, 跑完自动还原重建并复跑 streamidle 9/9、retry 9/9 全绿。 | 2026-09-17 18:30:11 |
| T-smart-006 | 智能路由档位按顶层成员切分（子分组整条链归档，不被从中间切开） | internal/relay/smart.go | done | 2026-09-17 19:24:01 |
| | 备注：上一版按展平后的成员条数对半切, 链长不等时会把执行链成员算进决策档（复杂请求失败后会滑到便宜链）且简单请求丢掉执行链首选项（回退到全体成员时落到强渠道 = 成本泄漏）。修复：op 新增 FlattenGroupItemsWithTopCounts（与 FlattenGroupItems 同一遍历实现）, relay 新增 SmartTierTopCount / SmartDecisionMembers / smartTierItems 与 SmartRoute 值类型, 选路与竞速两个入口都改收它。判据 S9 第一版装置判不出来（变异 SM5 全绿）, 重做为「执行链 3 名且首位快成员 + 后两名必失败」后, 变异 SM5 如期变红（简单请求尝试 ['mock-bad','mock-bad','mock-good'] 而正确实现是 ['mock-plain']）。 | 2026-09-17 19:24:01 |
| T-log-005 | 日志记录上游轮次 attempts（排障：这次请求换没换人） | internal/model/log.go | done | 2026-09-17 21:06:50 |
| T-balance-001 | 总余额聚合（渠道余额/剩余次数/Key 额度折算成总额，标准协议可查） | 需求 | done | - | 2026-09-17 22:39:58 | 新增 internal/model/balance.go（形状 + 归一化/折算）、internal/op/balance.go（聚合：读 ChannelBalance 内存快照 / 包月额度 / Key 统计；未知余额只计数）、internal/server/handlers/balance.go（转发口 /v1/dashboard/billing/subscription|usage + 自有 /v1/balance，APIKeyAuth）、internal/server/handlers/balance_admin.go（管理口 /api/v1/balance/summary，middleware.Auth）、设置项 balance_points_per_unit(默认 500000)/balance_currency，面板首页总余额卡片 + web/src/api/balance.ts + 三语文案。口径：hard_limit_usd=总额+本 Key 已花、total_usage(分)/100=本 Key 已花 ⇒ 客户端减法正好是总余额；未知余额不计入总额（否则把「还没扫到」显示成「没钱了」）；Key 的 MaxCost 是「允许花多少」，不进上游总额。读数：单测 4 例；活体套件 scripts/api-tests/run_balance_test.py 14/14（矩阵第 29 套）：B2b 380 点/100000=0.0038、B2c total=0.0038 known=1 unknown=23、B3b/B3c 坏口径 400、B5 等式成立、B6 无 Key 401；面板 P11 11/11。第一版管理口漏挂 middleware.Auth，被 B1（匿名读到 200）当场抓出后补上。
| T-balance-002 | 总余额判据灵敏度（变异检查：折算口径 / 管理面鉴权） | 测试 | done | - | 2026-09-17 22:54:19 | BAL1 把折算口径写死成默认 500000 → B2b 如期变红（balance=0.00076，应为 0.0038）；BAL2 把管理面 Use(middleware.Auth()) 换成 Use(middleware.RequireJSON()) → B1 如期变红（匿名读回 HTTP 200，应为 401）。两次装置修正记在 worklog《总余额聚合（标准协议余额查询）》的「判据灵敏度」一节：① BAL1 首轮期望写成 B3a，而 B3a 的期望值恰好等于默认口径下的折算，真正抓到变异的是 B2b —— 变异不变红先查是不是撞上另一条口径；② BAL2 首轮用「注释掉中间件」，middleware 导入变成未使用导致编译失败（装置不成立），改成替换成同包另一个已导出的中间件才测到鉴权本身。还原后干净重建 + streamidle 9/9、retry 10/10 全绿。 |
| T-decision-001 | 判定理由写进响应头与日志（这次为什么走了这个成员） | internal/relay/decision.go | done | - | 2026-09-18 02:02:44 | 新增 internal/relay/decision.go: Decision{Mode,Tier,Reason,Slot,Attempt} + Text()（三处出口共用同一份文本）+ DescribeDecision（只读取路由状态副本）+ DecisionTier（档位只属于 smart）+ TopSlot（成员在分组里的顶层序号，子分组整条链归档）；handler.go 的 publishDecision 在每轮选路后与竞速胜出后刷新响应头 X-Octopus-Route（首字节前可覆盖）与 RequestState.Decision；relay_logs 新增 decision 字段、导出新增「判定理由」列（错误列 16→17，单测与审计套件同步）；导出文本不含渠道名与凭据。reason 口径＝谁决定了这次选择: manual/affinity/probe/priority/ranked。单测 5 例（含只读保证）；活体 R11 断言响应头与日志逐字一致且 slot=2 指向真正服务的成员。 |
| T-retry-003 | 请求本身非法(400 类)时的取向可切换: failover(默认) / failfast | internal/relay/retry.go | done | - | 2026-09-18 02:02:44 | 设置项 relay_request_fault_action（model/setting.go 键+默认 failover+Validate 只接受两值），装配层启动(cmd/start.go)与设置变更(handlers/setting.go)两处注入；relay 内部包内原子量，非法值回落 failover；failfast 时第一个成员被拒即结束（仍回上游原文），failover 保持第 8 回合语义。活体 R12 同二进制差分: failfast → 502/上游 1 次尝试；复原 failover → 200/[reject400, good]。单测 TestRequestFaultActionOnlyTwoValues。 |
| T-migrate-002 | 存量分组统一回填流式无进展上限（迁移 014） | internal/db/migrate/014.go | done | - | 2026-09-18 02:02:44 | member_stream_idle_timeout_seconds 是后加字段，存量分组 JSON 里没有该键 ⇒ 反序列化为 0 ⇒ 语义正好是「关闭保护」，升级后老分组仍会挂死。迁移 014（After 阶段，只 UPDATE 行不碰表）: 键缺失或 ≤0 → 300；已是正数保留；其它字段逐字不动；解析不了的行跳过。活体（真实库 388 个分组）: 迁移前 362 个缺键/0 → 迁移后 388 个全为 300、0 个缺键。单测 2 例（五类行的取舍 + 重跑幂等）。 |
| T-test-022 | 本轮三条守卫的判据灵敏度（DC1/DC2） | 测试 | done | - | 2026-09-18 02:22:02 | DC1 判定理由不写响应头（只留日志）→ R11 如期变红（响应头为空而日志仍在, 证明两个出口是独立的）; DC2 取向开关失效（取值判断恒真）→ R12 如期变红（failfast 分支退回 failover: 200/两次尝试）。还原后 streamidle 9/9、retry 12/12 全绿。 |
| T-decision-002 | 面板侧判定理由字段与取向开关设置界面 | web/src/components/modules/log | done | - | 2026-09-18 02:39:51 | 日志卡片新增 decision 字段（api/log.ts 的 RelayHistoryItem/RelayLogOverview 加 decision；display.ts 直接透传后端同一份文本；store.ts 字段名+出厂可见；FilterBar.tsx 开关；Item.tsx 单元格用 Route 图标截断显示）；设置页新增 relay_request_fault_action 的 select（复用 WeightSettingField，两选项 failover/failfast）；三语 locales 加 log.filter.decision 与 setting.routing.requestFault*。判据：tsc --noEmit 0、eslint 改动文件无新增问题（FilterBar 的 react-refresh 报错为既有）、panel 套件新增 P12 → 12/12、矩阵 29 套 0 失败。 |
| T-pool-004 | 号池管理选型报告补全（R-pool-003 全文） | docs/requirements/2026-09-13-号池管理选型.md | done | - | 2026-09-18 02:39:51 | 用户裁决「我全都要」后把草稿展开为全文（四节口径）：① 候选清单 6 个真实项目 + 统一字段（路线/登录协议/额度可见性/封号风险/维护活跃度/与本项目关系），其中逆向类与批量注册类两条一票否决并写明红线判据；② 合规红线（官方 OAuth、密文落库、日志/导出/响应头无凭据、不接逆向与批量注册）；③ 并入映射逐项落到 Channel/ChannelKey/ChannelModel/ChannelGrant 四件套 + pool.Kinds/Entries/Snapshot + 余额与探活接线 + 面板，点明唯一缺口＝会话级粘性（opencodex 思路，需先定会话标识来源，未做）；④ 推荐优先级 OpenAI→Gemini→Claude + 第三方只作只读适配对象，并与 ⑩ 的四个边界对接。来源 6 条 URL 全部标注。 |
| T-pool-005 | 运行时声明式适配器（白名单/只读能力位/不跟跳转/凭据不回显） | internal/pool, internal/poolstore, internal/server/handlers | done | - | 2026-09-18 03:02:08 | 一份 JSON 接入外部反代工具包：internal/pool/declarative.go（DeclarativeSpec/Auth/List + 五条硬约束：域名白名单 pool_declarative_hosts 为空即一律拒绝、只允许 list/get、不跟 3xx 跳转、超时 10s 与响应体 1MiB 上限、kind 必须 custom- 前缀）+ pool.go 的 RegisterRuntime/UnregisterRuntime/BuiltinKind（运行时适配器可替换可移除，内置 kind 既不可覆盖也不可移除）+ internal/poolstore（读设置→解密→校验→注册的编排层，避开 pool↔op 导入环）+ internal/op/pool_adapter.go（整份 spec 的 AES-256-GCM 封装，AAD 固定 pool:declarative，缺密钥拒绝注册）+ model/setting.go 两个设置项与校验 + cmd/start.go 启动装载（单条失效只记日志）+ setting 接口改白名单即时生效。判据：单测 9 例；活体 run_pool_declarative_test.py 9/9（D1 fail closed / D2 白名单差分 / D3 只读能力位 / D4 sync→400 / D5 白名单外→403 / D6 真读到外部清单 / D7 移除后可再注册 / D8 只读通道 / D9 凭据不回显）。 |
| T-pool-006 | 号池只读 APIKey 通道（转发口 /v1/pool/*） | internal/server/handlers/pool_apikey.go | done | - | 2026-09-18 03:02:08 | 外部工具包不必再存一套管理员口令：转发口 + middleware.APIKeyAuth 暴露 GET /v1/pool/{kinds,entries,stats,summary}，与管理面同源同口径；只有只读动词（写动作在该通道上不存在，实测 404）；匿名 401。判据：D8。 |
| T-deploy-004 | 发布件重建（含手动订阅）与全新安装冒烟 | %TEMP% 外发件, internal/db/fresh_install_test.go | done | - | 2026-09-18 05:35:00 | ① 上一轮发布件（`a491300`）不含第 23 回合的手动订阅，拿它上线会少一个功能，因此按交付时 HEAD 重建；**不改版本号**（v0.14.0 从未上线、没被取走）。**校验值与提交号只记在随件**（`SHA256SUMS.txt` + `build-record.txt`，与二进制同行，verify.sh 就地读）：git 文档一改提交号就变，两边永远追不上（本轮实测踩过这个循环）。② 新功能真的进二进制：Go 侧字串直接命中；面板侧 `//go:embed all:out` 与 `static/out` 字节级比对 **45/45 命中**，其中 `home-*.js.gz` 解压含「手录合计」与 `/api/v1/subscription/create`（前端 gzip 内嵌，纯文本搜索查不到）。③ WSL 空数据目录全新安装：22 张表齐全（含 manual_subscriptions 列齐全）、横幅 v0.14.0、加密已启用、建渠道→建分组→真转发 200、手录订阅 total=2/manual_total=2/来源 manual；再把凭据改回明文重启 → 「补加密：1 行明文已转为密文」sealed=1/1；自带 verify.sh 五项全 PASS。④ 补常驻判据 `internal/db/fresh_install_test.go`：空库跑完整 InitDB → 22 张表/20 个模型，变异（去掉 ManualSubscription 登记）立即红并点名缺表。 | 2026-09-18 05:35:00 |
| T-acct-005 | 手动订阅独立表（无接口站点的余额来源，R-acct-004 余项） | internal/model/manual_subscription.go, internal/op/manual_subscription.go, internal/op/balance.go, internal/server/handlers/subscription.go, internal/db/db.go, web/src/api/subscription.ts, web/src/components/modules/home/manual-subscription.tsx | done | - | 2026-09-18 05:10:00 | 上游没有余额接口时由人录一条套餐余额与有效期，按与自动读数**同一口径**（balance_points_per_unit，可按条覆盖 points_per_unit）折算后并入总余额。为什么独立表（指导口径的落地）：渠道凭据是「能连上游的秘密」要加密要轮换，手动订阅是「我在哪买了套餐」的采购信息，两者塞一张表会让「备份里哪些字段是敏感面、哪些值要跟着轮换」同时失去边界。三条入账规则：绑定渠道且该渠道无自动读数 → 该渠道由未知变已知（balance_source=manual）并计入；绑定渠道但已有自动读数 → 不计（同一笔钱不能算两遍）；停用/已过期 → 不计但明细照列（面板提醒续费）；不绑渠道的通用额度 → 计入。每条明细带 counted，面板只显示后端判断、不重复实现折算。判据：单测 5 例（校验/天数/入账与三种不计/行级口径/未知渠道拒绝）；活体 run_manual_subscription_test.py 9/9（M1 匿名 401、M2 来源 manual 且 balance=2、M3 重复不计 counted=False、M4 过期停用不计、M5 四类脏数据 400、M6 差额自检 2.0000→3.0000、M8 列表接口、M7 转发仍 200）；panel P14；矩阵 32 套 0 失败。 | 2026-09-18 05:10:00 |
| T-deploy-003 | v0.14.0 发布件与上线运行手册（Ubuntu 单二进制） | docs/deploy, main.go, %TEMP% 外发件 | done | - | 2026-09-18 04:30:00 | ① 版本号 v0.13.4 → v0.14.0（main.go 的 `// Version`，发布件启动横幅与 `octopus version` 都能看到）。② 交叉编译产出 linux/amd64 单二进制（前端已内嵌，54 个 \*.js.gz），用 `-ldflags` 注入版本/提交/构建时间，附 SHA256SUMS。③ 交付三份文档：上线运行手册（备份→key 轮换→替换→启动检查→回滚→已知运营项）、key 轮换清单（T-sec-001 的可执行版）、systemd 单元模板。④ 发布件自带 verify.sh：校验和/版本/实例状态/加密启用/管理面拒匿名/真实转发/库内密文形态，任一项不满足即非零退出。 | 2026-09-18 04:30:00 |
| T-sec-002 | 渠道凭据静态加密（库内密文、进程内明文） | internal/secret/secret.go, internal/op/credential_cipher.go, internal/op/channel.go, internal/op/official_pool.go, internal/db/migrate/015.go, cmd/start.go | done | - | 2026-09-18 04:20:00 | R-sec-001 余项：channel_keys.key 一直是明文落库，数据库文件或备份被拿走就等于上游凭据泄露。口径＝库内密文、进程内明文，收口在缓存层（读：channelRefreshCache/reloadChannelChildren/officialPoolKeys 统一 DecryptRows；写：syncChannelKeys 与官方账号物化统一 sealChannelKeyForStore；存量行：迁移 015 + 启动补加密，幂等只处理无前缀行），因此转发链路一行未改。密文形状 enc:v1: + base64(nonce‖ct)，AAD 固定 credential:channel-key；带前缀才是密文、无前缀按明文（存量行不失效）；解密失败一律报错（绝不把密文当 API key 发出去）；真无密钥时写入退回明文只告警一次、读取侧仍报错。密钥来源两个都做：OCTOPUS_OFFICIAL_KEY（与官方账号同一把）优先，其次数据库同目录的 credential.key（首次自动生成，Unix 0600，启动日志打印路径）。撞到三个坑：① 比较必须明文对明文（否则每次保存都重新加密、且判断不出是否改过）；② migrate 不能 import op（op→db 成环），故加解密原语下沉到 internal/secret；③ Seal 必须幂等（对已加密值原样返回）、空串保持空串。判据：单测 13 例（secret 6 / op 3 / migrate 3 + 边界）；活体 run_credential_encryption_test.py 6/6；矩阵 31 套 0 失败（所有渠道夹具经 API 新建即密文，等于一次大范围解密回归）。 | 2026-09-18 04:20:00 |
| T-smart-007 | 智能路由的显式档位（决策引擎渠道由用户指定） | internal/model/group.go, internal/op/group.go, internal/relay/smart.go, web/src/components/modules/group/* | done | - | 2026-09-18 05:10:00 | 此前只能靠成员顺序分档（前半决策档、后半执行档），而顺序同时决定 failover 切换顺序与界面展示，两件事被绑在一起。本轮加 GroupItem.SmartTier（decision/execution，留空=按顺序自动切分）+ 三条规则：无声明逐字保持旧口径；有声明则声明 decision 的进决策档、其余（含未声明）进执行档；目标档为空回退全体（标注问题不升级成故障）。档位是顶层成员粒度并随展平下传给整条链（内层声明优先）——否则标在子分组上等于没标。堵掉三个静默丢字段的坑：保存路径是重建而非透传（GroupItemInput + 两处构造点都要带）、卡片提交条件（只提交变化过的成员集合）必须把档位算进差异、拖拽提交另一条载荷路径（两条路径共用 memberPayload）。判据：单测 4 例（含显式清空档位）；活体 S10（装置里成员顺序与档位刻意相反，两个方向都与顺序口径相反）+ S10c（只改「有没有档位」就翻转，同二进制差分对照）+ S10d（显式档位 × 首字竞速）→ 套件 15/15；面板 P13 13/13；矩阵 30 套 0 失败；变异 SM6（忽略声明）→ S10a 红。 |
| T-pool-007 | 声明式适配器守卫的变异检查 | internal/pool/declarative.go | done | - | 2026-09-18 03:02:08 | MD2（放开写能力位）→ D4 红；MD3（hostAllowed 恒真）→ D5 红；MD1c（同时破坏两层 fail-closed 防线）→ D1 红。MD1/MD1b 两次单点变异**不变红**并已查明原因：fail-closed 是双重保障（显式分支 + 空名单匹配不上任何主机），单点破坏行为不变——这是「防线有两层」的正常形态，不是判据失效；要把「默认放行」做成红测必须同时破坏两层。 |

| | 备注：上游 #395「重试详情」的最小切片（维护者明确不做完整重试详情，本项目只补一个数）。RelayLog.Attempts ← RequestState.Round（首字竞速多路算一轮，与面板同口径）；导出加「上游轮次」列（错误列后移）；面板 display/store/FilterBar/Item + 三语文案 attempts。判据：retry R10（耗尽上限=6 / 换人成功=2）、panel P10、导出单测（表头与值）与 export 审计套件（11/11）。首字响应（TTFT）本轮核对为**已有**（FirstByteMs + 卡片「首字时间」+ 导出「首字节(ms)」+ 筛选栏）。 | 2026-09-17 21:06:50 |

| | 备注：T-smart-007 的口径边界 —— 档位只在 mode=smart 下被读，其它模式标了也不影响行为；「目标档为空回退全体」与既有「档内选不出人回退全体」是两条不同的兜底，前者管「标注与请求对不上」，后者管「档内成员都不可用」。 | 2026-09-18 05:10:00 |

| | 备注：T-sec-002 的两个后果已写进启动日志与 worklog —— credential.key 丢失等于已加密凭据解不开（面板可重填，没有别的办法找回），请与数据备份一起保存；降级老版本二进制会把密文当 API key 发上游（表现为 401），回退前需在面板把凭据改回明文。本轮**未做**：备份转储的凭据脱敏（R-sec-001 原文的「导出默认脱敏」）—— 备份里现在存的是密文，可恢复性优先，脱敏是另一件事。 | 2026-09-18 04:20:00 |

| | 备注：T-deploy-003 的验证是在 **WSL（Ubuntu）里跑真二进制**，不是只做构建：① 能起、横幅 `Version: v0.14.0`、加密状态行 `密钥来源：OCTOPUS_OFFICIAL_KEY`；② 面板 `GET /` 200（4337 字节）；管理面匿名 401 / 登录后 200；③ 新建渠道 → 库内 `enc:v1:…`，sealed=1 leaked=0；④ **升级演练**：把库内凭据改回明文（模拟 v0.13.4 旧库）→ 重启 → 日志 `渠道凭据补加密：1 行明文已转为密文`，sealed=1 leaked=0；⑤ 转发口匿名 401；⑥ 自带 verify.sh 五项全 PASS（退出码 0）。**T-deploy-002（真上线）仍未开工**：硬前置是用户侧动作（key 轮换 + 上线拍板），而且产品内置更新器写死了上游仓库 `bestruirui/octopus`（internal/update/update.go），所以本发布件不会被自动更新拉到，必须人工替换二进制。 | 2026-09-18 04:30:00 |


| | 备注：T-deploy-004 的第二条教训 —— **校验值/提交号属于产物，不属于文档**：本轮先写文档记录 sha，提交后 amend 又让提交号变了，二进制内嵌的提交号立刻对不上。正确做法是把这两个值放在随件里（`SHA256SUMS.txt`/`build-record.txt`），文档只说去哪看、不重复记录。 | 2026-09-18 05:35:00 |
| | 备注：T-deploy-004 的装置教训 —— WSL 冒烟脚本把设置表写成 `service_settings`（真实表名 `settings`），于是「全新安装缺表」是我自己的期望值错了，不是产品缺陷。**装置里的期望值同样必须来自代码事实（读 pragma 或模型清单），不能凭印象写**；这也正是把它固化成单测的原因：单测的模型清单直接引用 `model.*` 类型，不会写错表名。 | 2026-09-18 05:35:00 |
| | 备注：T-acct-005 的两个坑 —— ① 面板不调用 subscription/list（首页读余额快照里的 manual_subscriptions，单一数据源），该字面量被摇掉，拿它当 P14 产物标记会假红：**面板产物标记只能挑面板真的会调用/显示的东西**，接口存在性由活体套件证明；② 活体用例的「匿名」会在登录之后失去意义（cookie 会被带上），M1 必须排在登录之前。 | 2026-09-18 05:10:00 |
| T-deploy-005 | 一条命令升级与回滚（服务器侧脚本） | docs/deploy/upgrade-on-server.sh | done | - | 2026-09-18 06:10:00 | WSL 真演练全 PASS（旧版在跑 + 明文凭据 → 干跑 → 一条命令升级 → 坏包被拦 → 回滚）：备份含 credential.key、线上 sha == 发布件 sha、补加密 1 行、旧版 404 的新接口升级后 200、随件自检五项全绿、坏包 exit 1 且线上未变、回滚二进制与 data 整目录还原 |

| | 备注：T-deploy-005 的三条装置教训 —— ① 进程识别必须按 `/proc/<pid>/exe` 反查，手工部署的命令行是相对的（`cd /opt/octonexus && ./octopus start`），按字符串 pkill 一个都匹配不到，脚本会以为已停服 → 新旧实例抢端口挂死（第一版演练卡到 600s 超时就是这么来的）；② 版本解析必须锚定行首 `/^Version:/`，否则先命中 `Go Version: go1.27.0 linux/amd64`，把「版本」显示成 Go 版本；③ 回滚必须**整目录替换 `data/`**：只覆盖 `data.db` 时升级后产生的 `data.db-wal` 会被 SQLite 回放，密文又回来（演练实测）。 | 2026-09-18 06:10:00 |

| | 备注：`verify.sh` 新增第 5 个参数「安装目录」—— 在发布件目录里调它（升级脚本就是这么调的）时，库内凭据形态与启动日志必须去安装目录看；不传的话两项常年被跳过，而「跳过」在自检输出里长得很像「通过」。转发口也必须独立传参（把管理口当转发口用会得到 404）。 | 2026-09-18 06:10:00 |
