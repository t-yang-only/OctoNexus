# 认领：T-test-001 本地备份隔离测试（导入数据备份验证登录/渠道/分组/协议转化）

- 认领 Agent：cursor-local（NM-CUR-136）
- 认领时间：2026-09-13
- 登记台：`docs/agents/ledger.md` T-test-001（todo，无认领）
- 未选 T-pool-001（075/076 claim 在仓，30+ 行 owner 在途，本轮不抢）、T-quota-001/002 与 T-acct-001~002（均 doing 在途）、
  T-group-002/003（依赖 T-group-001 重新落地）、T-log-002（用户指派但日志路径待确认）、
  T-test-001 依赖仅本地备份文件且已有 067/072 隔离验收基础；按可独立交付领取。
- 来源：R-test-001（需求登记.md，用户原话）+ ledger T-test-001 登记语义。
- 范围（只测试，不改业务）：
  1. 确认备份文件存在且在仓外 `../octopus-本地数据/`，全程不读取、不提交、不贴任何密钥原文；
  2. 使用隔离端口启动 octopus，导入备份到隔离库（绝不连接生产库/生产端口）；
  3. 验证登录、渠道读取、分组读取、协议候选/路由形状（以只读接口和库内数据核对为主）；
  4. 测后停进程并删除隔离库，输出报告。
- 红线：不碰生产；不提交密钥/备份；不发真实上游请求；不修改 `reference/`。
- 验收：登录成功、渠道/分组计数与备份一致（或差异有解释）、协议字段可读且形状合法、临时产物清理。
