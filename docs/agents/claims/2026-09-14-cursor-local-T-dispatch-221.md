# 认领：T-dispatch-221 收口调度（关账四件套+八路增量指导）

- 认领 Agent：cursor-local（NM-CUR-221，2026-09-14）
- 用户指令：再次指导，分配新的任务，**尽快收口**。
- 范围：只读核验+ledger 收口口径+worklog 调度文；不动业务代码；不抢 L3（217）/L2（195）在途。

## 1. 终态核验（2026-09-14 约 15:00 实测）

- git：ahead 27，文档调度链 f0241dc→bb0dc51→10cb2e4→f19e415 四连。
- 工作树 WIP 14 文件：L4 acct/setting 面已落盘（`internal/model/setting.go`、`internal/op/setting.go`、`internal/db/db.go` 新增改动）→ L4 实施中；L3 quota-001 由 217 在途；group 前端 WIP 维持。
- 预览实例：`octopus.exe` 进程已退出（13399 down）——L9 未值守，预览生命周期断链（指导 §3）。
- 217/220 两行登记 started 但无日志节 → 收敛名单。

## 2. 关账四件套（本轮裁决，给执行路）

| 件 | 裁决 | 执行路 | 判据 |
|---|---|---|---|
| ① L1 归档 commit | deploy-001 唯一代码收口缺口；点名 add（backup.go/backup_raw_test.go/183 报告/exec-183 claim），禁 `git add .`（工作树混 L4 WIP） | L1 | commit 带 T-deploy-001 + `go test ./internal/op -count=1` 过 |
| ② 陈旧 started 行收敛 | 135/153dup/154/158×2/158-7c/159/160/165/170b/171/173/184/185/195/199/206/207/213/214/215/216/217/220/221 中：实质已收敛的批量关行（150 惯例），保留 217（L3 在途）唯一有效实施行 | 调度（本轮 + 下轮巡检复点） | 登记表无悬挂 started 行（除 217） |
| ③ group-003 拍板门 | 代码层三层终验已齐（171+184+214）；视觉验收预览实例已 down → 改判：**以代码层终验+API 闭环为准转 done**，视觉走查降级为 R2 观察项（不拦门）；ledger review→done 本轮执行 | 调度 | ledger 行 done + 备注视觉走查观察项 |
| ④ deploy-001 拍板 | 183 D1-D9 全绿 + L1 归档后，**建议 done**；用户不反对即视为过门（尽快收口授权已给） | 调度（L1 完成后） | ledger deploy-001 doing→done |

## 3. 八路增量指导（201/212/214 之上只讲增量）

- **L1（deploy 收尾）**：立即执行关账件①；完成后件④。
- **L2（group-003）**：195 关行；无需再做——件③已代为收口，后续只补 worklog 一行说明。
- **L3（quota 接线）**：217 继续跑，不拦；**新红线：setting.go/db.go 已被 L4 动，L3 不许碰这两文件**（文件锁表更新）。
- **L4（acct-001）**：WIP 已落盘（model/op setting+db），继续；commit 时一并登 API变更.md（若有新接口）。
- **L5/L6（relay）**：等 L4 commit 后再动 relay 包（handler.go/route.go 当前是 L4 之外的他人 WIP，合并顺序 L5→L6 不变）。
- **L7/L8**：草稿均已交付，无增量；L8 巡检频率降为每 3 小时一次（减少空转行）。
- **L0/L9**：预览实例已 down——L9 重启并值守（config 无 BOM 教训），L0 视觉验收降级不拦门（件③），用户想看时 L9 拉起即可。

## 4. 新任务分配（收口期两道）

| # | 任务 | 完成判据 |
|---|---|---|
| L10 | **R1 收口验证**：L1 归档 commit 后全量 build/vet/test -count=1 八包+tsc 实测，产出《R1 收口终验》一页（含 commit hash+八包绿证据+遗留清单），worklog 落盘 | 一页终验+全绿证据 |
| L11 | **登记表大扫除**：按 §2 件②名单执行批量关行+状态注记，产出收敛前后行数对照（预计 24 悬挂→3 有效） | 对照表+无悬挂行 |

## 5. 用户待办（缩减为 2 项）

1. ~~R-pool-003 二选一~~（草稿建议搁置，不反对即搁置）；
2. sec-001 key 轮换时机（deploy-002 上线门，唯一硬前置）。
