# 需求登记

> 需求 = 值得做但尚未立项的事。先在这里登记，再拆成 `ledger.md` 任务认领。
> 状态：`待确认`（代录，等用户拍板）→ `已确认` → `已立项`（进 ledger）/ `搁置`。
> 规范：只追加不删除；变更状态时保留历史行备注。
> ⚠️ 并行通道：NM-CUR-018 另建了 [docs/requirements/2026-09-13-渠道监控与负载均衡.md](../requirements/2026-09-13-渠道监控与负载均衡.md)（该文头把台账编号误写为 NM-CUR-017，实为 018）；NM-CUR-019 另有 [可升级方向研究](../worklog/2026-09-13-项目可升级方向研究.md)。三者主题重叠，待用户验收时定单一事实源；本表 R-alert-001≈其 M1+通知、R-quota-001≈api-monitor 借鉴、R-route-001/R-limit-001≈其 M2/M3。

| 需求 ID | 需求 | 来源 | 关联研究 | 关联任务 | 状态 | 登记 |
|---------|------|------|----------|----------|------|------|
| R-alert-001 | 告警通知：渠道冷却/成员耗尽/健康探测失败/上游公告与价格变化 → webhook+SMTP 推送，含发送前真实测试 | 对标研究 G1+G6（api-monitor 规则引擎） | [T-research-004] | - | 待确认 | cursor-local 代录 2026-09-13 |
| R-sec-001 | 凭据静态加密：渠道 key/代理密码 AES-GCM 入库，备份导出默认脱敏 | 对标研究 G3（api-monitor APP_SECRET）；现网 `ChannelDetailGet` 明文 | - | T-sec-001(存量,密钥文件移出) | 待确认 | cursor-local 代录 2026-09-13 |
| R-quota-001 | 上游余额/额度监控：new-api/sub2api 类渠道用普通用户凭证查余额，低于阈值告警 | 对标研究 G2（api-monitor connectors） | [T-research-002] | - | 待确认 | cursor-local 代录 2026-09-13 |
| R-hedge-001 | 首字竞速：高峰期/冷启动时并发请求分组内排序靠前的多个成员取最快首字（用户 2026-09-16 指令；设计稿 docs/research/2026-09-16-T-hedge-001-首字竞速设计稿.md）| route | 已交付 |
| R-weight-001 | 权重模型：包月/按次/余额/倍率/质量多维加权决定成员分配（用户 2026-09-16 指令）| route | 已交付并面板可用（T-weight-001 机制、T-weight-002 计费四维、T-weight-003 面板字段与权重界面）；三个取舍点全部做成设置项（默认权重可改、质量权重决定「贵而快」能否胜出、包月用尽 demote/exclude）。未做：人工视觉验收 |
| R-route-001 | 路由策略扩展：GroupMode 新增 lowest_cost（数据现成），后续 lowest_latency | 对标研究 G4（litellm router_strategy） | [T-research-003],[T-route-003] | - | 🔶部分交付 | cursor-local 代录 2026-09-13；**NM-DS-003（2026-09-15）交付第一阶段**：GroupMode lowest_cost 价格定序（单位成本=价表 Input+Output，查不到或 0 价沉底），前端可选 + 三语文案；活体 A/B 实测模式确实改变选中成员（同批成员 lowest_cost→glm-5.3-flash vs failover→deepseek-v4-flash-0731），成本与价表算式吻合；lowest_latency/least_busy 未做 |
| R-limit-001 | Key 级限流：RPM/TPM 滑动窗口，内存实现不引 Redis | 对标研究 G5（litellm parallel_request_limiter） | - | - | 待确认 | cursor-local 代录 2026-09-13 |
| R-probe-001 | 周期健康探测：task ticker 空闲期最小请求探测冷却成员，恢复即通知 | 对标研究 G6 | [T-research-004] | - | 待确认 | cursor-local 代录 2026-09-13 |
| R-quota-002 | 余额归零停模型：余额/额度归零自动停用对应模型（授权/成员级），阈值告警，手动恢复 | 用户 2026-09-13 原话 | [T-research-002] | T-quota-001,T-quota-002（已登 ledger todo 待认领） | 待确认 | cursor-local 代录 2026-09-13（NM-CUR-107） |
| R-group-001 | 分组嵌套子分组：分组内可再设分组（树形层级），选路递归展开，防循环 | 用户 2026-09-13 原话 | - | T-group-001/002/003（已登 ledger todo 待认领） | 待确认 | cursor-local 代录 2026-09-13（NM-CUR-107） |
| R-pool-ext-001 | 号池高度扩展性：基于已有的抽象出可插拔适配器 + 成批 API 接口，供各种反代工具包扩展（用户 2026-09-16 原话）| pool/api | 🔶进行中 | T-pool-ext-001（第一片：注册表 + 统一视图 + 只读三接口）；后续批次见 docs/requirements/2026-09-16-号池扩展性-design.md §4；四个取舍待用户拍板（声明式注册的安全边界 / 只读 APIKey 通道 / 通用 sync 抽象 / 首批接哪些工具包）| dsh-local 登记 2026-09-16 |
| R-acct-001 | 官方账号授权接入：OpenAI/Gemini/Claude 经官方授权链接接入，扫码后自动读取套餐档位/健康与 5H、7D 可用窗口 | 用户 2026-09-13 原话（官方账号三类） | - | T-acct-001（已登 ledger todo 待认领） | 待确认 | cursor-local 代录 2026-09-13（NM-CUR-123） |
| R-acct-002 | 中转站用户登录：New API 用户账号密码登录（/api/user/login+/api/user/self）；Sub2Api 用户邮箱密码登录（API Keys/订阅/platform quotas） | 用户 2026-09-13 原话（NA/S2 用户登录） | - | T-acct-002（已登 ledger todo 待认领） | 待确认 | cursor-local 代录 2026-09-13（NM-CUR-123） |
| R-acct-003 | Token/Key 健康监控：New API Token 单 token 用量可用性；Sub2Api Token 经 /v1/models 探测；OpenAI Admin Key 经 costs API 读组织成本；OpenAI Key 经 /v1/models；Anthropic Key 经 /v1/models | 用户 2026-09-13 原话（API Key/Token 五类） | [T-research-002] | T-acct-003（已登 ledger todo 待认领） | 待确认 | cursor-local 代录 2026-09-13（NM-CUR-123） |
| R-acct-004 | 手动与通用余额：手动订阅（官方无接口时录套餐余额有效期）；通用 HTTP 自定义 JSON 余额接口 | 用户 2026-09-13 原话（M/HTTP） | - | T-acct-004（已登 ledger todo 待认领） | 待确认 | cursor-local 代录 2026-09-13（NM-CUR-123） |
| R-pool-002 | 官方账号号池转发：Google/ChatGPT 官方账号直接当号池做转发，轮询+均衡请求+协议转化 | 用户 2026-09-13 原话 | - | T-pool-002,T-route-002,T-proto-001（已登 ledger todo 待认领） | 待确认 | cursor-local 代录 2026-09-13（NM-CUR-123） |
| R-pool-003 | 仓库号池选型报告：是否为仓库补一份 ChatGPT/Gemini 账号池开源候选+并入方案（原 T-pool-001 残门转化） | NM-CUR-182 核验（需求台无此需求，T-pool-001 自证路径） | - | T-pool-001（收敛出口，不再卡部署测试） | 待确认 | cursor-local 代录 2026-09-14（NM-CUR-182） |
| R-login-001 | 手动登录跳转页：NA/S2 用户登录加可生成页面跳转手动登录，防验证码拦截 | 用户 2026-09-13 原话 | - | T-acct-005（已登 ledger todo 待认领） | 待确认 | cursor-local 代录 2026-09-13（NM-CUR-123） |
| R-test-001 | 本地备份隔离测试：导入`../octopus-本地数据/数据备份(勿提交).json`（12 渠道/340 分组/615 项）到隔离库，验证登录/渠道/分组/协议转化，不碰生产 | 用户 2026-09-13 原话 | - | T-test-001（已登 ledger todo 待认领） | 待确认 | cursor-local 代录 2026-09-13（NM-CUR-123） |
| R-log-002 | 本地日志错误分析：读`../octopus-本地数据/`7 份日志包，定位历史错误并修复 | 用户 2026-09-13 原话 | - | T-log-002（已登 ledger todo 待认领）；本轮补做 T-timeout-001（流式无进展上限：Caddy 日志 300–760s 挂死的根因） | ✅已交付 | cursor-local 代录 2026-09-13（NM-CUR-123）；NM-DS-034 补做 |

## 存量登记（2026-09-12 bootstrap 遗留，已在 ledger）

- T-docs-001 Python 原型去向 ｜ T-sec-001 备份密钥外移 ｜ T-env-001 本机环境验证 —— 见 `ledger.md`，不重复登记。

---

> **状态收口（NM-CUR-230，2026-09-15）**：上表全部 R-* 条目的当前落地状态已在
> [2026-09-15-需求事实源整合.md](../requirements/2026-09-15-需求事实源整合.md) §2 逐条标注
> （✅ 已交付 ｜ 🔶 部分交付 ｜ ⬜ 未立项 ｜ ⏸ 待拍板），该文件为后续问询的单一事实源；
> 本表历史行一字未改。要点：R-quota-001/002、R-limit-001、R-group-001、R-acct-001~005、
> R-test-001、R-log-002 已交付；R-alert-001（webhook 部分）、R-route-001（加权轮询部分）、
> R-sec-001（官方账号加密部分）部分交付；R-probe-001 未立项、R-pool-002 todo、R-pool-003 待拍板。

| R-pool-ext-002 | 号池把渠道凭据（中转站/反代站点）纳入统一视图 | 用户要求「号池加入高度扩展性…可用于各种反代工具包扩展」并给出站点清单（数据备份，未提交）| ✅ 已交付：kind=channel（list/get/probe/refresh/toggle），探活/余额/启停复用既有 health/op；账号级（登录态）视图需存站点账号密码，待拍板 | T-pool-ext-009 / NM-DS-030 |

| R-alert-002 | 通知渠道新增 Server酱，并要求"遇到问题通知我" | 用户给出 SendKey（SCT...）与 serverchan-sdk 用法，要求继续完成要求并在遇到问题时通知 | ✅ 已交付：serverchan 渠道（表单推送 / code+errno 判定 / SendKey 优先走环境变量 / 接口不回显）；面板设置页可勾选与填写；活体套件 +3 用例；一次性真实推送已验证 | T-alert-002 / NM-DS-031 |
| R-test-001 | 本地备份隔离测试（套件化落地 + 顺带查出迁移缺陷）| 用户 2026-09-13 原话（既有行见上表第 28 行，此行只记落地方式）| ✅ 已交付为**可反复执行**的活体套件 `scripts/api-tests/run_backup_import_test.py`（22/22，已进矩阵）：独立实例（`--config` 指定 13304/11235 与 `%TEMP%` 独立库）+ 脱敏副本（渠道 base_url 全指本地 mock）+ 关掉会自己出网的后台任务（余额扫描/主动探活/告警目标）；导入前有硬安全闸门（先证明临时库已生成，否则绝不导入）。用用户自己的分组名跑通非流式/流式/三协议入口，并断言生产库六表计数与渠道指纹不变 | T-test-015 / T-migrate-001 / T-test-016 |
| | 说明：导入的是**脱敏副本**（只把 12 个渠道的 base_url 改指本地桩、并关掉出网类设置，其余逐字保留）。这是刻意的安全设计——备份里全是用户付费真站点，而产品自带"余额扫描（默认 5 分钟一轮）"与"冷却成员主动探活（真实计费请求）"两条**不需要我发请求也会出网**的路径，直接导原件无法保证零真实调用。原件的结构有效性另由只读脚本 `scripts/backup/verify_backup.py` 覆盖（引用完整性 0 破损）。 | 2026-09-16 16:20 |
| R-upstream-001 | 查底层项目（上游 bestruirui/octopus）的 PR/issue，本项目存在的同源问题一并解决 | 用户 2026-09-17 原话：「然后再查看底层项目的PR有没有可以参考的，他们提出找到的问题如果我这个项目也有这个问题一并解决了」| ✅ 本轮交付：读上游 4 个 open PR / 46 个 merged PR / 15 个 open issue / 80 个 closed issue，逐条在本项目代码里核实并给出依据（见 worklog 2026-09-17 第二节表格）。**移植** PR #360（developer→system，本项目缺失）与 PR #344（导入孤儿行过滤，按本项目外键链适配）；**修掉** #388/#338（无限重试→尝试上限+退避+明确失败）与 #375（确定性错误分类）；**核实已修** #379（op/group.go:68 快照剔除 + balance/probe 双保险）与 #349（与取消记账同源）；**核实不适用** #361/#354（本项目无 NOT NULL 无默认值新增列、GroupItem.ChannelID 是 gorm:"-" 不落列）；**已兜住** #362（成员超时 + 尝试上限保证终态，且请求日志已落库）。|
| | 未做（需用户拍板）：400 类错误的两种取向（保留故障转移 vs fail-fast 回上游原文）待选；#387 见下行补记。|
| | 补记（2026-09-17 第二轮）：#387「多密钥模型列表不同时，协议全选会为不支持该模型的密钥创建授权」**已按 issue 的四条预期行为实现并交付**（worklog《授权矩阵按凭据支持收敛》，T-grant-001/002）——探测结论落进表单状态 `keyModels`（只收敛范围、不进提交载荷），全选跳过不可用组合、不可用组合置灰并写明原因、模型行分母只数可用凭据、提交前裁剪并 toast 报条数；口径为"未探测的凭据一律放行"，故不误伤手填模型与未探测凭据。验证：新增纯函数套件 `check_grants.js` 26/26（矩阵第 27 套）+ 面板产物标记 P8（8/8）+ `tsc --noEmit` 0 + eslint 改动文件 0 + `go vet` 0 + `go test ./...` 12 包全绿 + 完整矩阵 27 套 0 失败。| T-grant-001 / NM-DS-036 |
| | 补记（2026-09-17 第三轮）：上游剩余报告全部读完并结案（worklog 同篇第五节、T-upstream-002）——**#363**（转换路径没有提示缓存）同源存在但属转换能力边界，建议走同协议透传（与维护者处理一致），要转换路径也有缓存须合成 `cache_control`，属新功能未做；**#372**（上游 message 用非标准 Authorization 头导致 401）不是缺陷，绕过手段「自定义请求头」经代码核实同样可用；**#373**（流式中途故障下放 + 后两个模型不参与循环）两条均为技术限制/冷却设计，非缺陷。落后上游的 3 个提交（44f63cb 手动取消请求、00e0739 token speed、08d80c3 Logo）**均为功能/样式而非修复，未移植**，需要时可按要求移植。另新增活体断言 R7「客户端凭据不得透传给上游」（retry 套件 7/7）作为 #372 同类头部处理的实测证据。| T-upstream-002 / T-test-018 |
| | 补记（2026-09-17 第四轮）：把两条此前只有代码推导支撑的边界补成**活体测量**——① 客户端凭据不得落到上游：新增 `run_lossless_tests.py` 聚合断言，覆盖 3 客户端协议 × 3 上游协议位 + 1 条流式共 10 条路径，读数「上游请求 10 条, 携带渠道密钥 10 条, 携带客户端凭据 0 条」，与上轮 R7（chat 透传）互补；② 客户端断开即停上游（上游 #338 的正面证据）：mock 静默分支改为盯对端并记 `stall_peer_closed` 事件，新增 I8（客户端立即放弃 → 上游连接 0.0 秒关闭）与 I9（客户端在等 → 8.01 秒才关闭）作反向对照，服务端无进展上限一档为 2.0 秒，三档同源可对照。本轮无产品代码改动（行为已正确，只补证据）。| T-test-019 / T-test-020 / NM-DS-037 |
| | 补记（2026-09-17 第五轮，封闭性核对）：重新拉取上游全量条目（`gh api .../issues?state=all`，现存 387 条 issue/PR：今天新建 **0** 条，最新 #394 为 09-16，仍在上午已逐条核对过的窗口内）——输入集没有变化。更强的结论来自提交溯源：`git rev-list --count HEAD..origin/master` = **3**，`git log --oneline HEAD..origin/master` 只有 44f63cb（手动取消请求）、00e0739（token speed）、08d80c3（Logo）三个 **feat/style** 提交，而合并基 d5a893f 是 HEAD 的祖先（`git merge-base --is-ancestor` 成立）⇒ **上游除这 3 个非修复提交以外的全部历史提交都在本树内**，因此「上游修过而我没修」的同源缺陷在结构上不可能存在：除非上游产生新的提交或新的 issue/PR，R-upstream-001 的问题空间已经封闭（这 3 个功能提交待用户点名再移植）。| T-upstream-002 |
| | 补记（2026-09-17 第六轮：判据灵敏度，T-test-021 / T-devrole-001）：对第 8 回合的六处守卫做变异检查 —— 把取消记账、尝试上限、成员故障分类、两条 developer 归一化、全成员被拒终止逐个改回旧行为/改值，要求指定用例变红，六条全部如期变红（详见 worklog《判据灵敏度（变异检查）》）。**过程中查出一条真实漏网路径并已修**：上游 #360「developer→system」的移植只盖了 chat 的 messages 数组，客户端 chat 入、上游 Responses 出时 developer 原样发给上游（分协议位探针实测 developer 残留 True）；修复为按上游协议选角色路径（chat→messages、responses→input），新增单测 2 例 + 活体用例 R9，修复后残留 False。验证：go vet 0、go test 12 包全绿、完整矩阵 27 套 0 失败（retry 9/9、streamidle 9/9）。 | 2026-09-17 15:50:16 |
| | 补记（2026-09-17 第七轮：把跨协议不变量补进矩阵，T-devrole-002）：上一个 developer 漏网路径的教训不是「某条断言写错」, 而是**没有用例逐个路径看角色** —— 无损矩阵只断言标记文本在不在。本轮把角色契约补进 3×3 矩阵（chat / responses 客户端用 developer 携带指令 × 3 个上游协议位）: 断言上游正文里不再出现 developer 且指令文本仍在, 读数 6 条路径全部干净（lossless 17/17); 并加两处变异（注释直通钩子 / 注释转换钩子）确认新判据会变红。同一手法可继续用于其它结构性不变量（字段名、默认值、工具 schema 形状）。 | 2026-09-17 16:04:22 |
| R-smart-route-001 | 类似阶跃 Step Router 的智能路由：客户端只填一个模型名，按请求复杂度自动分流 | 用户 2026-09-17 原话：「我需要大概和这个相似的功能 https://platform.stepfun.com/docs/zh/guides/models/step-router」 | ✅ 已交付：新增分组模式 `smart` —— 三项特征（消息轮数 / 输入量 / 工具数量）等权评分 0..100，阈值 `relay_config.smart_route_threshold`（1..100，缺省 50）以上走靠前成员（决策引擎档）、以下走靠后成员（执行引擎档）；档内沿用既有选路与冷却/探测语义，档内无人可转发时回退全体成员（省钱不能变成不可用）；面板含模式选项与阈值输入（仅 smart 显示）+ 三语文案；单测 7 例 + 活体套件 8/8（含阈值两侧与档不可用回退），全量矩阵 28 套 0 失败 | T-smart-001 / T-smart-002 / T-smart-003 / NM-DS-040 |
| | 补记（2026-09-17 智能路由第八轮：与竞速的交叉点 + 判据灵敏度）：查出并修复「首字竞速绕过档位」—— 竞速候选原先不收敛档位, 简单请求会把靠前的强成员一起拉进竞速（多发一份请求还可能花贵渠道的钱）, 现已改为档内竞速、档内无人时回退全体（T-smart-004，活体 S8：简单请求两次尝试都在快渠道、复杂请求两次都在强渠道）。并对四处守卫做变异检查（档位切分 SM1→S1、评分维度 SM2→S3、回退 SM3→S5、竞速收敛 SM4→S8），四条均如期变红（T-smart-005）。 | 2026-09-17 18:30:11 |
| | 补记（2026-09-17 智能路由第九轮：子分组档位粒度）：上一版档位按**展平后**的成员条数对半切, 与本文件推荐的「用子分组当两条链 = 两档」用法冲突 —— 链长不等时会把执行链成员算进决策档, 简单请求也会因为档内只剩必失败成员而回退到强渠道（成本泄漏）。现改为按**顶层成员**切分、子分组整条链归档（T-smart-006）；判据 S9 第一版装置判不出来（变异 SM5 全绿）, 重做后变异如期变红（简单请求尝试 ['mock-bad','mock-bad','mock-good'] vs 正确实现 ['mock-plain']）。 | 2026-09-17 19:24:01 |
| R-upstream-002 | 上游 #395（重试详情/首字响应）对照 | 对照完成，取最小切片实现 | 2026-09-17 21:01:17 |
| R-balance-001 | 各 apikey 与各中转的剩余次数/余额自动折算成总余额，且用标准请求协议可查 | 用户 2026-09-17 原话：「我还需要进行所有的apikey以及各个中转的剩余次数和余额根据计算自动转化成总余额，请求api的时候可以通过标准的请求协议查看到这个余额」 | worklog《总余额聚合（标准协议余额查询）》 | [T-balance-001] | 已立项 | 2026-09-17 22:39:58 |
| | 口径（改动时不要破坏）：① hard_limit_usd = 总余额 + 本 Key 已花，total_usage（分）= 本 Key 已花 × 100，客户端做的减法正好等于总余额；② 未知余额的渠道只进 unknown_channels，不进总额；③ Key 的 MaxCost 是「允许花多少」不是「上游还有多少」，不得计入总额；④ 剩余额度不为负，口径非法（<=0 / 空货币名）在设置层直接拒绝。未做（等用户决定）：日期区间过滤（只有累计口径，假装支持会给错数）、货币换算（currency 只是显示名）、Key 级余额告警。 |
| | 结论：首字响应（TTFT）本项目**已有**（RelayLog.FirstByteMs → 卡片「首字时间」→ 导出「首字节(ms)」）；「重试详情」上游维护者明确暂不考虑，本项目只补「上游轮次」(attempts) 一个数（够回答「换没换人」），不建每轮明细子表。判据 retry R10 + panel P10 + 导出单测。 | 2026-09-17 21:01:17 |

## R-decide-001 用户对 I0 待决清单的裁决：「我全都要」（2026-09-18）

用户原话：「我全都要」。十个待决事项的裁决与执行状态如下（按我承诺的顺序推进）：

| # | 事项 | 裁决 | 状态 |
| --- | --- | --- | --- |
| ① | 智能路由随下轮上线 | 要 | 待上线（代码就绪） |
| ② | 判定理由写进日志/响应头 | **两者都要** | 已交付（T-decision-001 + 面板 T-decision-002） |
| ③ | 显式指定决策引擎渠道 | 要 | 未做（改动面最大：成员字段+迁移+界面） |
| ④ | 400 类错误取向 | **两种都要 → 做成开关** | 本轮已交付（T-retry-003） |
| ⑤ | 既有分组统一开 member_stream_idle_timeout_seconds | 要 | 本轮已交付（T-group-002，迁移 014） |
| ⑥ | 渠道凭据静态加密 R-sec-001 余项 | 要 | 待用户给密钥来源（环境变量 vs 数据目录密钥文件） |
| ⑦ | 手动订阅独立表存储位置 | 要 | 待用户拍板 |
| ⑧ | 号池报告补全 | 要 | 已交付（T-pool-004：R-pool-003 全文） |
| ⑨ | 线上 key 轮换 T-sec-001 → 上线 T-deploy-002 | 要 | 待用户给部署接入方式（本地已领先 fork 105+ 个提交，未推送未上线） |
| ⑩ | 号池扩展层四个边界 | 都要 | 未做（声明式适配器安全边界/只读 APIKey 渠道/通用入口→凭据同步/先接哪些工具包） |

「全都要」对"二选一"类事项的解读：**两种都要、默认保持既有行为、另一种做成开关**——不做取向上的取舍，也绝不改变已上线语义。

> **R-pool-ext-001 补记（第四轮，2026-09-18）**：用户对「四个边界」回复「我全都要」，四条都已落地——① 运行时声明式适配器**开放但受硬约束**（域名白名单为空即一律拒绝 / 只读能力位 / 不跟 3xx 跳转 / 超时与响应体上限 / kind 必须 custom- 前缀 / 内置 kind 不可覆盖不可移除）；② 外部工具包有**只读 APIKey 通道**（转发口 /v1/pool/*，写动作不存在）；③ 通用 sync 已确认为既有 Syncer + 能力位门禁，方向固定单向（条目→渠道凭据），声明式适配器不可能声明 sync；④ 首批模板按只读运维信息给出（new-api / one-api / sub2api 三形态，GET /api/v1/pool/adapters/templates）。台账 T-pool-005/006/007；活体 9/9、变异 3 条如期变红。
